.TH "omap_rproc_enable_timers" 9 "omap_rproc_enable_timers" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_rproc_enable_timers \- enable the timers for a remoteproc
.SH SYNOPSIS
.B "int" omap_rproc_enable_timers
.BI "(struct rproc *rproc "  ","
.BI "bool configure "  ");"
.SH ARGUMENTS
.IP "rproc" 12
handle of a remote processor
.IP "configure" 12
boolean flag used to acquire and configure the timer handle
.SH "DESCRIPTION"
This function is used primarily to enable the timers associated with
a remoteproc. The configure flag is provided to allow the driver to
to either acquire and start a timer (during device initialization) or
to just start a timer (during a resume operation).
.SH "RETURN"
0 on success, otherwise an appropriate failure
