.TH "dtpm_update_power" 9 "dtpm_update_power" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dtpm_update_power \- Update the power on the dtpm
.SH SYNOPSIS
.B "int" dtpm_update_power
.BI "(struct dtpm *dtpm "  ","
.BI "u64 power_min "  ","
.BI "u64 power_max "  ");"
.SH ARGUMENTS
.IP "dtpm" 12
a pointer to a dtpm structure to update
.IP "power_min" 12
a u64 representing the new power_min value
.IP "power_max" 12
a u64 representing the new power_max value
.SH "DESCRIPTION"
Function to update the power values of the dtpm node specified in
parameter. These new values will be propagated to the tree.
.SH "RETURN"
zero on success, -EINVAL if the values are inconsistent
