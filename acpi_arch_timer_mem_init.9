.TH "acpi_arch_timer_mem_init" 9 "acpi_arch_timer_mem_init" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_arch_timer_mem_init \- Get the info of all GT blocks in GTDT table.
.SH SYNOPSIS
.B "int" acpi_arch_timer_mem_init
.BI "(struct arch_timer_mem *timer_mem "  ","
.BI "int *timer_count "  ");"
.SH ARGUMENTS
.IP "timer_mem" 12
The pointer to the array of struct arch_timer_mem for returning
the result of parsing. The element number of this array should
be platform_timer_count(the total number of platform timers).
.IP "timer_count" 12
It points to a integer variable which is used for storing the
number of GT blocks we have parsed.
.SH "RETURN"
0 if success, -EINVAL/-ENODEV if error.
