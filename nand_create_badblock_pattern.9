.TH "nand_create_badblock_pattern" 9 "nand_create_badblock_pattern" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
nand_create_badblock_pattern \- [INTERN] Creates a BBT descriptor structure
.SH SYNOPSIS
.B "int" nand_create_badblock_pattern
.BI "(struct nand_chip *this "  ");"
.SH ARGUMENTS
.IP "this" 12
NAND chip to create descriptor for
.SH "DESCRIPTION"
This function allocates and initializes a nand_bbt_descr for BBM detection
based on the properties of \fIthis\fP. The new descriptor is stored in
this->badblock_pattern. Thus, this->badblock_pattern should be NULL when
passed to this function.
