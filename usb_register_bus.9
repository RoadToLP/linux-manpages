.TH "usb_register_bus" 9 "usb_register_bus" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
usb_register_bus \- registers the USB host controller with the usb core
.SH SYNOPSIS
.B "int" usb_register_bus
.BI "(struct usb_bus *bus "  ");"
.SH ARGUMENTS
.IP "bus" 12
pointer to the bus to register
.SH "CONTEXT"
task context, might sleep.
.SH "DESCRIPTION"
Assigns a bus number, and links the controller into usbcore data
structures so that it can be seen by scanning the bus list.
.SH "RETURN"
0 if successful. A negative error code otherwise.
