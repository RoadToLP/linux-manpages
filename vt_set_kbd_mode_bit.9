.TH "vt_set_kbd_mode_bit" 9 "vt_set_kbd_mode_bit" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vt_set_kbd_mode_bit \- read keyboard status bits
.SH SYNOPSIS
.B "void" vt_set_kbd_mode_bit
.BI "(int console "  ","
.BI "int bit "  ");"
.SH ARGUMENTS
.IP "console" 12
console to read from
.IP "bit" 12
mode bit to read

Set a vt mode bit. We do this without locking so the
caller must be sure that there are no synchronization needs
