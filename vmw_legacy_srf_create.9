.TH "vmw_legacy_srf_create" 9 "vmw_legacy_srf_create" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vmw_legacy_srf_create \- Create a device surface as part of the resource validation process.
.SH SYNOPSIS
.B "int" vmw_legacy_srf_create
.BI "(struct vmw_resource *res "  ");"
.SH ARGUMENTS
.IP "res" 12
Pointer to a struct vmw_surface.
.SH "DESCRIPTION"
If the surface doesn't have a hw id.

Returns -EBUSY if there wasn't sufficient device resources to
complete the validation. Retry after freeing up resources.

May return other errors if the kernel is out of guest resources.
