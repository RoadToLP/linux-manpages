.TH "acpi_device_wakeup_enable" 9 "acpi_device_wakeup_enable" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_device_wakeup_enable \- Enable wakeup functionality for device.
.SH SYNOPSIS
.B "int" acpi_device_wakeup_enable
.BI "(struct acpi_device *adev "  ","
.BI "u32 target_state "  ");"
.SH ARGUMENTS
.IP "adev" 12
ACPI device to enable wakeup functionality for.
.IP "target_state" 12
State the system is transitioning into.
.SH "DESCRIPTION"
Enable the GPE associated with \fIadev\fP so that it can generate wakeup signals
for the device in response to external (remote) events and enable wakeup
power for it.

Callers must ensure that \fIadev\fP is a valid ACPI device node before executing
this function.
