.TH "svc_rdma_xb_dma_map" 9 "svc_rdma_xb_dma_map" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
svc_rdma_xb_dma_map \- DMA map all segments of an xdr_buf
.SH SYNOPSIS
.B "int" svc_rdma_xb_dma_map
.BI "(const struct xdr_buf *xdr "  ","
.BI "void *data "  ");"
.SH ARGUMENTS
.IP "xdr" 12
xdr_buf containing portion of an RPC message to transmit
.IP "data" 12
pointer to arguments
.SH "RETURN"
0 if DMA mapping was successful
-EIO if DMA mapping failed
.SH "DESCRIPTION"
On failure, any DMA mappings that have been already done must be
unmapped by the caller.
