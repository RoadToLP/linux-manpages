.TH "it821x_firmware_command" 9 "it821x_firmware_command" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
it821x_firmware_command \- issue firmware command
.SH SYNOPSIS
.B "u8 *" it821x_firmware_command
.BI "(struct ata_port *ap "  ","
.BI "u8 cmd "  ","
.BI "int len "  ");"
.SH ARGUMENTS
.IP "ap" 12
IT821x port to interrogate
.IP "cmd" 12
command
.IP "len" 12
length

Issue firmware commands expecting data back from the controller. We
use this to issue commands that do not go via the normal paths. Other
commands such as 0xFC can be issued normally.
