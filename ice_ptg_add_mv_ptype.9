.TH "ice_ptg_add_mv_ptype" 9 "ice_ptg_add_mv_ptype" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_ptg_add_mv_ptype \- Adds/moves ptype to a particular packet type group
.SH SYNOPSIS
.B "enum ice_status" ice_ptg_add_mv_ptype
.BI "(struct ice_hw *hw "  ","
.BI "enum ice_block blk "  ","
.BI "u16 ptype "  ","
.BI "u8 ptg "  ");"
.SH ARGUMENTS
.IP "hw" 12
pointer to the hardware structure
.IP "blk" 12
HW block
.IP "ptype" 12
the ptype to add or move
.IP "ptg" 12
the PTG to add or move the ptype to
.SH "DESCRIPTION"
This function will either add or move a ptype to a particular PTG depending
on if the ptype is already part of another group. Note that using a
a destination PTG ID of ICE_DEFAULT_PTG (0) will move the ptype to the
default PTG.
