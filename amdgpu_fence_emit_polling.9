.TH "amdgpu_fence_emit_polling" 9 "amdgpu_fence_emit_polling" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
amdgpu_fence_emit_polling \- emit a fence on the requeste ring
.SH SYNOPSIS
.B "int" amdgpu_fence_emit_polling
.BI "(struct amdgpu_ring *ring "  ","
.BI "uint32_t *s "  ","
.BI "uint32_t timeout "  ");"
.SH ARGUMENTS
.IP "ring" 12
ring the fence is associated with
.IP "s" 12
resulting sequence number
.IP "timeout" 12
the timeout for waiting in usecs
.SH "DESCRIPTION"
Emits a fence command on the requested ring (all asics).
Used For polling fence.
Returns 0 on success, -ENOMEM on failure.
