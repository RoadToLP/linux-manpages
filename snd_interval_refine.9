.TH "snd_interval_refine" 9 "snd_interval_refine" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_interval_refine \- refine the interval value of configurator
.SH SYNOPSIS
.B "int" snd_interval_refine
.BI "(struct snd_interval *i "  ","
.BI "const struct snd_interval *v "  ");"
.SH ARGUMENTS
.IP "i" 12
the interval value to refine
.IP "v" 12
the interval value to refer to
.SH "DESCRIPTION"
Refines the interval value with the reference value.
The interval is changed to the range satisfying both intervals.
The interval status (min, max, integer, etc.) are evaluated.
.SH "RETURN"
Positive if the value is changed, zero if it's not changed, or a
negative error code.
