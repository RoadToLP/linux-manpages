.TH "batadv_get_real_netdevice" 9 "batadv_get_real_netdevice" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_get_real_netdevice \- check if the given netdev struct is a virtual interface on top of another 'real' interface
.SH SYNOPSIS
.B "struct net_device *" batadv_get_real_netdevice
.BI "(struct net_device *netdev "  ");"
.SH ARGUMENTS
.IP "netdev" 12
the device to check
.SH "DESCRIPTION"
Callers must hold the rtnl semaphore. You may want \fBbatadv_get_real_netdev\fP
instead of this.
.SH "RETURN"
the 'real' net device or the original net device and NULL in case
of an error.
