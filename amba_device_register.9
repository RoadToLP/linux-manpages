.TH "amba_device_register" 9 "amba_device_register" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
amba_device_register \- register an AMBA device
.SH SYNOPSIS
.B "int" amba_device_register
.BI "(struct amba_device *dev "  ","
.BI "struct resource *parent "  ");"
.SH ARGUMENTS
.IP "dev" 12
AMBA device to register
.IP "parent" 12
parent memory resource

Setup the AMBA device, reading the cell ID if present.
Claim the resource, and register the AMBA device with
the Linux device manager.
