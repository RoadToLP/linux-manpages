.TH "lpfc_fcp_imax_store" 9 "lpfc_fcp_imax_store" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_fcp_imax_store \- 
.SH SYNOPSIS
.B "ssize_t" lpfc_fcp_imax_store
.BI "(struct device *dev "  ","
.BI "struct device_attribute *attr "  ","
.BI "const char *buf "  ","
.BI "size_t count "  ");"
.SH ARGUMENTS
.IP "dev" 12
class device that is converted into a Scsi_host.
.IP "attr" 12
device attribute, not used.
.IP "buf" 12
string with the number of fast-path FCP interrupts per second.
.IP "count" 12
unused variable.
.SH "DESCRIPTION"
If val is in a valid range [636,651042], then set the adapter's
maximum number of fast-path FCP interrupts per second.
.SH "RETURN"
length of the buf on success if val is in range the intended mode
is supported.
-EINVAL if val out of range or intended mode is not supported.
