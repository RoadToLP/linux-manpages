.TH "unassign_domain_store" 9 "unassign_domain_store" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
unassign_domain_store \- 
.SH SYNOPSIS
.B "ssize_t" unassign_domain_store
.BI "(struct device *dev "  ","
.BI "struct device_attribute *attr "  ","
.BI "const char *buf "  ","
.BI "size_t count "  ");"
.SH ARGUMENTS
.IP "dev" 12
the matrix device
.IP "attr" 12
the mediated matrix device's unassign_domain attribute
.IP "buf" 12
a buffer containing the AP queue index (APQI) of the domain to
be unassigned
.IP "count" 12
the number of bytes in \fIbuf\fP
.SH "DESCRIPTION"
Parses the APQI from \fIbuf\fP and clears the corresponding bit in the
mediated matrix device's AQM.

Returns the number of bytes processed if the APQI is valid; otherwise,
returns one of the following errors:
-EINVAL if the APQI is not a number
-ENODEV if the APQI exceeds the maximum value configured for the system
