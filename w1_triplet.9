.TH "w1_triplet" 9 "w1_triplet" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
w1_triplet \- * Does a triplet - used for searching ROM addresses.
.SH SYNOPSIS
.B "u8" w1_triplet
.BI "(struct w1_master *dev "  ","
.BI "int bdir "  ");"
.SH ARGUMENTS
.IP "dev" 12
the master device
.IP "bdir" 12
the bit to write if both id_bit and comp_bit are 0
.SH "DESCRIPTION"
Return bits:
bit 0 = id_bit
bit 1 = comp_bit
bit 2 = dir_taken

If both bits 0 & 1 are set, the search should be restarted.
.SH "RETURN"
bit fields - see above
