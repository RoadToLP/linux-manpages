.TH "bnx2i_drop_session" 9 "bnx2i_drop_session" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
bnx2i_drop_session \- notifies iscsid of connection error.
.SH SYNOPSIS
.B "void" bnx2i_drop_session
.BI "(struct iscsi_cls_session *cls_session "  ");"
.SH ARGUMENTS
.IP "cls_session" 12
iscsi cls session pointer
.SH "DESCRIPTION"
This notifies iscsid that there is a error, so it can initiate
recovery.

This relies on caller using the iscsi class iterator so the object
is refcounted and does not disapper from under us.
