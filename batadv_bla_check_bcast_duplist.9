.TH "batadv_bla_check_bcast_duplist" 9 "batadv_bla_check_bcast_duplist" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_bla_check_bcast_duplist \- Check if a frame is in the broadcast dup.
.SH SYNOPSIS
.B "bool" batadv_bla_check_bcast_duplist
.BI "(struct batadv_priv *bat_priv "  ","
.BI "struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "skb" 12
contains the bcast_packet to be checked
.SH "DESCRIPTION"
Check if it is on our broadcast list. Another gateway might have sent the
same packet because it is connected to the same backbone, so we have to
remove this duplicate.
.SH "RETURN"
true if a packet is in the duplicate list, false otherwise.
