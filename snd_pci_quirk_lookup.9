.TH "snd_pci_quirk_lookup" 9 "snd_pci_quirk_lookup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_pci_quirk_lookup \- look up a PCI SSID quirk list
.SH SYNOPSIS
.B "const struct snd_pci_quirk *" snd_pci_quirk_lookup
.BI "(struct pci_dev *pci "  ","
.BI "const struct snd_pci_quirk *list "  ");"
.SH ARGUMENTS
.IP "pci" 12
pci_dev handle
.IP "list" 12
quirk list, terminated by a null entry
.SH "DESCRIPTION"
Look through the given quirk list and finds a matching entry
with the same PCI SSID.  When subdevice is 0, all subdevice
values may match.

Returns the matched entry pointer, or NULL if nothing matched.
