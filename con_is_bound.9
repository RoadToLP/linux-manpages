.TH "con_is_bound" 9 "con_is_bound" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
con_is_bound \- checks if driver is bound to the console
.SH SYNOPSIS
.B "int" con_is_bound
.BI "(const struct consw *csw "  ");"
.SH ARGUMENTS
.IP "csw" 12
console driver
.SH "RETURN"
zero if unbound, nonzero if bound
.SH "DESCRIPTION"
Drivers can call this and if zero, they should release
all resources allocated on \fBcon_startup\fP
