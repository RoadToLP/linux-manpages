.TH "batadv_get_vid" 9 "batadv_get_vid" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_get_vid \- extract the VLAN identifier from skb if any
.SH SYNOPSIS
.B "unsigned short" batadv_get_vid
.BI "(struct sk_buff *skb "  ","
.BI "size_t header_len "  ");"
.SH ARGUMENTS
.IP "skb" 12
the buffer containing the packet
.IP "header_len" 12
length of the batman header preceding the ethernet header
.SH "RETURN"
VID with the BATADV_VLAN_HAS_TAG flag when the packet embedded in the
skb is vlan tagged. Otherwise BATADV_NO_FLAGS.
