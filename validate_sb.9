.TH "validate_sb" 9 "validate_sb" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
validate_sb \- validate superblock node.
.SH SYNOPSIS
.B "int" validate_sb
.BI "(struct ubifs_info *c "  ","
.BI "struct ubifs_sb_node *sup "  ");"
.SH ARGUMENTS
.IP "c" 12
UBIFS file-system description object
.IP "sup" 12
superblock node
.SH "DESCRIPTION"
This function validates superblock node \fIsup\fP. Since most of data was read
from the superblock and stored in \fIc\fP, the function validates fields in \fIc\fP
instead. Returns zero in case of success and -EINVAL in case of validation
failure.
