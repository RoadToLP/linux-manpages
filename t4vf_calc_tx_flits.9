.TH "t4vf_calc_tx_flits" 9 "t4vf_calc_tx_flits" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
t4vf_calc_tx_flits \- calculate the number of flits for a packet TX WR
.SH SYNOPSIS
.B "unsigned int" t4vf_calc_tx_flits
.BI "(const struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "skb" 12
the packet

Returns the number of flits needed for a TX Work Request for the
given Ethernet packet, including the needed WR and CPL headers.
