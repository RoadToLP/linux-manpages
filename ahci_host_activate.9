.TH "ahci_host_activate" 9 "ahci_host_activate" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ahci_host_activate \- start AHCI host, request IRQs and register it
.SH SYNOPSIS
.B "int" ahci_host_activate
.BI "(struct ata_host *host "  ","
.BI "struct scsi_host_template *sht "  ");"
.SH ARGUMENTS
.IP "host" 12
target ATA host
.IP "sht" 12
scsi_host_template to use when registering the host

LOCKING:
Inherited from calling layer (may sleep).
.SH "RETURN"
0 on success, -errno otherwise.
