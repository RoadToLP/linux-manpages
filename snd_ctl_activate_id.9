.TH "snd_ctl_activate_id" 9 "snd_ctl_activate_id" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_ctl_activate_id \- activate/inactivate the control of the given id
.SH SYNOPSIS
.B "int" snd_ctl_activate_id
.BI "(struct snd_card *card "  ","
.BI "struct snd_ctl_elem_id *id "  ","
.BI "int active "  ");"
.SH ARGUMENTS
.IP "card" 12
the card instance
.IP "id" 12
the control id to activate/inactivate
.IP "active" 12
non-zero to activate
.SH "DESCRIPTION"
Finds the control instance with the given id, and activate or
inactivate the control together with notification, if changed.
The given ID data is filled with full information.
.SH "RETURN"
0 if unchanged, 1 if changed, or a negative error code on failure.
