.TH "snd_soc_find_dai" 9 "snd_soc_find_dai" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_soc_find_dai \- Find a registered DAI
.SH SYNOPSIS
.B "struct snd_soc_dai *" snd_soc_find_dai
.BI "(const struct snd_soc_dai_link_component *dlc "  ");"
.SH ARGUMENTS
.IP "dlc" 12
name of the DAI or the DAI driver and optional component info to match
.SH "DESCRIPTION"
This function will search all registered components and their DAIs to
find the DAI of the same name. The component's of_node and name
should also match if being specified.
.SH "RETURN"
pointer of DAI, or NULL if not found.
