.TH "changeset_dup_entry_check" 9 "changeset_dup_entry_check" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
changeset_dup_entry_check \- check for duplicate entries
.SH SYNOPSIS
.B "int" changeset_dup_entry_check
.BI "(struct overlay_changeset *ovcs "  ");"
.SH ARGUMENTS
.IP "ovcs" 12
Overlay changeset
.SH "DESCRIPTION"
Check changeset \fIovcs->cset\fP for multiple {add or delete} node entries for
the same node or duplicate {add, delete, or update} properties entries
for the same property.

Returns 0 on success, or -EINVAL if duplicate changeset entry found.
