.TH "subsys_dev_iter_init" 9 "subsys_dev_iter_init" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
subsys_dev_iter_init \- initialize subsys device iterator
.SH SYNOPSIS
.B "void" subsys_dev_iter_init
.BI "(struct subsys_dev_iter *iter "  ","
.BI "struct bus_type *subsys "  ","
.BI "struct device *start "  ","
.BI "const struct device_type *type "  ");"
.SH ARGUMENTS
.IP "iter" 12
subsys iterator to initialize
.IP "subsys" 12
the subsys we wanna iterate over
.IP "start" 12
the device to start iterating from, if any
.IP "type" 12
device_type of the devices to iterate over, NULL for all
.SH "DESCRIPTION"
Initialize subsys iterator \fIiter\fP such that it iterates over devices
of \fIsubsys\fP.  If \fIstart\fP is set, the list iteration will start there,
otherwise if it is NULL, the iteration starts at the beginning of
the list.
