.TH "verify_table_headers" 9 "verify_table_headers" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
verify_table_headers \- verify that the tables headers are as expected @tables - array of dfa tables to check (NOT NULL)
.SH SYNOPSIS
.B "int" verify_table_headers
.BI "(struct table_header **tables "  ","
.BI "int flags "  ");"
.SH ARGUMENTS
.IP "tables" 12
-- undescribed --
.IP "flags" 12
flags controlling what type of accept table are acceptable
.SH "DESCRIPTION"
Assumes dfa has gone through the first pass verification done by unpacking
.SH "NOTE"
this does not valid accept table values
.SH "RETURN"
0 else error code on failure to verify
