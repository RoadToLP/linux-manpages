.TH "omap_dev_ready" 9 "omap_dev_ready" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_dev_ready \- checks the NAND Ready GPIO line
.SH SYNOPSIS
.B "int" omap_dev_ready
.BI "(struct nand_chip *chip "  ");"
.SH ARGUMENTS
.IP "chip" 12
NAND chip object
.SH "DESCRIPTION"
Returns true if ready and false if busy.
