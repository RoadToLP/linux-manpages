.TH "ata_scsi_offline_dev" 9 "ata_scsi_offline_dev" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ata_scsi_offline_dev \- offline attached SCSI device
.SH SYNOPSIS
.B "int" ata_scsi_offline_dev
.BI "(struct ata_device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
ATA device to offline attached SCSI device for

This function is called from \fBata_eh_hotplug\fP and responsible
for taking the SCSI device attached to \fIdev\fP offline.  This
function is called with host lock which protects dev->sdev
against clearing.

LOCKING:
spin_lock_irqsave(host lock)
.SH "RETURN"
1 if attached SCSI device exists, 0 otherwise.
