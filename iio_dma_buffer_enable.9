.TH "iio_dma_buffer_enable" 9 "iio_dma_buffer_enable" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
iio_dma_buffer_enable \- Enable DMA buffer
.SH SYNOPSIS
.B "int" iio_dma_buffer_enable
.BI "(struct iio_buffer *buffer "  ","
.BI "struct iio_dev *indio_dev "  ");"
.SH ARGUMENTS
.IP "buffer" 12
IIO buffer to enable
.IP "indio_dev" 12
IIO device the buffer is attached to
.SH "DESCRIPTION"
Needs to be called when the device that the buffer is attached to starts
sampling. Typically should be the iio_buffer_access_ops enable callback.

This will allocate the DMA buffers and start the DMA transfers.
