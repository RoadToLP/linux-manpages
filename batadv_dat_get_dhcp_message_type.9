.TH "batadv_dat_get_dhcp_message_type" 9 "batadv_dat_get_dhcp_message_type" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_dat_get_dhcp_message_type \- get message type of a DHCP packet
.SH SYNOPSIS
.B "int" batadv_dat_get_dhcp_message_type
.BI "(struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "skb" 12
the DHCP packet to parse
.SH "DESCRIPTION"
Iterates over the DHCP options of the given DHCP packet to find a
DHCP Message Type option and parse it.

Caller needs to ensure that the given skb is a valid DHCP packet and
that the skb transport header is set correctly.
.SH "RETURN"
The found DHCP message type value, if found. -EINVAL otherwise.
