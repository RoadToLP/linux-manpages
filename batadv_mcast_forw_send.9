.TH "batadv_mcast_forw_send" 9 "batadv_mcast_forw_send" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_mcast_forw_send \- send packet to any detected multicast recipient
.SH SYNOPSIS
.B "int" batadv_mcast_forw_send
.BI "(struct batadv_priv *bat_priv "  ","
.BI "struct sk_buff *skb "  ","
.BI "unsigned short vid "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "skb" 12
the multicast packet to transmit
.IP "vid" 12
the vlan identifier
.SH "DESCRIPTION"
Sends copies of a frame with multicast destination to any node that signaled
interest in it, that is either via the translation table or the according
want-all flags. A transmission is performed via a batman-adv unicast packet
for each such destination node.

The given skb is consumed/freed.
.SH "RETURN"
NET_XMIT_DROP on memory allocation failure or if the protocol family
is neither IPv4 nor IPv6. NET_XMIT_SUCCESS otherwise.
