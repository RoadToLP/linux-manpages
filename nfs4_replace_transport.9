.TH "nfs4_replace_transport" 9 "nfs4_replace_transport" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
nfs4_replace_transport \- set up transport to destination server
.SH SYNOPSIS
.B "int" nfs4_replace_transport
.BI "(struct nfs_server *server "  ","
.BI "const struct nfs4_fs_locations *locations "  ");"
.SH ARGUMENTS
.IP "server" 12
export being migrated
.IP "locations" 12
fs_locations array
.SH "DESCRIPTION"
Returns zero on success, or a negative errno value.

The client tries all the entries in the "locations" array, in the
order returned by the server, until one works or the end of the
array is reached.
