.TH "ice_vf_rebuild_host_vlan_cfg" 9 "ice_vf_rebuild_host_vlan_cfg" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_vf_rebuild_host_vlan_cfg \- add VLAN 0 filter or rebuild the Port VLAN
.SH SYNOPSIS
.B "int" ice_vf_rebuild_host_vlan_cfg
.BI "(struct ice_vf *vf "  ");"
.SH ARGUMENTS
.IP "vf" 12
VF to add MAC filters for
.SH "DESCRIPTION"
Called after a VF VSI has been re-added/rebuilt during reset. The PF driver
always re-adds either a VLAN 0 or port VLAN based filter after reset.
