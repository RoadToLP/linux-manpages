.TH "tb_lc_dp_sink_alloc" 9 "tb_lc_dp_sink_alloc" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tb_lc_dp_sink_alloc \- Allocate DP sink
.SH SYNOPSIS
.B "int" tb_lc_dp_sink_alloc
.BI "(struct tb_switch *sw "  ","
.BI "struct tb_port *in "  ");"
.SH ARGUMENTS
.IP "sw" 12
Switch whose DP sink is allocated
.IP "in" 12
DP IN port the DP sink is allocated for
.SH "DESCRIPTION"
Allocate DP sink for \fIin\fP via LC SNK_ALLOCATION registers. If the
resource is available and allocation is successful returns 0. In all
other cases returs negative errno. In particular -EBUSY is returned if
the resource was not available.
