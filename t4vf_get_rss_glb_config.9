.TH "t4vf_get_rss_glb_config" 9 "t4vf_get_rss_glb_config" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
t4vf_get_rss_glb_config \- retrieve adapter RSS Global Configuration
.SH SYNOPSIS
.B "int" t4vf_get_rss_glb_config
.BI "(struct adapter *adapter "  ");"
.SH ARGUMENTS
.IP "adapter" 12
the adapter

Retrieves global RSS mode and parameters with which we have to live
and stores them in the \fIadapter\fP's RSS parameters.
