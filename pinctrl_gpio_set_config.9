.TH "pinctrl_gpio_set_config" 9 "pinctrl_gpio_set_config" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pinctrl_gpio_set_config \- Apply config to given GPIO pin
.SH SYNOPSIS
.B "int" pinctrl_gpio_set_config
.BI "(unsigned gpio "  ","
.BI "unsigned long config "  ");"
.SH ARGUMENTS
.IP "gpio" 12
the GPIO pin number from the GPIO subsystem number space
.IP "config" 12
the configuration to apply to the GPIO
.SH "DESCRIPTION"
This function should *ONLY* be used from gpiolib-based GPIO drivers, if
they need to call the underlying pin controller to change GPIO config
(for example set debounce time).
