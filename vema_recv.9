.TH "vema_recv" 9 "vema_recv" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vema_recv \- - Recv handler for VEMA MAD agent
.SH SYNOPSIS
.B "void" vema_recv
.BI "(struct ib_mad_agent *mad_agent "  ","
.BI "struct ib_mad_send_buf *send_buf "  ","
.BI "struct ib_mad_recv_wc *mad_wc "  ");"
.SH ARGUMENTS
.IP "mad_agent" 12
pointer to the mad agent
.IP "send_buf" 12
Send buffer if found, else NULL
.IP "mad_wc" 12
pointer to mad send work completion information
.SH "DESCRIPTION"
Handle only set and get methods and respond to other methods
as unsupported. Allocate response buffer and address handle
for the response MAD.
