.TH "acpi_is_root_bridge" 9 "acpi_is_root_bridge" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_is_root_bridge \- determine whether an ACPI CA node is a PCI root bridge
.SH SYNOPSIS
.B "int" acpi_is_root_bridge
.BI "(acpi_handle handle "  ");"
.SH ARGUMENTS
.IP "handle" 12
the ACPI CA node in question.
.SH "NOTE"
we could make this API take a struct acpi_device * instead, but
for now, it's more convenient to operate on an acpi_handle.
