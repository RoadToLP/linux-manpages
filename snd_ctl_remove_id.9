.TH "snd_ctl_remove_id" 9 "snd_ctl_remove_id" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_ctl_remove_id \- remove the control of the given id and release it
.SH SYNOPSIS
.B "int" snd_ctl_remove_id
.BI "(struct snd_card *card "  ","
.BI "struct snd_ctl_elem_id *id "  ");"
.SH ARGUMENTS
.IP "card" 12
the card instance
.IP "id" 12
the control id to remove
.SH "DESCRIPTION"
Finds the control instance with the given id, removes it from the
card list and releases it.
.SH "RETURN"
0 if successful, or a negative error code on failure.
