.TH "synth_event_add_field" 9 "synth_event_add_field" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
synth_event_add_field \- Add a new field to a synthetic event cmd
.SH SYNOPSIS
.B "int" synth_event_add_field
.BI "(struct dynevent_cmd *cmd "  ","
.BI "const char *type "  ","
.BI "const char *name "  ");"
.SH ARGUMENTS
.IP "cmd" 12
A pointer to the dynevent_cmd struct representing the new event
.IP "type" 12
The type of the new field to add
.IP "name" 12
The name of the new field to add
.SH "DESCRIPTION"
Add a new field to a synthetic event cmd object.  Field ordering is in
the same order the fields are added.

See \fBsynth_field_size\fP for available types. If field_name contains
[n] the field is considered to be an array.
.SH "RETURN"
0 if successful, error otherwise.
