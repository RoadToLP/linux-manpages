.TH "snd_interval_list" 9 "snd_interval_list" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_interval_list \- refine the interval value from the list
.SH SYNOPSIS
.B "int" snd_interval_list
.BI "(struct snd_interval *i "  ","
.BI "unsigned int count "  ","
.BI "const unsigned int *list "  ","
.BI "unsigned int mask "  ");"
.SH ARGUMENTS
.IP "i" 12
the interval value to refine
.IP "count" 12
the number of elements in the list
.IP "list" 12
the value list
.IP "mask" 12
the bit-mask to evaluate
.SH "DESCRIPTION"
Refines the interval value from the list.
When mask is non-zero, only the elements corresponding to bit 1 are
evaluated.
.SH "RETURN"
Positive if the value is changed, zero if it's not changed, or a
negative error code.
