.TH "gpiod_set_config" 9 "gpiod_set_config" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
gpiod_set_config \- sets @config for a GPIO
.SH SYNOPSIS
.B "int" gpiod_set_config
.BI "(struct gpio_desc *desc "  ","
.BI "unsigned long config "  ");"
.SH ARGUMENTS
.IP "desc" 12
descriptor of the GPIO for which to set the configuration
.IP "config" 12
Same packed config format as generic pinconf
.SH "RETURN"
0 on success, -ENOTSUPP if the controller doesn't support setting the
configuration.
