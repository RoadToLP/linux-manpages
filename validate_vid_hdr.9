.TH "validate_vid_hdr" 9 "validate_vid_hdr" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
validate_vid_hdr \- validate a volume identifier header.
.SH SYNOPSIS
.B "int" validate_vid_hdr
.BI "(const struct ubi_device *ubi "  ","
.BI "const struct ubi_vid_hdr *vid_hdr "  ");"
.SH ARGUMENTS
.IP "ubi" 12
UBI device description object
.IP "vid_hdr" 12
the volume identifier header to check
.SH "DESCRIPTION"
This function checks that data stored in the volume identifier header
\fIvid_hdr\fP. Returns zero if the VID header is OK and 1 if not.
