.TH "ata_id_c_string" 9 "ata_id_c_string" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ata_id_c_string \- Convert IDENTIFY DEVICE page into C string
.SH SYNOPSIS
.B "void" ata_id_c_string
.BI "(const u16 *id "  ","
.BI "unsigned char *s "  ","
.BI "unsigned int ofs "  ","
.BI "unsigned int len "  ");"
.SH ARGUMENTS
.IP "id" 12
IDENTIFY DEVICE results we will examine
.IP "s" 12
string into which data is output
.IP "ofs" 12
offset into identify device page
.IP "len" 12
length of string to return. must be an odd number.

This function is identical to ata_id_string except that it
trims trailing spaces and terminates the resulting string with
null.  \fIlen\fP must be actual maximum length (even number) + 1.

LOCKING:
caller.
