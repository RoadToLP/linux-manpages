.TH "jr_func" 9 "jr_func" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
jr_func \- Emulate a JR instruction.
.SH SYNOPSIS
.B "int" jr_func
.BI "(struct pt_regs *regs "  ","
.BI "u32 ir "  ");"
.SH ARGUMENTS
.IP "regs" 12
-- undescribed --
.IP "ir" 12
Instruction
.SH "DESCRIPTION"
Returns SIGILL if JR was in delay slot, SIGEMT if we
can't compute the EPC, SIGSEGV if we can't access the
userland instruction or 0 on success.
