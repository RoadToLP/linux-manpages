.TH "find_var_ref" 9 "find_var_ref" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
find_var_ref \- Check if a trigger has a reference to a trigger variable
.SH SYNOPSIS
.B "struct hist_field *" find_var_ref
.BI "(struct hist_trigger_data *hist_data "  ","
.BI "struct hist_trigger_data *var_data "  ","
.BI "unsigned int var_idx "  ");"
.SH ARGUMENTS
.IP "hist_data" 12
The hist trigger that might have a reference to the variable
.IP "var_data" 12
The hist trigger that owns the variable
.IP "var_idx" 12
The trigger variable identifier
.SH "DESCRIPTION"
Check the list of var_refs[] on the first hist trigger to see
whether any of them are references to the variable on the second
trigger.
.SH "RETURN"
The VAR_REF field referencing the variable if so, NULL if not
