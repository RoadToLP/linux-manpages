.TH "ovs_vport_free" 9 "ovs_vport_free" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ovs_vport_free \- uninitialize and free vport
.SH SYNOPSIS
.B "void" ovs_vport_free
.BI "(struct vport *vport "  ");"
.SH ARGUMENTS
.IP "vport" 12
vport to free
.SH "DESCRIPTION"
Frees a vport allocated with \fBvport_alloc\fP when it is no longer needed.

The caller must ensure that an RCU grace period has passed since the last
time \fIvport\fP was in a datapath.
