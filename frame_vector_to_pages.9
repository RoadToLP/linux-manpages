.TH "frame_vector_to_pages" 9 "frame_vector_to_pages" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
frame_vector_to_pages \- convert frame vector to contain page pointers
.SH SYNOPSIS
.B "int" frame_vector_to_pages
.BI "(struct frame_vector *vec "  ");"
.SH ARGUMENTS
.IP "vec" 12
frame vector to convert
.SH "DESCRIPTION"
Convert \fIvec\fP to contain array of page pointers.  If the conversion is
successful, return 0. Otherwise return an error. Note that we do not grab
page references for the page structures.
