.TH "radeon_atif_get_notification_params" 9 "radeon_atif_get_notification_params" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
radeon_atif_get_notification_params \- determine notify configuration
.SH SYNOPSIS
.B "int" radeon_atif_get_notification_params
.BI "(acpi_handle handle "  ","
.BI "struct radeon_atif_notification_cfg *n "  ");"
.SH ARGUMENTS
.IP "handle" 12
acpi handle
.IP "n" 12
atif notification configuration struct
.SH "DESCRIPTION"
Execute the ATIF_FUNCTION_GET_SYSTEM_PARAMETERS ATIF function
to determine if a notifier is used and if so which one
(all asics).  This is either Notify(VGA, 0x81) or Notify(VGA, n)
where n is specified in the result if a notifier is used.
Returns 0 on success, error on failure.
