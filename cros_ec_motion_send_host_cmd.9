.TH "cros_ec_motion_send_host_cmd" 9 "cros_ec_motion_send_host_cmd" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
cros_ec_motion_send_host_cmd \- send motion sense host command
.SH SYNOPSIS
.B "int" cros_ec_motion_send_host_cmd
.BI "(struct cros_ec_sensors_core_state *state "  ","
.BI "u16 opt_length "  ");"
.SH ARGUMENTS
.IP "state" 12
pointer to state information for device
.IP "opt_length" 12
optional length to reduce the response size, useful on the data
path. Otherwise, the maximal allowed response size is used
.SH "DESCRIPTION"
When called, the sub-command is assumed to be set in param->cmd.
.SH "RETURN"
0 on success, -errno on failure.
