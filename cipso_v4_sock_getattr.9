.TH "cipso_v4_sock_getattr" 9 "cipso_v4_sock_getattr" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
cipso_v4_sock_getattr \- Get the security attributes from a sock
.SH SYNOPSIS
.B "int" cipso_v4_sock_getattr
.BI "(struct sock *sk "  ","
.BI "struct netlbl_lsm_secattr *secattr "  ");"
.SH ARGUMENTS
.IP "sk" 12
the sock
.IP "secattr" 12
the security attributes
.SH "DESCRIPTION"
Query \fIsk\fP to see if there is a CIPSO option attached to the sock and if
there is return the CIPSO security attributes in \fIsecattr\fP.  This function
requires that \fIsk\fP be locked, or privately held, but it does not do any
locking itself.  Returns zero on success and negative values on failure.
