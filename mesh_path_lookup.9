.TH "mesh_path_lookup" 9 "mesh_path_lookup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
mesh_path_lookup \- look up a path in the mesh path table
.SH SYNOPSIS
.B "struct mesh_path *" mesh_path_lookup
.BI "(struct ieee80211_sub_if_data *sdata "  ","
.BI "const u8 *dst "  ");"
.SH ARGUMENTS
.IP "sdata" 12
local subif
.IP "dst" 12
hardware address (ETH_ALEN length) of destination
.SH "RETURN"
pointer to the mesh path structure, or NULL if not found
.SH "DESCRIPTION"
Locking: must be called within a read rcu section.
