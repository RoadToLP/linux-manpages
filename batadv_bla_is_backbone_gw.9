.TH "batadv_bla_is_backbone_gw" 9 "batadv_bla_is_backbone_gw" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_bla_is_backbone_gw \- check if originator is a backbone gw for a VLAN
.SH SYNOPSIS
.B "bool" batadv_bla_is_backbone_gw
.BI "(struct sk_buff *skb "  ","
.BI "struct batadv_orig_node *orig_node "  ","
.BI "int hdr_size "  ");"
.SH ARGUMENTS
.IP "skb" 12
the frame to be checked
.IP "orig_node" 12
the orig_node of the frame
.IP "hdr_size" 12
maximum length of the frame
.SH "RETURN"
true if the orig_node is also a gateway on the soft interface,
otherwise it returns false.
