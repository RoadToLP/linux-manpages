.TH "intel_guc_fw_upload" 9 "intel_guc_fw_upload" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
intel_guc_fw_upload \- load GuC uCode to device
.SH SYNOPSIS
.B "int" intel_guc_fw_upload
.BI "(struct intel_guc *guc "  ");"
.SH ARGUMENTS
.IP "guc" 12
intel_guc structure
.SH "DESCRIPTION"
Called from \fBintel_uc_init_hw\fP during driver load, resume from sleep and
after a GPU reset.

The firmware image should have already been fetched into memory, so only
check that fetch succeeded, and then transfer the image to the h/w.
.SH "RETURN"
non-zero code on error
