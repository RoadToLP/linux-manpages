.TH "scsi_internal_device_unblock" 9 "scsi_internal_device_unblock" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
scsi_internal_device_unblock \- resume a device after a block request
.SH SYNOPSIS
.B "int" scsi_internal_device_unblock
.BI "(struct scsi_device *sdev "  ","
.BI "enum scsi_device_state new_state "  ");"
.SH ARGUMENTS
.IP "sdev" 12
device to resume
.IP "new_state" 12
state to set the device to after unblocking
.SH "DESCRIPTION"
Restart the device queue for a previously suspended SCSI device. May sleep.

Returns zero if successful or a negative error code upon failure.
.SH "NOTES"
This routine transitions the device to the SDEV_RUNNING state or to one of
the offline states (which must be a legal transition) allowing the midlayer
to goose the queue for this device.
