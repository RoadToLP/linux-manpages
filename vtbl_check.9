.TH "vtbl_check" 9 "vtbl_check" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vtbl_check \- check if volume table is not corrupted and sensible.
.SH SYNOPSIS
.B "int" vtbl_check
.BI "(const struct ubi_device *ubi "  ","
.BI "const struct ubi_vtbl_record *vtbl "  ");"
.SH ARGUMENTS
.IP "ubi" 12
UBI device description object
.IP "vtbl" 12
volume table
.SH "DESCRIPTION"
This function returns zero if \fIvtbl\fP is all right, 1 if CRC is incorrect,
and -EINVAL if it contains inconsistent data.
