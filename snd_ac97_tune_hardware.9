.TH "snd_ac97_tune_hardware" 9 "snd_ac97_tune_hardware" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_ac97_tune_hardware \- tune up the hardware
.SH SYNOPSIS
.B "int" snd_ac97_tune_hardware
.BI "(struct snd_ac97 *ac97 "  ","
.BI "const struct ac97_quirk *quirk "  ","
.BI "const char *override "  ");"
.SH ARGUMENTS
.IP "ac97" 12
the ac97 instance
.IP "quirk" 12
quirk list
.IP "override" 12
explicit quirk value (overrides the list if non-NULL)
.SH "DESCRIPTION"
Do some workaround for each pci device, such as renaming of the
headphone (true line-out) control as "Master".
The quirk-list must be terminated with a zero-filled entry.
.SH "RETURN"
Zero if successful, or a negative error code on failure.
