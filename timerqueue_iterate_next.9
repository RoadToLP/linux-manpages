.TH "timerqueue_iterate_next" 9 "timerqueue_iterate_next" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
timerqueue_iterate_next \- Returns the timer after the provided timer
.SH SYNOPSIS
.B "struct timerqueue_node *" timerqueue_iterate_next
.BI "(struct timerqueue_node *node "  ");"
.SH ARGUMENTS
.IP "node" 12
Pointer to a timer.
.SH "DESCRIPTION"
Provides the timer that is after the given node. This is used, when
necessary, to iterate through the list of timers in a timer list
without modifying the list.
