.TH "lpfc_fc_hdr_get_vfi" 9 "lpfc_fc_hdr_get_vfi" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_fc_hdr_get_vfi \- Get the VFI from an FC frame
.SH SYNOPSIS
.B "uint32_t" lpfc_fc_hdr_get_vfi
.BI "(struct fc_frame_header *fc_hdr "  ");"
.SH ARGUMENTS
.IP "fc_hdr" 12
A pointer to the FC Header data (In Big Endian Format)
.SH "DESCRIPTION"
This function processes the FC header to retrieve the VFI from the VF
header, if one exists. This function will return the VFI if one exists
or 0 if no VSAN Header exists.
