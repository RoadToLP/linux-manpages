.TH "splice_to_pipe" 9 "splice_to_pipe" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
splice_to_pipe \- fill passed data into a pipe
.SH SYNOPSIS
.B "ssize_t" splice_to_pipe
.BI "(struct pipe_inode_info *pipe "  ","
.BI "struct splice_pipe_desc *spd "  ");"
.SH ARGUMENTS
.IP "pipe" 12
pipe to fill
.IP "spd" 12
data to fill
.SH "DESCRIPTION"
\fIspd\fP contains a map of pages and len/offset tuples, along with
the struct pipe_buf_operations associated with these pages. This
function will link that data to the pipe.
