.TH "binder_enqueue_thread_work_ilocked" 9 "binder_enqueue_thread_work_ilocked" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
binder_enqueue_thread_work_ilocked \- Add an item to the thread work list
.SH SYNOPSIS
.B "void" binder_enqueue_thread_work_ilocked
.BI "(struct binder_thread *thread "  ","
.BI "struct binder_work *work "  ");"
.SH ARGUMENTS
.IP "thread" 12
thread to queue work to
.IP "work" 12
struct binder_work to add to list
.SH "DESCRIPTION"
Adds the work to the todo list of the thread, and enables processing
of the todo queue.

Requires the proc->inner_lock to be held.
