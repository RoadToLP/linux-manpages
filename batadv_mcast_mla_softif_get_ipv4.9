.TH "batadv_mcast_mla_softif_get_ipv4" 9 "batadv_mcast_mla_softif_get_ipv4" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_mcast_mla_softif_get_ipv4 \- get softif IPv4 multicast listeners
.SH SYNOPSIS
.B "int" batadv_mcast_mla_softif_get_ipv4
.BI "(struct net_device *dev "  ","
.BI "struct hlist_head *mcast_list "  ","
.BI "struct batadv_mcast_mla_flags *flags "  ");"
.SH ARGUMENTS
.IP "dev" 12
the device to collect multicast addresses from
.IP "mcast_list" 12
a list to put found addresses into
.IP "flags" 12
flags indicating the new multicast state
.SH "DESCRIPTION"
Collects multicast addresses of IPv4 multicast listeners residing
on this kernel on the given soft interface, dev, in
the given mcast_list. In general, multicast listeners provided by
your multicast receiving applications run directly on this node.
.SH "RETURN"
-ENOMEM on memory allocation error or the number of
items added to the mcast_list otherwise.
