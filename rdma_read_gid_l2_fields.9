.TH "rdma_read_gid_l2_fields" 9 "rdma_read_gid_l2_fields" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
rdma_read_gid_l2_fields \- Read the vlan ID and source MAC address of a GID entry.
.SH SYNOPSIS
.B "int" rdma_read_gid_l2_fields
.BI "(const struct ib_gid_attr *attr "  ","
.BI "u16 *vlan_id "  ","
.BI "u8 *smac "  ");"
.SH ARGUMENTS
.IP "attr" 12
GID attribute pointer whose L2 fields to be read
.IP "vlan_id" 12
Pointer to vlan id to fill up if the GID entry has
vlan id. It is optional.
.IP "smac" 12
Pointer to smac to fill up for a GID entry. It is optional.
.SH "DESCRIPTION"
\fBrdma_read_gid_l2_fields\fP returns 0 on success and returns vlan id
(if gid entry has vlan) and source MAC, or returns error.
