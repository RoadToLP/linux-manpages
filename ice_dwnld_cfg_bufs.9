.TH "ice_dwnld_cfg_bufs" 9 "ice_dwnld_cfg_bufs" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_dwnld_cfg_bufs \- 
.SH SYNOPSIS
.B "enum ice_status" ice_dwnld_cfg_bufs
.BI "(struct ice_hw *hw "  ","
.BI "struct ice_buf *bufs "  ","
.BI "u32 count "  ");"
.SH ARGUMENTS
.IP "hw" 12
pointer to the hardware structure
.IP "bufs" 12
pointer to an array of buffers
.IP "count" 12
the number of buffers in the array
.SH "DESCRIPTION"
Obtains global config lock and downloads the package configuration buffers
to the firmware. Metadata buffers are skipped, and the first metadata buffer
found indicates that the rest of the buffers are all metadata buffers.
