.TH "comedi_to_usb_dev" 9 "comedi_to_usb_dev" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
comedi_to_usb_dev \- Return USB device attached to COMEDI device
.SH SYNOPSIS
.B "struct usb_device *" comedi_to_usb_dev
.BI "(struct comedi_device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
COMEDI device.
.SH "DESCRIPTION"
Assuming \fIdev->hw_dev\fP is non-NULL, it is assumed to be pointing to a
a \fIstruct device\fP embedded in a \fIstruct usb_interface\fP.
.SH "RETURN"
USB device to which the USB interface belongs if \fIdev->hw_dev\fP is
non-NULL.  Return NULL if \fIdev->hw_dev\fP is NULL.
