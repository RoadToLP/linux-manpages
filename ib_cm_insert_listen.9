.TH "ib_cm_insert_listen" 9 "ib_cm_insert_listen" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ib_cm_insert_listen \- Create a new listening ib_cm_id and listen on the given service ID.
.SH SYNOPSIS
.B "struct ib_cm_id *" ib_cm_insert_listen
.BI "(struct ib_device *device "  ","
.BI "ib_cm_handler cm_handler "  ","
.BI "__be64 service_id "  ");"
.SH ARGUMENTS
.IP "device" 12
Device associated with the cm_id.  All related communication will
be associated with the specified device.
.IP "cm_handler" 12
Callback invoked to notify the user of CM events.
.IP "service_id" 12
Service identifier matched against incoming connection
and service ID resolution requests.  The service ID should be specified
network-byte order.  If set to IB_CM_ASSIGN_SERVICE_ID, the CM will
assign a service ID to the caller.
.SH "DESCRIPTION"

If there's an existing ID listening on that same device and service ID,
return it.

Callers should call ib_destroy_cm_id when done with the listener ID.
