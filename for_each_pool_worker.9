.TH "for_each_pool_worker" 9 "for_each_pool_worker" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
for_each_pool_worker \- iterate through all workers of a worker_pool
.SH SYNOPSIS
.B "for_each_pool_worker
.BI "(worker "  ","
.BI "pool "  ");"
.SH ARGUMENTS
.IP "worker" 12
iteration cursor
.IP "pool" 12
worker_pool to iterate workers of
.SH "DESCRIPTION"
This must be called with wq_pool_attach_mutex.

The if/else clause exists only for the lockdep assertion and can be
ignored.
