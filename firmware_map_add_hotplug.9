.TH "firmware_map_add_hotplug" 9 "firmware_map_add_hotplug" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
firmware_map_add_hotplug \- Adds a firmware mapping entry when we do memory hotplug.
.SH SYNOPSIS
.B "int" firmware_map_add_hotplug
.BI "(u64 start "  ","
.BI "u64 end "  ","
.BI "const char *type "  ");"
.SH ARGUMENTS
.IP "start" 12
Start of the memory range.
.IP "end" 12
End of the memory range (exclusive)
.IP "type" 12
Type of the memory range.
.SH "DESCRIPTION"
Adds a firmware mapping entry. This function is for memory hotplug, it is
similar to function \fBfirmware_map_add_early\fP. The only difference is that
it will create the syfs entry dynamically.
.SH "RETURN"
0 on success, or -ENOMEM if no memory could be allocated.
