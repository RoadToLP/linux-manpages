.TH "devcd_free_sgtable" 9 "devcd_free_sgtable" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
devcd_free_sgtable \- free all the memory of the given scatterlist table (i.e. both pages and scatterlist instances)
.SH SYNOPSIS
.B "void" devcd_free_sgtable
.BI "(void *data "  ");"
.SH ARGUMENTS
.IP "data" 12
-- undescribed --
.SH "NOTE"
if two tables allocated with devcd_alloc_sgtable and then chained
using the sg_chain function then that function should be called only once
on the chained table
