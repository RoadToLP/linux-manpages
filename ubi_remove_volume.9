.TH "ubi_remove_volume" 9 "ubi_remove_volume" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ubi_remove_volume \- remove volume.
.SH SYNOPSIS
.B "int" ubi_remove_volume
.BI "(struct ubi_volume_desc *desc "  ","
.BI "int no_vtbl "  ");"
.SH ARGUMENTS
.IP "desc" 12
volume descriptor
.IP "no_vtbl" 12
do not change volume table if not zero
.SH "DESCRIPTION"
This function removes volume described by \fIdesc\fP. The volume has to be opened
in "exclusive" mode. Returns zero in case of success and a negative error
code in case of failure. The caller has to have the \fIubi->device_mutex\fP
locked.
