.TH "ata_phys_link_offline" 9 "ata_phys_link_offline" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ata_phys_link_offline \- test whether the given link is offline
.SH SYNOPSIS
.B "bool" ata_phys_link_offline
.BI "(struct ata_link *link "  ");"
.SH ARGUMENTS
.IP "link" 12
ATA link to test

Test whether \fIlink\fP is offline.  Note that this function
returns 0 if offline status of \fIlink\fP cannot be obtained, so
ata_link_online(link) != !ata_link_offline(link).

LOCKING:
None.
.SH "RETURN"
True if the port offline status is available and offline.
