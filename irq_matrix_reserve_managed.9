.TH "irq_matrix_reserve_managed" 9 "irq_matrix_reserve_managed" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
irq_matrix_reserve_managed \- Reserve a managed interrupt in a CPU map
.SH SYNOPSIS
.B "int" irq_matrix_reserve_managed
.BI "(struct irq_matrix *m "  ","
.BI "const struct cpumask *msk "  ");"
.SH ARGUMENTS
.IP "m" 12
Matrix pointer
.IP "msk" 12
On which CPUs the bits should be reserved.
.SH "DESCRIPTION"
Can be called for offline CPUs. Note, this will only reserve one bit
on all CPUs in \fImsk\fP, but it's not guaranteed that the bits are at the
same offset on all CPUs
