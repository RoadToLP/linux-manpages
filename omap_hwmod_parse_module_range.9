.TH "omap_hwmod_parse_module_range" 9 "omap_hwmod_parse_module_range" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_hwmod_parse_module_range \- map module IO range from device tree
.SH SYNOPSIS
.B "int" omap_hwmod_parse_module_range
.BI "(struct omap_hwmod *oh "  ","
.BI "struct device_node *np "  ","
.BI "struct resource *res "  ");"
.SH ARGUMENTS
.IP "oh" 12
struct omap_hwmod *
.IP "np" 12
struct device_node *
.IP "res" 12
-- undescribed --
.SH "DESCRIPTION"
Parse the device tree range an interconnect target module provides
for it's child device IP blocks. This way we can support the old
"ti,hwmods" property with just dts data without a need for platform
data for IO resources. And we don't need all the child IP device
nodes available in the dts.
