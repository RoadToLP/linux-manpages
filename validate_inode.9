.TH "validate_inode" 9 "validate_inode" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
validate_inode \- validate inode.
.SH SYNOPSIS
.B "int" validate_inode
.BI "(struct ubifs_info *c "  ","
.BI "const struct inode *inode "  ");"
.SH ARGUMENTS
.IP "c" 12
UBIFS file-system description object
.IP "inode" 12
the inode to validate
.SH "DESCRIPTION"
This is a helper function for '\fBubifs_iget\fP' which validates various fields
of a newly built inode to make sure they contain sane values and prevent
possible vulnerabilities. Returns zero if the inode is all right and
a non-zero error code if not.
