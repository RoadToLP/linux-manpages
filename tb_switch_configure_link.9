.TH "tb_switch_configure_link" 9 "tb_switch_configure_link" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tb_switch_configure_link \- Set link configured
.SH SYNOPSIS
.B "int" tb_switch_configure_link
.BI "(struct tb_switch *sw "  ");"
.SH ARGUMENTS
.IP "sw" 12
Switch whose link is configured
.SH "DESCRIPTION"
Sets the link upstream from \fIsw\fP configured (from both ends) so that
it will not be disconnected when the domain exits sleep. Can be
called for any switch.

It is recommended that this is called after lane bonding is enabled.

Returns 0 on success and negative errno in case of error.
