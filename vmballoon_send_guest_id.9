.TH "vmballoon_send_guest_id" 9 "vmballoon_send_guest_id" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vmballoon_send_guest_id \- communicate guest type to the host.
.SH SYNOPSIS
.B "int" vmballoon_send_guest_id
.BI "(struct vmballoon *b "  ");"
.SH ARGUMENTS
.IP "b" 12
pointer to the balloon.
.SH "DESCRIPTION"
Communicate guest type to the host so that it can adjust ballooning
algorithm to the one most appropriate for the guest. This command
is normally issued after sending "start" command and is part of
standard reset sequence.
.SH "RETURN"
zero on success or appropriate error code.
