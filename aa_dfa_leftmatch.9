.TH "aa_dfa_leftmatch" 9 "aa_dfa_leftmatch" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
aa_dfa_leftmatch \- traverse @dfa to find state @str stops at
.SH SYNOPSIS
.B "unsigned int" aa_dfa_leftmatch
.BI "(struct aa_dfa *dfa "  ","
.BI "unsigned int start "  ","
.BI "const char *str "  ","
.BI "unsigned int *count "  ");"
.SH ARGUMENTS
.IP "dfa" 12
the dfa to match \fIstr\fP against  (NOT NULL)
.IP "start" 12
the state of the dfa to start matching in
.IP "str" 12
the null terminated string of bytes to match against the dfa (NOT NULL)
.IP "count" 12
current count of longest left.
.SH "DESCRIPTION"
aa_dfa_match will match \fIstr\fP against the dfa and return the state it
finished matching in. The final state can be used to look up the accepting
label, or as the start state of a continuing match.
.SH "RETURN"
final state reached after input is consumed
