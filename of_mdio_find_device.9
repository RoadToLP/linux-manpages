.TH "of_mdio_find_device" 9 "of_mdio_find_device" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
of_mdio_find_device \- Given a device tree node, find the mdio_device
.SH SYNOPSIS
.B "struct mdio_device *" of_mdio_find_device
.BI "(struct device_node *np "  ");"
.SH ARGUMENTS
.IP "np" 12
pointer to the mdio_device's device tree node
.SH "DESCRIPTION"
If successful, returns a pointer to the mdio_device with the embedded
struct device refcount incremented by one, or NULL on failure.
The caller should call \fBput_device\fP on the mdio_device after its use
