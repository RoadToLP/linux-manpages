.TH "legacy_timer_tick" 9 "legacy_timer_tick" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
legacy_timer_tick \- advances the timekeeping infrastructure
.SH SYNOPSIS
.B "void" legacy_timer_tick
.BI "(unsigned long ticks "  ");"
.SH ARGUMENTS
.IP "ticks" 12
number of ticks, that have elapsed since the last call.
.SH "DESCRIPTION"
This is used by platforms that have not been converted to
generic clockevents.

If 'ticks' is zero, the CPU is not handling timekeeping, so
only perform process accounting and profiling.

Must be called with interrupts disabled.
