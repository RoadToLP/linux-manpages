.TH "tep_filter_compare" 9 "tep_filter_compare" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tep_filter_compare \- compare two filters and return if they are the same
.SH SYNOPSIS
.B "int" tep_filter_compare
.BI "(struct tep_event_filter *filter1 "  ","
.BI "struct tep_event_filter *filter2 "  ");"
.SH ARGUMENTS
.IP "filter1" 12
Filter to compare with \fIfilter2\fP
.IP "filter2" 12
Filter to compare with \fIfilter1\fP
.SH "RETURN"
1 if the two filters hold the same content.
0 if they do not.
