.TH "qmi_encode_basic_elem" 9 "qmi_encode_basic_elem" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
qmi_encode_basic_elem \- Encodes elements of basic/primary data type
.SH SYNOPSIS
.B "int" qmi_encode_basic_elem
.BI "(void *buf_dst "  ","
.BI "const void *buf_src "  ","
.BI "u32 elem_len "  ","
.BI "u32 elem_size "  ");"
.SH ARGUMENTS
.IP "buf_dst" 12
Buffer to store the encoded information.
.IP "buf_src" 12
Buffer containing the elements to be encoded.
.IP "elem_len" 12
Number of elements, in the buf_src, to be encoded.
.IP "elem_size" 12
Size of a single instance of the element to be encoded.
.SH "DESCRIPTION"
This function encodes the "elem_len" number of data elements, each of
size "elem_size" bytes from the source buffer "buf_src" and stores the
encoded information in the destination buffer "buf_dst". The elements are
of primary data type which include u8 - u64 or similar. This
function returns the number of bytes of encoded information.
.SH "RETURN"
The number of bytes of encoded information.
