.TH "drm_print_regset32" 9 "drm_print_regset32" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
drm_print_regset32 \- print the contents of registers to a &drm_printer stream.
.SH SYNOPSIS
.B "void" drm_print_regset32
.BI "(struct drm_printer *p "  ","
.BI "struct debugfs_regset32 *regset "  ");"
.SH ARGUMENTS
.IP "p" 12
the \fIdrm\fP printer
.IP "regset" 12
the list of registers to print.
.SH "DESCRIPTION"
Often in driver debug, it's useful to be able to either capture the
contents of registers in the steady state using debugfs or at
specific points during operation.  This lets the driver have a
single list of registers for both.
