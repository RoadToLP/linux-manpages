.TH "unregister_netdevice_queue" 9 "unregister_netdevice_queue" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
unregister_netdevice_queue \- remove device from the kernel
.SH SYNOPSIS
.B "void" unregister_netdevice_queue
.BI "(struct net_device *dev "  ","
.BI "struct list_head *head "  ");"
.SH ARGUMENTS
.IP "dev" 12
device
.IP "head" 12
list

This function shuts down a device interface and removes it
from the kernel tables.
If head not NULL, device is queued to be unregistered later.

Callers must hold the rtnl semaphore.  You may want
\fBunregister_netdev\fP instead of this.
