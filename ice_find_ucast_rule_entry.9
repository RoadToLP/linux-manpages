.TH "ice_find_ucast_rule_entry" 9 "ice_find_ucast_rule_entry" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_find_ucast_rule_entry \- Search for a unicast MAC filter rule entry
.SH SYNOPSIS
.B "struct ice_fltr_mgmt_list_entry *" ice_find_ucast_rule_entry
.BI "(struct ice_hw *hw "  ","
.BI "u8 recp_id "  ","
.BI "struct ice_fltr_info *f_info "  ");"
.SH ARGUMENTS
.IP "hw" 12
pointer to the hardware structure
.IP "recp_id" 12
lookup type for which the specified rule needs to be searched
.IP "f_info" 12
rule information
.SH "DESCRIPTION"
Helper function to search for a unicast rule entry - this is to be used
to remove unicast MAC filter that is not shared with other VSIs on the
PF switch.

Returns pointer to entry storing the rule if found
