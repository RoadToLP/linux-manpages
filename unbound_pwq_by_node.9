.TH "unbound_pwq_by_node" 9 "unbound_pwq_by_node" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
unbound_pwq_by_node \- return the unbound pool_workqueue for the given node
.SH SYNOPSIS
.B "struct pool_workqueue *" unbound_pwq_by_node
.BI "(struct workqueue_struct *wq "  ","
.BI "int node "  ");"
.SH ARGUMENTS
.IP "wq" 12
the target workqueue
.IP "node" 12
the node ID
.SH "DESCRIPTION"
This must be called with any of wq_pool_mutex, wq->mutex or RCU
read locked.
If the pwq needs to be used beyond the locking in effect, the caller is
responsible for guaranteeing that the pwq stays online.
.SH "RETURN"
The unbound pool_workqueue for \fInode\fP.
