.TH "svc_rdma_iov_write" 9 "svc_rdma_iov_write" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
svc_rdma_iov_write \- Construct RDMA Writes from an iov
.SH SYNOPSIS
.B "int" svc_rdma_iov_write
.BI "(struct svc_rdma_write_info *info "  ","
.BI "const struct kvec *iov "  ");"
.SH ARGUMENTS
.IP "info" 12
pointer to write arguments
.IP "iov" 12
kvec to write
.SH "RETURN"
On succes, returns zero
-E2BIG if the client-provided Write chunk is too small
-ENOMEM if a resource has been exhausted
-EIO if an rdma-rw error occurred
