.TH "dwarf_frame_alloc_reg" 9 "dwarf_frame_alloc_reg" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dwarf_frame_alloc_reg \- allocate memory for a DWARF register
.SH SYNOPSIS
.B "struct dwarf_reg *" dwarf_frame_alloc_reg
.BI "(struct dwarf_frame *frame "  ","
.BI "unsigned int reg_num "  ");"
.SH ARGUMENTS
.IP "frame" 12
the DWARF frame whose list of registers we insert on
.IP "reg_num" 12
the register number

Allocate space for, and initialise, a dwarf reg from
dwarf_reg_pool and insert it onto the (unsorted) linked-list of
dwarf registers for \fIframe\fP.

Return the initialised DWARF reg.
