.TH "skb_eth_push" 9 "skb_eth_push" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
skb_eth_push \- Add a new Ethernet header at the head of a packet
.SH SYNOPSIS
.B "int" skb_eth_push
.BI "(struct sk_buff *skb "  ","
.BI "const unsigned char *dst "  ","
.BI "const unsigned char *src "  ");"
.SH ARGUMENTS
.IP "skb" 12
Socket buffer to modify
.IP "dst" 12
Destination MAC address of the new header
.IP "src" 12
Source MAC address of the new header
.SH "DESCRIPTION"
Prepend \fIskb\fP with a new Ethernet header.

Expects that skb->data points to the mac header, which must be empty.

Returns 0 on success, -errno otherwise.
