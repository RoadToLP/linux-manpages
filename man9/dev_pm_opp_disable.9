.TH "dev_pm_opp_disable" 9 "dev_pm_opp_disable" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dev_pm_opp_disable \- Disable a specific OPP
.SH SYNOPSIS
.B "int" dev_pm_opp_disable
.BI "(struct device *dev "  ","
.BI "unsigned long freq "  ");"
.SH ARGUMENTS
.IP "dev" 12
device for which we do this operation
.IP "freq" 12
OPP frequency to disable
.SH "DESCRIPTION"
Disables a provided opp. If the operation is valid, this returns
0, else the corresponding error value. It is meant to be a temporary
control by users to make this OPP not available until the circumstances are
right to make it available again (with a call to dev_pm_opp_enable).
.SH "RETURN"
-EINVAL for bad pointers, -ENOMEM if no memory available for the
copy operation, returns 0 if no modification was done OR modification was
successful.
