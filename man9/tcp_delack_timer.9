.TH "tcp_delack_timer" 9 "tcp_delack_timer" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tcp_delack_timer \- The TCP delayed ACK timeout handler
.SH SYNOPSIS
.B "void" tcp_delack_timer
.BI "(struct timer_list *t "  ");"
.SH ARGUMENTS
.IP "t" 12
Pointer to the timer. (gets casted to struct sock *)

This function gets (indirectly) called when the kernel timer for a TCP packet
of this socket expires. Calls \fBtcp_delack_timer_handler\fP to do the actual work.
.SH "RETURN"
Nothing (void)
