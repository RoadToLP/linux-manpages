.TH "radix_tree_tag_set" 9 "radix_tree_tag_set" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
radix_tree_tag_set \- set a tag on a radix tree node
.SH SYNOPSIS
.B "void *" radix_tree_tag_set
.BI "(struct radix_tree_root *root "  ","
.BI "unsigned long index "  ","
.BI "unsigned int tag "  ");"
.SH ARGUMENTS
.IP "root" 12
radix tree root
.IP "index" 12
index key
.IP "tag" 12
tag index

Set the search tag (which must be < RADIX_TREE_MAX_TAGS)
corresponding to \fIindex\fP in the radix tree.  From
the root all the way down to the leaf node.

Returns the address of the tagged item.  Setting a tag on a not-present
item is a bug.
