.TH "seccomp_filter_release" 9 "seccomp_filter_release" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
seccomp_filter_release \- Detach the task from its filter tree, drop its reference count, and notify about unused filters
.SH SYNOPSIS
.B "void" seccomp_filter_release
.BI "(struct task_struct *tsk "  ");"
.SH ARGUMENTS
.IP "tsk" 12
-- undescribed --
.SH "DESCRIPTION"

This function should only be called when the task is exiting as
it detaches it from its filter tree. As such, \fBREAD_ONCE\fP and
barriers are not needed here, as would normally be needed.
