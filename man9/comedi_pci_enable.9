.TH "comedi_pci_enable" 9 "comedi_pci_enable" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
comedi_pci_enable \- Enable the PCI device and request the regions
.SH SYNOPSIS
.B "int" comedi_pci_enable
.BI "(struct comedi_device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
COMEDI device.
.SH "DESCRIPTION"
Assuming \fIdev->hw_dev\fP is non-NULL, it is assumed to be pointing to a
a \fIstruct device\fP embedded in a \fIstruct pci_dev\fP.  Enable the PCI device
and request its regions.  Set \fIdev->ioenabled\fP to true if successful,
otherwise undo what was done.

Calls to \fBcomedi_pci_enable\fP and \fBcomedi_pci_disable\fP cannot be nested.
.SH "RETURN"
0 on success,
-ENODEV if \fIdev->hw_dev\fP is NULL,
-EBUSY if regions busy,
or some negative error number if failed to enable PCI device.
