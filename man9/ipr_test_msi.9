.TH "ipr_test_msi" 9 "ipr_test_msi" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ipr_test_msi \- Test for Message Signaled Interrupt (MSI) support.
.SH SYNOPSIS
.B "int" ipr_test_msi
.BI "(struct ipr_ioa_cfg *ioa_cfg "  ","
.BI "struct pci_dev *pdev "  ");"
.SH ARGUMENTS
.IP "ioa_cfg" 12
ioa config struct
.IP "pdev" 12
PCI device struct
.SH "DESCRIPTION"
This routine sets up and initiates a test interrupt to determine
if the interrupt is received via the \fBipr_test_intr\fP service routine.
If the tests fails, the driver will fall back to LSI.

Return value:
0 on success / non-zero on failure
