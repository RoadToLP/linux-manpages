.TH "may_allow_all" 9 "may_allow_all" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
may_allow_all \- checks if it's possible to change the behavior to allow based on parent's rules.
.SH SYNOPSIS
.B "int" may_allow_all
.BI "(struct dev_cgroup *parent "  ");"
.SH ARGUMENTS
.IP "parent" 12
device cgroup's parent
.SH "RETURN"
!= 0 in case it's allowed, 0 otherwise
