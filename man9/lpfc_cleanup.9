.TH "lpfc_cleanup" 9 "lpfc_cleanup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_cleanup \- Performs vport cleanups before deleting a vport
.SH SYNOPSIS
.B "void" lpfc_cleanup
.BI "(struct lpfc_vport *vport "  ");"
.SH ARGUMENTS
.IP "vport" 12
pointer to a virtual N_Port data structure.
.SH "DESCRIPTION"
This routine performs the necessary cleanups before deleting the \fIvport\fP.
It invokes the discovery state machine to perform necessary state
transitions and to release the ndlps associated with the \fIvport\fP. Note,
the physical port is treated as \fIvport\fP 0.
