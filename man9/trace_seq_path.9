.TH "trace_seq_path" 9 "trace_seq_path" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
trace_seq_path \- copy a path into the sequence buffer
.SH SYNOPSIS
.B "int" trace_seq_path
.BI "(struct trace_seq *s "  ","
.BI "const struct path *path "  ");"
.SH ARGUMENTS
.IP "s" 12
trace sequence descriptor
.IP "path" 12
path to write into the sequence buffer.
.SH "DESCRIPTION"
Write a path name into the sequence buffer.

Returns 1 if we successfully written all the contents to
the buffer.
Returns 0 if we the length to write is bigger than the
reserved buffer space. In this case, nothing gets written.
