.TH "ftrace_graph_get_ret_stack" 9 "ftrace_graph_get_ret_stack" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ftrace_graph_get_ret_stack \- return the entry of the shadow stack
.SH SYNOPSIS
.B "struct ftrace_ret_stack *" ftrace_graph_get_ret_stack
.BI "(struct task_struct *task "  ","
.BI "int idx "  ");"
.SH ARGUMENTS
.IP "task" 12
The task to read the shadow stack from
.IP "idx" 12
Index down the shadow stack
.SH "DESCRIPTION"
Return the ret_struct on the shadow stack of the \fItask\fP at the
call graph at \fIidx\fP starting with zero. If \fIidx\fP is zero, it
will return the last saved ret_stack entry. If it is greater than
zero, it will return the corresponding ret_stack for the depth
of saved return addresses.
