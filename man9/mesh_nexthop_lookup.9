.TH "mesh_nexthop_lookup" 9 "mesh_nexthop_lookup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
mesh_nexthop_lookup \- put the appropriate next hop on a mesh frame. Calling this function is considered "using" the associated mpath, so preempt a path refresh if this mpath expires soon.
.SH SYNOPSIS
.B "int" mesh_nexthop_lookup
.BI "(struct ieee80211_sub_if_data *sdata "  ","
.BI "struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "sdata" 12
network subif the frame will be sent through
.IP "skb" 12
802.11 frame to be sent
.SH "RETURN"
0 if the next hop was found. Nonzero otherwise.
