.TH "__prci_wrpll_unpack" 9 "__prci_wrpll_unpack" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
__prci_wrpll_unpack \- unpack WRPLL configuration registers into parameters
.SH SYNOPSIS
.B "void" __prci_wrpll_unpack
.BI "(struct wrpll_cfg *c "  ","
.BI "u32 r "  ");"
.SH ARGUMENTS
.IP "c" 12
ptr to a struct wrpll_cfg record to write config into
.IP "r" 12
value read from the PRCI PLL configuration register
.SH "DESCRIPTION"
Given a value \fIr\fP read from an FU740 PRCI PLL configuration register,
split it into fields and populate it into the WRPLL configuration record
pointed to by \fIc\fP.

The COREPLLCFG0 macros are used below, but the other *PLLCFG0 macros
have the same register layout.
.SH "CONTEXT"
Any context.
