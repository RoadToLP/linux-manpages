.TH "devm_of_phy_get_by_index" 9 "devm_of_phy_get_by_index" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
devm_of_phy_get_by_index \- lookup and obtain a reference to a phy by index.
.SH SYNOPSIS
.B "struct phy *" devm_of_phy_get_by_index
.BI "(struct device *dev "  ","
.BI "struct device_node *np "  ","
.BI "int index "  ");"
.SH ARGUMENTS
.IP "dev" 12
device that requests this phy
.IP "np" 12
node containing the phy
.IP "index" 12
index of the phy
.SH "DESCRIPTION"
Gets the phy using \fB_of_phy_get\fP, then gets a refcount to it,
and associates a device with it using devres. On driver detach,
release function is invoked on the devres data,
then, devres data is freed.
