.TH "class_dev_iter_init" 9 "class_dev_iter_init" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
class_dev_iter_init \- initialize class device iterator
.SH SYNOPSIS
.B "void" class_dev_iter_init
.BI "(struct class_dev_iter *iter "  ","
.BI "struct class *class "  ","
.BI "struct device *start "  ","
.BI "const struct device_type *type "  ");"
.SH ARGUMENTS
.IP "iter" 12
class iterator to initialize
.IP "class" 12
the class we wanna iterate over
.IP "start" 12
the device to start iterating from, if any
.IP "type" 12
device_type of the devices to iterate over, NULL for all
.SH "DESCRIPTION"
Initialize class iterator \fIiter\fP such that it iterates over devices
of \fIclass\fP.  If \fIstart\fP is set, the list iteration will start there,
otherwise if it is NULL, the iteration starts at the beginning of
the list.
