.TH "omap_rproc_disable_timers" 9 "omap_rproc_disable_timers" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_rproc_disable_timers \- disable the timers for a remoteproc
.SH SYNOPSIS
.B "int" omap_rproc_disable_timers
.BI "(struct rproc *rproc "  ","
.BI "bool configure "  ");"
.SH ARGUMENTS
.IP "rproc" 12
handle of a remote processor
.IP "configure" 12
boolean flag used to release the timer handle
.SH "DESCRIPTION"
This function is used primarily to disable the timers associated with
a remoteproc. The configure flag is provided to allow the driver to
to either stop and release a timer (during device shutdown) or to just
stop a timer (during a suspend operation).
.SH "RETURN"
0 on success or no timers
