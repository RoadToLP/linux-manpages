.TH "devm_ti_sci_get_handle" 9 "devm_ti_sci_get_handle" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
devm_ti_sci_get_handle \- Managed get handle
.SH SYNOPSIS
.B "const struct ti_sci_handle *" devm_ti_sci_get_handle
.BI "(struct device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
device for which we want SCI handle for.
.SH "NOTE"
This releases the handle once the device resources are
no longer needed. MUST NOT BE released with ti_sci_put_handle.
The function does not track individual clients of the framework
and is expected to be maintained by caller of TI SCI protocol library.
.SH "RETURN"
0 if all went fine, else corresponding error.
