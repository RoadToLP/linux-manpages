.TH "sdio_uart_install" 9 "sdio_uart_install" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
sdio_uart_install \- install method
.SH SYNOPSIS
.B "int" sdio_uart_install
.BI "(struct tty_driver *driver "  ","
.BI "struct tty_struct *tty "  ");"
.SH ARGUMENTS
.IP "driver" 12
the driver in use (sdio_uart in our case)
.IP "tty" 12
the tty being bound

Look up and bind the tty and the driver together. Initialize
any needed private data (in our case the termios)
