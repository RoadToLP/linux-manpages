.TH "xprt_request_transmit" 9 "xprt_request_transmit" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
xprt_request_transmit \- send an RPC request on a transport
.SH SYNOPSIS
.B "int" xprt_request_transmit
.BI "(struct rpc_rqst *req "  ","
.BI "struct rpc_task *snd_task "  ");"
.SH ARGUMENTS
.IP "req" 12
pointer to request to transmit
.IP "snd_task" 12
RPC task that owns the transport lock
.SH "DESCRIPTION"
This performs the transmission of a single request.
Note that if the request is not the same as snd_task, then it
does need to be pinned.
Returns '0' on success.
