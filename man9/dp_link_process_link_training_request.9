.TH "dp_link_process_link_training_request" 9 "dp_link_process_link_training_request" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dp_link_process_link_training_request \- processes new training requests
.SH SYNOPSIS
.B "int" dp_link_process_link_training_request
.BI "(struct dp_link_private *link "  ");"
.SH ARGUMENTS
.IP "link" 12
Display Port link data
.SH "DESCRIPTION"
This function will handle new link training requests that are initiated by
the sink. In particular, it will update the requested lane count and link
rate, and then trigger the link retraining procedure.

The function will return 0 if a link training request has been processed,
otherwise it will return -EINVAL.
