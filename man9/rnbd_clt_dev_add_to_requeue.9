.TH "rnbd_clt_dev_add_to_requeue" 9 "rnbd_clt_dev_add_to_requeue" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
rnbd_clt_dev_add_to_requeue \- add device to requeue if session is busy
.SH SYNOPSIS
.B "bool" rnbd_clt_dev_add_to_requeue
.BI "(struct rnbd_clt_dev *dev "  ","
.BI "struct rnbd_queue *q "  ");"
.SH ARGUMENTS
.IP "dev" 12
Device to be checked
.IP "q" 12
Queue to be added to the requeue list if required
.SH "DESCRIPTION"
If session is busy, that means someone will requeue us when resources
are freed.  If session is not doing anything - device is not added to
the list and \fIfalse\fP is returned.
