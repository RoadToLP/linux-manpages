.TH "ktime_get_ts64" 9 "ktime_get_ts64" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ktime_get_ts64 \- get the monotonic clock in timespec64 format
.SH SYNOPSIS
.B "void" ktime_get_ts64
.BI "(struct timespec64 *ts "  ");"
.SH ARGUMENTS
.IP "ts" 12
pointer to timespec variable
.SH "DESCRIPTION"
The function calculates the monotonic clock from the realtime
clock and the wall_to_monotonic offset and stores the result
in normalized timespec64 format in the variable pointed to by \fIts\fP.
