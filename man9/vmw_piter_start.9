.TH "vmw_piter_start" 9 "vmw_piter_start" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vmw_piter_start \- Initialize a struct vmw_piter.
.SH SYNOPSIS
.B "void" vmw_piter_start
.BI "(struct vmw_piter *viter "  ","
.BI "const struct vmw_sg_table *vsgt "  ","
.BI "unsigned long p_offset "  ");"
.SH ARGUMENTS
.IP "viter" 12
Pointer to the iterator to initialize
.IP "vsgt" 12
Pointer to a struct vmw_sg_table to initialize from
.IP "p_offset" 12
-- undescribed --
.SH "DESCRIPTION"
Note that we're following the convention of __sg_page_iter_start, so that
the iterator doesn't point to a valid page after initialization; it has
to be advanced one step first.
