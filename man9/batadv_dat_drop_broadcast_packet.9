.TH "batadv_dat_drop_broadcast_packet" 9 "batadv_dat_drop_broadcast_packet" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_dat_drop_broadcast_packet \- check if an ARP request has to be dropped (because the node has already obtained the reply via DAT) or not
.SH SYNOPSIS
.B "bool" batadv_dat_drop_broadcast_packet
.BI "(struct batadv_priv *bat_priv "  ","
.BI "struct batadv_forw_packet *forw_packet "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "forw_packet" 12
the broadcast packet
.SH "RETURN"
true if the node can drop the packet, false otherwise.
