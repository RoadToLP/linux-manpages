.TH "tb_lc_start_lane_initialization" 9 "tb_lc_start_lane_initialization" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tb_lc_start_lane_initialization \- Start lane initialization
.SH SYNOPSIS
.B "int" tb_lc_start_lane_initialization
.BI "(struct tb_port *port "  ");"
.SH ARGUMENTS
.IP "port" 12
Device router lane 0 adapter
.SH "DESCRIPTION"
Starts lane initialization for \fIport\fP after the router resumed from
sleep. Should be called for those downstream lane adapters that were
not connected (\fBtb_lc_configure_port\fP was not called) before sleep.

Returns 0 in success and negative errno in case of failure.
