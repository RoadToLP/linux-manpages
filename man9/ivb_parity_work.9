.TH "ivb_parity_work" 9 "ivb_parity_work" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ivb_parity_work \- Workqueue called when a parity error interrupt occurred.
.SH SYNOPSIS
.B "void" ivb_parity_work
.BI "(struct work_struct *work "  ");"
.SH ARGUMENTS
.IP "work" 12
workqueue struct
.SH "DESCRIPTION"
Doesn't actually do anything except notify userspace. As a consequence of
this event, userspace should try to remap the bad rows since statistically
it is likely the same row is more likely to go bad again.
