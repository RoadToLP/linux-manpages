.TH "phy_configure" 9 "phy_configure" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
phy_configure \- Changes the phy parameters
.SH SYNOPSIS
.B "int" phy_configure
.BI "(struct phy *phy "  ","
.BI "union phy_configure_opts *opts "  ");"
.SH ARGUMENTS
.IP "phy" 12
the phy returned by \fBphy_get\fP
.IP "opts" 12
New configuration to apply
.SH "DESCRIPTION"
Used to change the PHY parameters. \fBphy_init\fP must have been called
on the phy. The configuration will be applied on the current phy
mode, that can be changed using \fBphy_set_mode\fP.
.SH "RETURN"
0 if successful, an negative error code otherwise
