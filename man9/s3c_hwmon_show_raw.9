.TH "s3c_hwmon_show_raw" 9 "s3c_hwmon_show_raw" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
s3c_hwmon_show_raw \- show a conversion from the raw channel number.
.SH SYNOPSIS
.B "ssize_t" s3c_hwmon_show_raw
.BI "(struct device *dev "  ","
.BI "struct device_attribute *attr "  ","
.BI "char *buf "  ");"
.SH ARGUMENTS
.IP "dev" 12
The device that the attribute belongs to.
.IP "attr" 12
The attribute being read.
.IP "buf" 12
The result buffer.
.SH "DESCRIPTION"
This show deals with the raw attribute, registered for each possible
ADC channel. This does a conversion and returns the raw (un-scaled)
value returned from the hardware.
