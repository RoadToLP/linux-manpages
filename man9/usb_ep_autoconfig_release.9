.TH "usb_ep_autoconfig_release" 9 "usb_ep_autoconfig_release" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
usb_ep_autoconfig_release \- releases endpoint and set it to initial state
.SH SYNOPSIS
.B "void" usb_ep_autoconfig_release
.BI "(struct usb_ep *ep "  ");"
.SH ARGUMENTS
.IP "ep" 12
endpoint which should be released
.SH "DESCRIPTION"
This function can be used during function bind for endpoints obtained
from \fBusb_ep_autoconfig\fP. It unclaims endpoint claimed by
\fBusb_ep_autoconfig\fP to make it available for other functions. Endpoint
which was released is no longer invalid and shouldn't be used in
context of function which released it.
