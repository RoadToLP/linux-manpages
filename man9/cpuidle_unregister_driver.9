.TH "cpuidle_unregister_driver" 9 "cpuidle_unregister_driver" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
cpuidle_unregister_driver \- unregisters a driver
.SH SYNOPSIS
.B "void" cpuidle_unregister_driver
.BI "(struct cpuidle_driver *drv "  ");"
.SH ARGUMENTS
.IP "drv" 12
a pointer to a valid struct cpuidle_driver
.SH "DESCRIPTION"
Unregisters the cpuidle driver under a lock to prevent concurrent attempts
to [un]register the driver from occuring at the same time.  \fIdrv\fP has to
match the currently registered driver.
