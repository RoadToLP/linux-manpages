.TH "omap_rproc_start_timer" 9 "omap_rproc_start_timer" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_rproc_start_timer \- start a timer for a remoteproc
.SH SYNOPSIS
.B "int" omap_rproc_start_timer
.BI "(struct omap_rproc_timer *timer "  ");"
.SH ARGUMENTS
.IP "timer" 12
handle to a OMAP rproc timer
.SH "DESCRIPTION"
This helper function is used to start a timer associated with a remoteproc,
obtained using the request_timer ops. The helper function needs to be
invoked by the driver to start the timer (during device initialization)
or to just resume the timer.
.SH "RETURN"
0 on success, otherwise a failure as returned by DMTimer ops
