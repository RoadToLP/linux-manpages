.TH "adapter_init_chip" 9 "adapter_init_chip" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
adapter_init_chip \- Get the chip into a know state and figure out some of the settings that apply to this adapter.
.SH SYNOPSIS
.B "void" adapter_init_chip
.BI "(struct AdapterCtlBlk *acb "  ");"
.SH ARGUMENTS
.IP "acb" 12
The adapter which we are to init.
.SH "DESCRIPTION"

The io port in the adapter needs to have been set before calling
this function. The config will be configured correctly on return.
