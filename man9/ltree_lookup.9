.TH "ltree_lookup" 9 "ltree_lookup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ltree_lookup \- look up the lock tree.
.SH SYNOPSIS
.B "struct ubi_ltree_entry *" ltree_lookup
.BI "(struct ubi_device *ubi "  ","
.BI "int vol_id "  ","
.BI "int lnum "  ");"
.SH ARGUMENTS
.IP "ubi" 12
UBI device description object
.IP "vol_id" 12
volume ID
.IP "lnum" 12
logical eraseblock number
.SH "DESCRIPTION"
This function returns a pointer to the corresponding \fIstruct ubi_ltree_entry\fP
object if the logical eraseblock is locked and NULL if it is not.
\fIubi->ltree_lock\fP has to be locked.
