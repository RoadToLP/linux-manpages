.TH "snd_device_disconnect" 9 "snd_device_disconnect" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_device_disconnect \- disconnect the device
.SH SYNOPSIS
.B "void" snd_device_disconnect
.BI "(struct snd_card *card "  ","
.BI "void *device_data "  ");"
.SH ARGUMENTS
.IP "card" 12
the card instance
.IP "device_data" 12
the data pointer to disconnect
.SH "DESCRIPTION"
Turns the device into the disconnection state, invoking
dev_disconnect callback, if the device was already registered.

Usually called from \fBsnd_card_disconnect\fP.
.SH "RETURN"
Zero if successful, or a negative error code on failure or if the
device not found.
