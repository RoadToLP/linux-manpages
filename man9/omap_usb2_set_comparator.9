.TH "omap_usb2_set_comparator" 9 "omap_usb2_set_comparator" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_usb2_set_comparator \- links the comparator present in the system with this phy @comparator - the companion phy(comparator) for this phy
.SH SYNOPSIS
.B "int" omap_usb2_set_comparator
.BI "(struct phy_companion *comparator "  ");"
.SH ARGUMENTS
.IP "comparator" 12
-- undescribed --
.SH "DESCRIPTION"

The phy companion driver should call this API passing the phy_companion
filled with set_vbus and start_srp to be used by usb phy.

For use by phy companion driver
