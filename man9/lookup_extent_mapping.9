.TH "lookup_extent_mapping" 9 "lookup_extent_mapping" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lookup_extent_mapping \- lookup extent_map
.SH SYNOPSIS
.B "struct extent_map *" lookup_extent_mapping
.BI "(struct extent_map_tree *tree "  ","
.BI "u64 start "  ","
.BI "u64 len "  ");"
.SH ARGUMENTS
.IP "tree" 12
tree to lookup in
.IP "start" 12
byte offset to start the search
.IP "len" 12
length of the lookup range
.SH "DESCRIPTION"
Find and return the first extent_map struct in \fItree\fP that intersects the
[start, len] range.  There may be additional objects in the tree that
intersect, so check the object returned carefully to make sure that no
additional lookups are needed.
