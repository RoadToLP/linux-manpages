.TH "ocfs2_find_resv_lhs" 9 "ocfs2_find_resv_lhs" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ocfs2_find_resv_lhs \- find the window which contains goal
.SH SYNOPSIS
.B "struct ocfs2_alloc_reservation *" ocfs2_find_resv_lhs
.BI "(struct ocfs2_reservation_map *resmap "  ","
.BI "unsigned int goal "  ");"
.SH ARGUMENTS
.IP "resmap" 12
reservation map to search
.IP "goal" 12
which bit to search for
.SH "DESCRIPTION"
If a window containing that goal is not found, we return the window
which comes before goal. Returns NULL on empty rbtree or no window
before goal.
