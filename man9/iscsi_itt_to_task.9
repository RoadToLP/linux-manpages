.TH "iscsi_itt_to_task" 9 "iscsi_itt_to_task" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
iscsi_itt_to_task \- look up task by itt
.SH SYNOPSIS
.B "struct iscsi_task *" iscsi_itt_to_task
.BI "(struct iscsi_conn *conn "  ","
.BI "itt_t itt "  ");"
.SH ARGUMENTS
.IP "conn" 12
iscsi connection
.IP "itt" 12
itt
.SH "DESCRIPTION"
This should be used for mgmt tasks like login and nops, or if
the LDD's itt space does not include the session age.

The session back_lock must be held.
