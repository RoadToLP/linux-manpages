.TH "spi_nor_sort_erase_mask" 9 "spi_nor_sort_erase_mask" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
spi_nor_sort_erase_mask \- sort erase mask
.SH SYNOPSIS
.B "u8" spi_nor_sort_erase_mask
.BI "(struct spi_nor_erase_map *map "  ","
.BI "u8 erase_mask "  ");"
.SH ARGUMENTS
.IP "map" 12
the erase map of the SPI NOR
.IP "erase_mask" 12
the erase type mask to be sorted
.SH "DESCRIPTION"
Replicate the sort done for the map's erase types in BFPT: sort the erase
mask in ascending order with the smallest erase type size starting from
BIT(0) in the sorted erase mask.
.SH "RETURN"
sorted erase mask.
