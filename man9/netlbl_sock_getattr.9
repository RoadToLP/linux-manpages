.TH "netlbl_sock_getattr" 9 "netlbl_sock_getattr" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
netlbl_sock_getattr \- Determine the security attributes of a sock
.SH SYNOPSIS
.B "int" netlbl_sock_getattr
.BI "(struct sock *sk "  ","
.BI "struct netlbl_lsm_secattr *secattr "  ");"
.SH ARGUMENTS
.IP "sk" 12
the sock
.IP "secattr" 12
the security attributes
.SH "DESCRIPTION"
Examines the given sock to see if any NetLabel style labeling has been
applied to the sock, if so it parses the socket label and returns the
security attributes in \fIsecattr\fP.  Returns zero on success, negative values
on failure.
