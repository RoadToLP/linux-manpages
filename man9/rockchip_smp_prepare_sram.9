.TH "rockchip_smp_prepare_sram" 9 "rockchip_smp_prepare_sram" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
rockchip_smp_prepare_sram \- populate necessary sram block Starting cores execute the code residing at the start of the on-chip sram after power-on. Therefore make sure, this sram region is reserved and big enough. After this check, copy the trampoline code that directs the core to the real startup code in ram into the sram-region.
.SH SYNOPSIS
.B "int" rockchip_smp_prepare_sram
.BI "(struct device_node *node "  ");"
.SH ARGUMENTS
.IP "node" 12
mmio-sram device node
