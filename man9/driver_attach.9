.TH "driver_attach" 9 "driver_attach" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
driver_attach \- try to bind driver to devices.
.SH SYNOPSIS
.B "int" driver_attach
.BI "(struct device_driver *drv "  ");"
.SH ARGUMENTS
.IP "drv" 12
driver.
.SH "DESCRIPTION"
Walk the list of devices that the bus has on it and try to
match the driver with each one.  If \fBdriver_probe_device\fP
returns 0 and the \fIdev->driver\fP is set, we've found a
compatible pair.
