.TH "unqueue_me" 9 "unqueue_me" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
unqueue_me \- Remove the futex_q from its futex_hash_bucket
.SH SYNOPSIS
.B "int" unqueue_me
.BI "(struct futex_q *q "  ");"
.SH ARGUMENTS
.IP "q" 12
The futex_q to unqueue
.SH "DESCRIPTION"
The q->lock_ptr must not be held by the caller. A call to \fBunqueue_me\fP must
be paired with exactly one earlier call to \fBqueue_me\fP.
.SH "RETURN"
- 1 - if the futex_q was still queued (and we removed unqueued it);
- 0 - if the futex_q was already removed by the waking thread
