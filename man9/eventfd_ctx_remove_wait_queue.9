.TH "eventfd_ctx_remove_wait_queue" 9 "eventfd_ctx_remove_wait_queue" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
eventfd_ctx_remove_wait_queue \- Read the current counter and removes wait queue.
.SH SYNOPSIS
.B "int" eventfd_ctx_remove_wait_queue
.BI "(struct eventfd_ctx *ctx "  ","
.BI "wait_queue_entry_t *wait "  ","
.BI "__u64 *cnt "  ");"
.SH ARGUMENTS
.IP "ctx" 12
[in] Pointer to eventfd context.
.IP "wait" 12
[in] Wait queue to be removed.
.IP "cnt" 12
[out] Pointer to the 64-bit counter value.
.SH "DESCRIPTION"
Returns 0 if successful, or the following error codes:

-EAGAIN      : The operation would have blocked.

This is used to atomically remove a wait queue entry from the eventfd wait
queue head, and read/reset the counter value.
