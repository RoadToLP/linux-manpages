.TH "batadv_tt_global_check_crc" 9 "batadv_tt_global_check_crc" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_tt_global_check_crc \- check if all the CRCs are correct
.SH SYNOPSIS
.B "bool" batadv_tt_global_check_crc
.BI "(struct batadv_orig_node *orig_node "  ","
.BI "struct batadv_tvlv_tt_vlan_data *tt_vlan "  ","
.BI "u16 num_vlan "  ");"
.SH ARGUMENTS
.IP "orig_node" 12
originator for which the CRCs have to be checked
.IP "tt_vlan" 12
pointer to the first tvlv VLAN entry
.IP "num_vlan" 12
number of tvlv VLAN entries
.SH "RETURN"
true if all the received CRCs match the locally stored ones, false
otherwise
