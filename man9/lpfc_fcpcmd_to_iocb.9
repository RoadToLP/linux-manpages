.TH "lpfc_fcpcmd_to_iocb" 9 "lpfc_fcpcmd_to_iocb" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_fcpcmd_to_iocb \- copy the fcp_cmd data into the IOCB
.SH SYNOPSIS
.B "void" lpfc_fcpcmd_to_iocb
.BI "(u8 *data "  ","
.BI "struct fcp_cmnd *fcp_cmnd "  ");"
.SH ARGUMENTS
.IP "data" 12
A pointer to the immediate command data portion of the IOCB.
.IP "fcp_cmnd" 12
The FCP Command that is provided by the SCSI layer.
.SH "DESCRIPTION"
The routine copies the entire FCP command from \fIfcp_cmnd\fP to \fIdata\fP while
byte swapping the data to big endian format for transmission on the wire.
