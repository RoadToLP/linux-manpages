.TH "ice_cfg_vlan_pruning" 9 "ice_cfg_vlan_pruning" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_cfg_vlan_pruning \- enable or disable VLAN pruning on the VSI
.SH SYNOPSIS
.B "int" ice_cfg_vlan_pruning
.BI "(struct ice_vsi *vsi "  ","
.BI "bool ena "  ","
.BI "bool vlan_promisc "  ");"
.SH ARGUMENTS
.IP "vsi" 12
VSI to enable or disable VLAN pruning on
.IP "ena" 12
set to true to enable VLAN pruning and false to disable it
.IP "vlan_promisc" 12
enable valid security flags if not in VLAN promiscuous mode
.SH "DESCRIPTION"
returns 0 if VSI is updated, negative otherwise
