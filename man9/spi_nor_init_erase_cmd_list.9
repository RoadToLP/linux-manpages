.TH "spi_nor_init_erase_cmd_list" 9 "spi_nor_init_erase_cmd_list" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
spi_nor_init_erase_cmd_list \- initialize erase command list
.SH SYNOPSIS
.B "int" spi_nor_init_erase_cmd_list
.BI "(struct spi_nor *nor "  ","
.BI "struct list_head *erase_list "  ","
.BI "u64 addr "  ","
.BI "u32 len "  ");"
.SH ARGUMENTS
.IP "nor" 12
pointer to a 'struct spi_nor'
.IP "erase_list" 12
list of erase commands to be executed once we validate that the
erase can be performed
.IP "addr" 12
offset in the serial flash memory
.IP "len" 12
number of bytes to erase
.SH "DESCRIPTION"
Builds the list of best fitted erase commands and verifies if the erase can
be performed.
.SH "RETURN"
0 on success, -errno otherwise.
