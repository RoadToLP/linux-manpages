.TH "lpfc_fc_frame_check" 9 "lpfc_fc_frame_check" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_fc_frame_check \- Check that this frame is a valid frame to handle
.SH SYNOPSIS
.B "int" lpfc_fc_frame_check
.BI "(struct lpfc_hba *phba "  ","
.BI "struct fc_frame_header *fc_hdr "  ");"
.SH ARGUMENTS
.IP "phba" 12
pointer to lpfc_hba struct that the frame was received on
.IP "fc_hdr" 12
A pointer to the FC Header data (In Big Endian Format)
.SH "DESCRIPTION"
This function checks the fields in the \fIfc_hdr\fP to see if the FC frame is a
valid type of frame that the LPFC driver will handle. This function will
return a zero if the frame is a valid frame or a non zero value when the
frame does not pass the check.
