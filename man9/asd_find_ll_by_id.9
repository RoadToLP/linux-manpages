.TH "asd_find_ll_by_id" 9 "asd_find_ll_by_id" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
asd_find_ll_by_id \- find a linked list entry by its id
.SH SYNOPSIS
.B "void *" asd_find_ll_by_id
.BI "(void * const start "  ","
.BI "const u8 id0 "  ","
.BI "const u8 id1 "  ");"
.SH ARGUMENTS
.IP "start" 12
void pointer to the first element in the linked list
.IP "id0" 12
the first byte of the id  (offs 0)
.IP "id1" 12
the second byte of the id (offs 1)
.SH "DESCRIPTION"
\fIstart\fP has to be the _base_ element start, since the
linked list entries's offset is from this pointer.
Some linked list entries use only the first id, in which case
you can pass 0xFF for the second.
