.TH "ice_vsi_setup_vector_base" 9 "ice_vsi_setup_vector_base" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_vsi_setup_vector_base \- Set up the base vector for the given VSI
.SH SYNOPSIS
.B "int" ice_vsi_setup_vector_base
.BI "(struct ice_vsi *vsi "  ");"
.SH ARGUMENTS
.IP "vsi" 12
ptr to the VSI
.SH "DESCRIPTION"
This should only be called after \fBice_vsi_alloc\fP which allocates the
corresponding SW VSI structure and initializes num_queue_pairs for the
newly allocated VSI.

Returns 0 on success or negative on failure
