.TH "lpfc_idiag_cmd_get" 9 "lpfc_idiag_cmd_get" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_idiag_cmd_get \- Get and parse idiag debugfs comands from user space
.SH SYNOPSIS
.B "int" lpfc_idiag_cmd_get
.BI "(const char __user *buf "  ","
.BI "size_t nbytes "  ","
.BI "struct lpfc_idiag_cmd *idiag_cmd "  ");"
.SH ARGUMENTS
.IP "buf" 12
The pointer to the user space buffer.
.IP "nbytes" 12
The number of bytes in the user space buffer.
.IP "idiag_cmd" 12
pointer to the idiag command struct.
.SH "DESCRIPTION"
This routine reads data from debugfs user space buffer and parses the
buffer for getting the idiag command and arguments. The while space in
between the set of data is used as the parsing separator.

This routine returns 0 when successful, it returns proper error code
back to the user space in error conditions.
