.TH "pcc_mbox_probe" 9 "pcc_mbox_probe" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pcc_mbox_probe \- Called when we find a match for the PCCT platform device. This is purely used to represent the PCCT as a virtual device for registering with the generic Mailbox framework.
.SH SYNOPSIS
.B "int" pcc_mbox_probe
.BI "(struct platform_device *pdev "  ");"
.SH ARGUMENTS
.IP "pdev" 12
Pointer to platform device returned when a match
is found.
.SH "RETURN"
0 for Success, else errno.
