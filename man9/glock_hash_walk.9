.TH "glock_hash_walk" 9 "glock_hash_walk" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
glock_hash_walk \- Call a function for glock in a hash bucket
.SH SYNOPSIS
.B "void" glock_hash_walk
.BI "(glock_examiner examiner "  ","
.BI "const struct gfs2_sbd *sdp "  ");"
.SH ARGUMENTS
.IP "examiner" 12
the function
.IP "sdp" 12
the filesystem
.SH "DESCRIPTION"
Note that the function can be called multiple times on the same
object.  So the user must ensure that the function can cope with
that.
