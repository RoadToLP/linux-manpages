.TH "vfs_create_mount" 9 "vfs_create_mount" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vfs_create_mount \- Create a mount for a configured superblock
.SH SYNOPSIS
.B "struct vfsmount *" vfs_create_mount
.BI "(struct fs_context *fc "  ");"
.SH ARGUMENTS
.IP "fc" 12
The configuration context with the superblock attached
.SH "DESCRIPTION"
Create a mount to an already configured superblock.  If necessary, the
caller should invoke \fBvfs_get_tree\fP before calling this.

Note that this does not attach the mount to anything.
