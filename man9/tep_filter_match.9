.TH "tep_filter_match" 9 "tep_filter_match" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tep_filter_match \- test if a record matches a filter
.SH SYNOPSIS
.B "enum tep_errno" tep_filter_match
.BI "(struct tep_event_filter *filter "  ","
.BI "struct tep_record *record "  ");"
.SH ARGUMENTS
.IP "filter" 12
filter struct with filter information
.IP "record" 12
the record to test against the filter
.SH "RETURN"
match result or error code (prefixed with TEP_ERRNO__)
FILTER_MATCH - filter found for event and \fIrecord\fP matches
FILTER_MISS  - filter found for event and \fIrecord\fP does not match
FILTER_NOT_FOUND - no filter found for \fIrecord\fP's event
NO_FILTER - if no filters exist
otherwise - error occurred during test
