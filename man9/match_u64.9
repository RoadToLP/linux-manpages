.TH "match_u64" 9 "match_u64" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
match_u64 \- scan a decimal representation of a u64 from a substring_t
.SH SYNOPSIS
.B "int" match_u64
.BI "(substring_t *s "  ","
.BI "u64 *result "  ");"
.SH ARGUMENTS
.IP "s" 12
substring_t to be scanned
.IP "result" 12
resulting unsigned long long on success
.SH "DESCRIPTION"
Attempts to parse the \fIsubstring_t\fP \fIs\fP as a long decimal
integer. On success, sets \fIresult\fP to the integer represented by the
string and returns 0.
Returns -ENOMEM, -EINVAL, or -ERANGE on failure.
