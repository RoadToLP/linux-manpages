.TH "pty_start" 9 "pty_start" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pty_start \- start() handler pty_stop - stop() handler
.SH SYNOPSIS
.B "void" pty_start
.BI "(struct tty_struct *tty "  ");"
.SH ARGUMENTS
.IP "tty" 12
tty being flow-controlled

Propagates the TIOCPKT status to the master pty.

NB: only the master pty can be in packet mode so only the slave
needs \fBstart\fP/\fBstop\fP handlers
