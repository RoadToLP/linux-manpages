.TH "amba_device_add" 9 "amba_device_add" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
amba_device_add \- add a previously allocated AMBA device structure
.SH SYNOPSIS
.B "int" amba_device_add
.BI "(struct amba_device *dev "  ","
.BI "struct resource *parent "  ");"
.SH ARGUMENTS
.IP "dev" 12
AMBA device allocated by amba_device_alloc
.IP "parent" 12
resource parent for this devices resources

Claim the resource, and read the device cell ID if not already
initialized.  Register the AMBA device with the Linux device
manager.
