.TH "rmi_register_transport_device" 9 "rmi_register_transport_device" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
rmi_register_transport_device \- register a transport device connection on the RMI bus. Transport drivers provide communication from the devices on a bus (such as SPI, I2C, and so on) to the RMI4 sensor.
.SH SYNOPSIS
.B "int" rmi_register_transport_device
.BI "(struct rmi_transport_dev *xport "  ");"
.SH ARGUMENTS
.IP "xport" 12
the transport device to register
