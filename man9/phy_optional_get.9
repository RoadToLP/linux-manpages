.TH "phy_optional_get" 9 "phy_optional_get" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
phy_optional_get \- lookup and obtain a reference to an optional phy.
.SH SYNOPSIS
.B "struct phy *" phy_optional_get
.BI "(struct device *dev "  ","
.BI "const char *string "  ");"
.SH ARGUMENTS
.IP "dev" 12
device that requests this phy
.IP "string" 12
the phy name as given in the dt data or the name of the controller
port for non-dt case
.SH "DESCRIPTION"
Returns the phy driver, after getting a refcount to it; or
NULL if there is no such phy.  The caller is responsible for
calling \fBphy_put\fP to release that count.
