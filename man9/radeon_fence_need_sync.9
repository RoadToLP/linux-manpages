.TH "radeon_fence_need_sync" 9 "radeon_fence_need_sync" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
radeon_fence_need_sync \- do we need a semaphore
.SH SYNOPSIS
.B "bool" radeon_fence_need_sync
.BI "(struct radeon_fence *fence "  ","
.BI "int dst_ring "  ");"
.SH ARGUMENTS
.IP "fence" 12
radeon fence object
.IP "dst_ring" 12
which ring to check against
.SH "DESCRIPTION"
Check if the fence needs to be synced against another ring
(all asics).  If so, we need to emit a semaphore.
Returns true if we need to sync with another ring, false if
not.
