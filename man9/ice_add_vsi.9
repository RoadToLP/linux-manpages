.TH "ice_add_vsi" 9 "ice_add_vsi" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_add_vsi \- add VSI context to the hardware and VSI handle list
.SH SYNOPSIS
.B "enum ice_status" ice_add_vsi
.BI "(struct ice_hw *hw "  ","
.BI "u16 vsi_handle "  ","
.BI "struct ice_vsi_ctx *vsi_ctx "  ","
.BI "struct ice_sq_cd *cd "  ");"
.SH ARGUMENTS
.IP "hw" 12
pointer to the HW struct
.IP "vsi_handle" 12
unique VSI handle provided by drivers
.IP "vsi_ctx" 12
pointer to a VSI context struct
.IP "cd" 12
pointer to command details structure or NULL
.SH "DESCRIPTION"
Add a VSI context to the hardware also add it into the VSI handle list.
If this function gets called after reset for existing VSIs then update
with the new HW VSI number in the corresponding VSI handle list entry.
