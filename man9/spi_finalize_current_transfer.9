.TH "spi_finalize_current_transfer" 9 "spi_finalize_current_transfer" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
spi_finalize_current_transfer \- report completion of a transfer
.SH SYNOPSIS
.B "void" spi_finalize_current_transfer
.BI "(struct spi_controller *ctlr "  ");"
.SH ARGUMENTS
.IP "ctlr" 12
the controller reporting completion
.SH "DESCRIPTION"
Called by SPI drivers using the core \fBtransfer_one_message\fP
implementation to notify it that the current interrupt driven
transfer has finished and the next one may be scheduled.
