.TH "die_entrypc" 9 "die_entrypc" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
die_entrypc \- Returns entry PC (the lowest address) of a DIE
.SH SYNOPSIS
.B "int" die_entrypc
.BI "(Dwarf_Die *dw_die "  ","
.BI "Dwarf_Addr *addr "  ");"
.SH ARGUMENTS
.IP "dw_die" 12
a DIE
.IP "addr" 12
where to store entry PC
.SH "DESCRIPTION"
Since \fBdwarf_entrypc\fP does not return entry PC if the DIE has only address
range, we have to use this to retrieve the lowest address from the address
range attribute.
