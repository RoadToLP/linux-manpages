.TH "usb_pipe_type_check" 9 "usb_pipe_type_check" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
usb_pipe_type_check \- sanity check of a specific pipe for a usb device
.SH SYNOPSIS
.B "int" usb_pipe_type_check
.BI "(struct usb_device *dev "  ","
.BI "unsigned int pipe "  ");"
.SH ARGUMENTS
.IP "dev" 12
struct usb_device to be checked
.IP "pipe" 12
pipe to check
.SH "DESCRIPTION"
This performs a light-weight sanity check for the endpoint in the
given usb device.  It returns 0 if the pipe is valid for the specific usb
device, otherwise a negative error code.
