.TH "batadv_nc_code_packets" 9 "batadv_nc_code_packets" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_nc_code_packets \- code a received unicast_packet with an nc packet into a coded_packet and send it
.SH SYNOPSIS
.B "bool" batadv_nc_code_packets
.BI "(struct batadv_priv *bat_priv "  ","
.BI "struct sk_buff *skb "  ","
.BI "struct ethhdr *ethhdr "  ","
.BI "struct batadv_nc_packet *nc_packet "  ","
.BI "struct batadv_neigh_node *neigh_node "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "skb" 12
data skb to forward
.IP "ethhdr" 12
pointer to the ethernet header inside the skb
.IP "nc_packet" 12
structure containing the packet to the skb can be coded with
.IP "neigh_node" 12
next hop to forward packet to
.SH "RETURN"
true if both packets are consumed, false otherwise.
