.TH "pci_match_id" 9 "pci_match_id" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pci_match_id \- See if a PCI device matches a given pci_id table
.SH SYNOPSIS
.B "const struct pci_device_id *" pci_match_id
.BI "(const struct pci_device_id *ids "  ","
.BI "struct pci_dev *dev "  ");"
.SH ARGUMENTS
.IP "ids" 12
array of PCI device ID structures to search in
.IP "dev" 12
the PCI device structure to match against.
.SH "DESCRIPTION"
Used by a driver to check whether a PCI device is in its list of
supported devices.  Returns the matching pci_device_id structure or
NULL if there is no match.

Deprecated; don't use this as it will not catch any dynamic IDs
that a driver might want to check for.
