.TH "blk_queue_required_elevator_features" 9 "blk_queue_required_elevator_features" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
blk_queue_required_elevator_features \- Set a queue required elevator features
.SH SYNOPSIS
.B "void" blk_queue_required_elevator_features
.BI "(struct request_queue *q "  ","
.BI "unsigned int features "  ");"
.SH ARGUMENTS
.IP "q" 12
the request queue for the target device
.IP "features" 12
Required elevator features OR'ed together
.SH "DESCRIPTION"
Tell the block layer that for the device controlled through \fIq\fP, only the
only elevators that can be used are those that implement at least the set of
features specified by \fIfeatures\fP.
