.TH "lpfc_rscn_recovery_check" 9 "lpfc_rscn_recovery_check" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_rscn_recovery_check \- Send recovery event to vport nodes matching rscn
.SH SYNOPSIS
.B "int" lpfc_rscn_recovery_check
.BI "(struct lpfc_vport *vport "  ");"
.SH ARGUMENTS
.IP "vport" 12
pointer to a host virtual N_Port data structure.
.SH "DESCRIPTION"
This routine sends recovery (NLP_EVT_DEVICE_RECOVERY) event to the
state machine for a \fIvport\fP's nodes that are with pending RSCN (Registration
State Change Notification).

Return code
0 - Successful (currently alway return 0)
