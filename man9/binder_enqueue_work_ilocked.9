.TH "binder_enqueue_work_ilocked" 9 "binder_enqueue_work_ilocked" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
binder_enqueue_work_ilocked \- Add an item to the work list
.SH SYNOPSIS
.B "void" binder_enqueue_work_ilocked
.BI "(struct binder_work *work "  ","
.BI "struct list_head *target_list "  ");"
.SH ARGUMENTS
.IP "work" 12
struct binder_work to add to list
.IP "target_list" 12
list to add work to
.SH "DESCRIPTION"
Adds the work to the specified list. Asserts that work
is not already on a list.

Requires the proc->inner_lock to be held.
