.TH "tep_find_function" 9 "tep_find_function" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tep_find_function \- find a function by a given address
.SH SYNOPSIS
.B "const char *" tep_find_function
.BI "(struct tep_handle *tep "  ","
.BI "unsigned long long addr "  ");"
.SH ARGUMENTS
.IP "tep" 12
a handle to the trace event parser context
.IP "addr" 12
the address to find the function with
.SH "DESCRIPTION"
Returns a pointer to the function stored that has the given
address. Note, the address does not have to be exact, it
will select the function that would contain the address.
