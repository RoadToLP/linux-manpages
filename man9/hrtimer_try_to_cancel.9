.TH "hrtimer_try_to_cancel" 9 "hrtimer_try_to_cancel" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
hrtimer_try_to_cancel \- try to deactivate a timer
.SH SYNOPSIS
.B "int" hrtimer_try_to_cancel
.BI "(struct hrtimer *timer "  ");"
.SH ARGUMENTS
.IP "timer" 12
hrtimer to stop
.SH "RETURN"

*  0 when the timer was not active
*  1 when the timer was active
* -1 when the timer is currently executing the callback function and
cannot be stopped
