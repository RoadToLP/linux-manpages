.TH "iavf_clean_tx_irq" 9 "iavf_clean_tx_irq" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
iavf_clean_tx_irq \- Reclaim resources after transmit completes
.SH SYNOPSIS
.B "bool" iavf_clean_tx_irq
.BI "(struct iavf_vsi *vsi "  ","
.BI "struct iavf_ring *tx_ring "  ","
.BI "int napi_budget "  ");"
.SH ARGUMENTS
.IP "vsi" 12
the VSI we care about
.IP "tx_ring" 12
Tx ring to clean
.IP "napi_budget" 12
Used to determine if we are in netpoll
.SH "DESCRIPTION"
Returns true if there's any budget left (e.g. the clean is finished)
