.TH "lpfc_sli4_config" 9 "lpfc_sli4_config" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_sli4_config \- Initialize the SLI4 Config Mailbox command
.SH SYNOPSIS
.B "int" lpfc_sli4_config
.BI "(struct lpfc_hba *phba "  ","
.BI "struct lpfcMboxq *mbox "  ","
.BI "uint8_t subsystem "  ","
.BI "uint8_t opcode "  ","
.BI "uint32_t length "  ","
.BI "bool emb "  ");"
.SH ARGUMENTS
.IP "phba" 12
pointer to lpfc hba data structure.
.IP "mbox" 12
pointer to lpfc mbox command.
.IP "subsystem" 12
The sli4 config sub mailbox subsystem.
.IP "opcode" 12
The sli4 config sub mailbox command opcode.
.IP "length" 12
Length of the sli4 config mailbox command (including sub-header).
.IP "emb" 12
True if embedded mbox command should be setup.
.SH "DESCRIPTION"
This routine sets up the header fields of SLI4 specific mailbox command
for sending IOCTL command.
.SH "RETURN"
the actual length of the mbox command allocated (mostly useful
for none embedded mailbox command).
