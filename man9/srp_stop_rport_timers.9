.TH "srp_stop_rport_timers" 9 "srp_stop_rport_timers" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
srp_stop_rport_timers \- stop the transport layer recovery timers
.SH SYNOPSIS
.B "void" srp_stop_rport_timers
.BI "(struct srp_rport *rport "  ");"
.SH ARGUMENTS
.IP "rport" 12
SRP remote port for which to stop the timers.
.SH "DESCRIPTION"
Must be called after \fBsrp_remove_host\fP and \fBscsi_remove_host\fP. The caller
must hold a reference on the rport (rport->dev) and on the SCSI host
(rport->dev.parent).
