.TH "ipa_endpoint_reset_rx_aggr" 9 "ipa_endpoint_reset_rx_aggr" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ipa_endpoint_reset_rx_aggr \- Reset RX endpoint with aggregation active
.SH SYNOPSIS
.B "int" ipa_endpoint_reset_rx_aggr
.BI "(struct ipa_endpoint *endpoint "  ");"
.SH ARGUMENTS
.IP "endpoint" 12
Endpoint to be reset
.SH "DESCRIPTION"
If aggregation is active on an RX endpoint when a reset is performed
on its underlying GSI channel, a special sequence of actions must be
taken to ensure the IPA pipeline is properly cleared.
.SH "RETURN"
0 if successful, or a negative error code
