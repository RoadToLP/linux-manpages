.TH "netdev_tx" 9 "netdev_tx" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
netdev_tx \- send out packet
.SH SYNOPSIS
.B "netdev_tx_t" netdev_tx
.BI "(struct sk_buff *skb "  ","
.BI "struct net_device *dev "  ");"
.SH ARGUMENTS
.IP "skb" 12
Socket buffer.
.IP "dev" 12
Network device.
.SH "DESCRIPTION"
This function is used by the upper network layer to send out a packet.

Return 0 if successful; otherwise an error code indicating failure.
