.TH "snd_soc_dapm_add_routes" 9 "snd_soc_dapm_add_routes" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_soc_dapm_add_routes \- Add routes between DAPM widgets
.SH SYNOPSIS
.B "int" snd_soc_dapm_add_routes
.BI "(struct snd_soc_dapm_context *dapm "  ","
.BI "const struct snd_soc_dapm_route *route "  ","
.BI "int num "  ");"
.SH ARGUMENTS
.IP "dapm" 12
DAPM context
.IP "route" 12
audio routes
.IP "num" 12
number of routes
.SH "DESCRIPTION"
Connects 2 dapm widgets together via a named audio path. The sink is
the widget receiving the audio signal, whilst the source is the sender
of the audio signal.

Returns 0 for success else error. On error all resources can be freed
with a call to \fBsnd_soc_card_free\fP.
