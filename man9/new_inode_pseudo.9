.TH "new_inode_pseudo" 9 "new_inode_pseudo" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
new_inode_pseudo \- obtain an inode
.SH SYNOPSIS
.B "struct inode *" new_inode_pseudo
.BI "(struct super_block *sb "  ");"
.SH ARGUMENTS
.IP "sb" 12
superblock

Allocates a new inode for given superblock.
Inode wont be chained in superblock s_inodes list
This means :
- fs can't be unmount
- quotas, fsnotify, writeback can't work
