.TH "adf_dev_get" 9 "adf_dev_get" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
adf_dev_get \- Increment accel_dev reference count
.SH SYNOPSIS
.B "int" adf_dev_get
.BI "(struct adf_accel_dev *accel_dev "  ");"
.SH ARGUMENTS
.IP "accel_dev" 12
Pointer to acceleration device.
.SH "DESCRIPTION"
Increment the accel_dev refcount and if this is the first time
incrementing it during this period the accel_dev is in use,
increment the module refcount too.
To be used by QAT device specific drivers.
.SH "RETURN"
0 when successful, EFAULT when fail to bump module refcount
