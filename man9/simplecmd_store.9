.TH "simplecmd_store" 9 "simplecmd_store" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
simplecmd_store \- Send a simple IPC command
.SH SYNOPSIS
.B "ssize_t" simplecmd_store
.BI "(struct device *dev "  ","
.BI "struct device_attribute *attr "  ","
.BI "const char *buf "  ","
.BI "size_t count "  ");"
.SH ARGUMENTS
.IP "dev" 12
Device under the attribute is
.IP "attr" 12
Attribute in question
.IP "buf" 12
Buffer holding data to be stored to the attribute
.IP "count" 12
Number of bytes in \fIbuf\fP
.SH "DESCRIPTION"
Expects a string with two integers separated with space. These two
values hold command and subcommand that is send to PMC.
.SH "RETURN"
Number number of bytes written (\fIcount\fP) or negative errno in
case of error.
