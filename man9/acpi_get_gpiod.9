.TH "acpi_get_gpiod" 9 "acpi_get_gpiod" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_get_gpiod \- Translate ACPI GPIO pin to GPIO descriptor usable with GPIO API
.SH SYNOPSIS
.B "struct gpio_desc *" acpi_get_gpiod
.BI "(char *path "  ","
.BI "int pin "  ");"
.SH ARGUMENTS
.IP "path" 12
ACPI GPIO controller full path name, (e.g. "\\_SB.GPO1")
.IP "pin" 12
ACPI GPIO pin number (0-based, controller-relative)
.SH "RETURN"
GPIO descriptor to use with Linux generic GPIO API, or ERR_PTR
error value. Specifically returns -EPROBE_DEFER if the referenced GPIO
controller does not have GPIO chip registered at the moment. This is to
support probe deferral.
