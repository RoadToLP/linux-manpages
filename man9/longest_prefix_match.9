.TH "longest_prefix_match" 9 "longest_prefix_match" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
longest_prefix_match \- determine the longest prefix
.SH SYNOPSIS
.B "size_t" longest_prefix_match
.BI "(const struct lpm_trie *trie "  ","
.BI "const struct lpm_trie_node *node "  ","
.BI "const struct bpf_lpm_trie_key *key "  ");"
.SH ARGUMENTS
.IP "trie" 12
The trie to get internal sizes from
.IP "node" 12
The node to operate on
.IP "key" 12
The key to compare to \fInode\fP
.SH "DESCRIPTION"
Determine the longest prefix of \fInode\fP that matches the bits in \fIkey\fP.
