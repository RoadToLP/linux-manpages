.TH "phy_set_max_speed" 9 "phy_set_max_speed" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
phy_set_max_speed \- Set the maximum speed the PHY should support
.SH SYNOPSIS
.B "int" phy_set_max_speed
.BI "(struct phy_device *phydev "  ","
.BI "u32 max_speed "  ");"
.SH ARGUMENTS
.IP "phydev" 12
The phy_device struct
.IP "max_speed" 12
Maximum speed
.SH "DESCRIPTION"
The PHY might be more capable than the MAC. For example a Fast Ethernet
is connected to a 1G PHY. This function allows the MAC to indicate its
maximum speed, and so limit what the PHY will advertise.
