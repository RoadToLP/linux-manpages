.TH "ixgbe_get_san_mac_addr_generic" 9 "ixgbe_get_san_mac_addr_generic" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ixgbe_get_san_mac_addr_generic \- SAN MAC address retrieval from the EEPROM
.SH SYNOPSIS
.B "s32" ixgbe_get_san_mac_addr_generic
.BI "(struct ixgbe_hw *hw "  ","
.BI "u8 *san_mac_addr "  ");"
.SH ARGUMENTS
.IP "hw" 12
pointer to hardware structure
.IP "san_mac_addr" 12
SAN MAC address

Reads the SAN MAC address from the EEPROM, if it's available.  This is
per-port, so \fBset_lan_id\fP must be called before reading the addresses.
\fBset_lan_id\fP is called by \fBidentify_sfp\fP, but this cannot be relied
upon for non-SFP connections, so we must call it here.
