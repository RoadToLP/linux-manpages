.TH "pwrdm_for_each" 9 "pwrdm_for_each" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pwrdm_for_each \- call function on each registered clockdomain
.SH SYNOPSIS
.B "int" pwrdm_for_each
.BI "(int (*fn)(struct powerdomain *pwrdm, void *user) "  ","
.BI "void *user "  ");"
.SH ARGUMENTS
.IP "fn" 12
callback function *
.IP "user" 12
-- undescribed --
.SH "DESCRIPTION"
Call the supplied function \fIfn\fP for each registered powerdomain.
The callback function \fIfn\fP can return anything but 0 to bail out
early from the iterator.  Returns the last return value of the
callback function, which should be 0 for success or anything else
to indicate failure; or -EINVAL if the function pointer is null.
