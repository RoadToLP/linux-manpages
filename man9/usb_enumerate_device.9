.TH "usb_enumerate_device" 9 "usb_enumerate_device" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
usb_enumerate_device \- Read device configs/intfs/otg (usbcore-internal)
.SH SYNOPSIS
.B "int" usb_enumerate_device
.BI "(struct usb_device *udev "  ");"
.SH ARGUMENTS
.IP "udev" 12
newly addressed device (in ADDRESS state)
.SH "DESCRIPTION"
This is only called by \fBusb_new_device\fP and \fBusb_authorize_device\fP
and FIXME -- all comments that apply to them apply here wrt to
environment.

If the device is WUSB and not authorized, we don't attempt to read
the string descriptors, as they will be errored out by the device
until it has been authorized.
.SH "RETURN"
0 if successful. A negative error code otherwise.
