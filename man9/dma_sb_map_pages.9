.TH "dma_sb_map_pages" 9 "dma_sb_map_pages" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dma_sb_map_pages \- Maps dma pages into the io controller bus address space.
.SH SYNOPSIS
.B "int" dma_sb_map_pages
.BI "(struct ps3_dma_region *r "  ","
.BI "unsigned long phys_addr "  ","
.BI "unsigned long len "  ","
.BI "struct dma_chunk **c_out "  ","
.BI "u64 iopte_flag "  ");"
.SH ARGUMENTS
.IP "r" 12
Pointer to a struct ps3_dma_region.
.IP "phys_addr" 12
Starting physical address of the area to map.
.IP "len" 12
Length in bytes of the area to map.
c_out: A pointer to receive an allocated struct dma_chunk for this area.
.IP "c_out" 12
-- undescribed --
.IP "iopte_flag" 12
-- undescribed --
.SH "DESCRIPTION"
This is the lowest level dma mapping routine, and is the one that will
make the HV call to add the pages into the io controller address space.
