.TH "_setup_reset" 9 "_setup_reset" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
_setup_reset \- reset an IP block during the setup process
.SH SYNOPSIS
.B "int" _setup_reset
.BI "(struct omap_hwmod *oh "  ");"
.SH ARGUMENTS
.IP "oh" 12
struct omap_hwmod *
.SH "DESCRIPTION"
Reset the IP block corresponding to the hwmod \fIoh\fP during the setup
process.  The IP block is first enabled so it can be successfully
reset.  Returns 0 upon success or a negative error code upon
failure.
