.TH "amdgpu_ring_commit" 9 "amdgpu_ring_commit" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
amdgpu_ring_commit \- tell the GPU to execute the new commands on the ring buffer
.SH SYNOPSIS
.B "void" amdgpu_ring_commit
.BI "(struct amdgpu_ring *ring "  ");"
.SH ARGUMENTS
.IP "ring" 12
amdgpu_ring structure holding ring information
.SH "DESCRIPTION"
Update the wptr (write pointer) to tell the GPU to
execute new commands on the ring buffer (all asics).
