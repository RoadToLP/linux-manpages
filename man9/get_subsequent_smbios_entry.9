.TH "get_subsequent_smbios_entry" 9 "get_subsequent_smbios_entry" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
get_subsequent_smbios_entry \- get the next entry from bios table.
.SH SYNOPSIS
.B "void __iomem *" get_subsequent_smbios_entry
.BI "(void __iomem *smbios_start "  ","
.BI "void __iomem *smbios_table "  ","
.BI "void __iomem *curr "  ");"
.SH ARGUMENTS
.IP "smbios_start" 12
where to start in the SMBIOS table
.IP "smbios_table" 12
location of the SMBIOS table
.IP "curr" 12
NULL or pointer to previously returned structure
.SH "DESCRIPTION"
Gets the first entry if previous == NULL;
otherwise, returns the next entry.
Uses global SMBIOS Table pointer.

Returns a pointer to an SMBIOS structure or NULL if none found.
