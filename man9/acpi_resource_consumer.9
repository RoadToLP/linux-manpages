.TH "acpi_resource_consumer" 9 "acpi_resource_consumer" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_resource_consumer \- Find the ACPI device that consumes @res.
.SH SYNOPSIS
.B "struct acpi_device *" acpi_resource_consumer
.BI "(struct resource *res "  ");"
.SH ARGUMENTS
.IP "res" 12
Resource to search for.
.SH "DESCRIPTION"
Search the current resource settings (_CRS) of every ACPI device node
for \fIres\fP.  If we find an ACPI device whose _CRS includes \fIres\fP, return
it.  Otherwise, return NULL.
