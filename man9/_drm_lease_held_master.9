.TH "_drm_lease_held_master" 9 "_drm_lease_held_master" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
_drm_lease_held_master \- check to see if an object is leased (or owned) by master (idr_mutex held)
.SH SYNOPSIS
.B "int" _drm_lease_held_master
.BI "(struct drm_master *master "  ","
.BI "int id "  ");"
.SH ARGUMENTS
.IP "master" 12
the master to check the lease status of
.IP "id" 12
the id to check
.SH "DESCRIPTION"
Checks if the specified master holds a lease on the object. Return
value:

true            'master' holds a lease on (or owns) the object
false           'master' does not hold a lease.
