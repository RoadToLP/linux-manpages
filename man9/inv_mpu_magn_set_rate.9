.TH "inv_mpu_magn_set_rate" 9 "inv_mpu_magn_set_rate" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
inv_mpu_magn_set_rate \- set magnetometer sampling rate
.SH SYNOPSIS
.B "int" inv_mpu_magn_set_rate
.BI "(const struct inv_mpu6050_state *st "  ","
.BI "int fifo_rate "  ");"
.SH ARGUMENTS
.IP "st" 12
driver internal state
.IP "fifo_rate" 12
mpu set fifo rate
.SH "DESCRIPTION"
Returns 0 on success, a negative error code otherwise

Limit sampling frequency to the maximum value supported by the
magnetometer chip. Resulting in duplicated data for higher frequencies.
Noop if there is no magnetometer in the chip.
