.TH "ice_find_boost_entry" 9 "ice_find_boost_entry" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_find_boost_entry \- 
.SH SYNOPSIS
.B "enum ice_status" ice_find_boost_entry
.BI "(struct ice_seg *ice_seg "  ","
.BI "u16 addr "  ","
.BI "struct ice_boost_tcam_entry **entry "  ");"
.SH ARGUMENTS
.IP "ice_seg" 12
pointer to the ice segment (non-NULL)
.IP "addr" 12
Boost TCAM address of entry to search for
.IP "entry" 12
returns pointer to the entry
.SH "DESCRIPTION"
Finds a particular Boost TCAM entry and returns a pointer to that entry
if it is found. The ice_seg parameter must not be NULL since the first call
to ice_pkg_enum_entry requires a pointer to an actual ice_segment structure.
