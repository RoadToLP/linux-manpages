.TH "binder_inc_ref_olocked" 9 "binder_inc_ref_olocked" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
binder_inc_ref_olocked \- increment the ref for given handle
.SH SYNOPSIS
.B "int" binder_inc_ref_olocked
.BI "(struct binder_ref *ref "  ","
.BI "int strong "  ","
.BI "struct list_head *target_list "  ");"
.SH ARGUMENTS
.IP "ref" 12
ref to be incremented
.IP "strong" 12
if true, strong increment, else weak
.IP "target_list" 12
list to queue node work on
.SH "DESCRIPTION"
Increment the ref. \fIref->proc->outer_lock\fP must be held on entry
.SH "RETURN"
0, if successful, else errno
