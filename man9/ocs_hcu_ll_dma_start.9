.TH "ocs_hcu_ll_dma_start" 9 "ocs_hcu_ll_dma_start" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ocs_hcu_ll_dma_start \- Start OCS HCU hashing via DMA
.SH SYNOPSIS
.B "int" ocs_hcu_ll_dma_start
.BI "(struct ocs_hcu_dev *hcu_dev "  ","
.BI "const struct ocs_hcu_dma_list *dma_list "  ","
.BI "bool finalize "  ");"
.SH ARGUMENTS
.IP "hcu_dev" 12
The OCS HCU device to use.
.IP "dma_list" 12
The OCS DMA list mapping the data to hash.
.IP "finalize" 12
Whether or not this is the last hashing operation and therefore
the final hash should be compute even if data is not
block-aligned.
.SH "RETURN"
0 on success, negative error code otherwise.
