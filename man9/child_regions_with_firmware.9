.TH "child_regions_with_firmware" 9 "child_regions_with_firmware" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
child_regions_with_firmware \- 
.SH SYNOPSIS
.B "int" child_regions_with_firmware
.BI "(struct device_node *overlay "  ");"
.SH ARGUMENTS
.IP "overlay" 12
device node of the overlay
.SH "DESCRIPTION"
If the overlay adds child FPGA regions, they are not allowed to have
firmware-name property.

Return 0 for OK or -EINVAL if child FPGA region adds firmware-name.
