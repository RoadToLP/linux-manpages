.TH "qfprom_enable_fuse_blowing" 9 "qfprom_enable_fuse_blowing" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
qfprom_enable_fuse_blowing \- Enable fuse blowing.
.SH SYNOPSIS
.B "int" qfprom_enable_fuse_blowing
.BI "(const struct qfprom_priv *priv "  ","
.BI "struct qfprom_touched_values *old "  ");"
.SH ARGUMENTS
.IP "priv" 12
Our driver data.
.IP "old" 12
We'll stash stuff here to use when disabling.
.SH "DESCRIPTION"
Sets the value of the blow timer, accel register and the clock
and voltage settings.

Prints messages if there are errors so caller doesn't need to.
.SH "RETURN"
0 or -err.
