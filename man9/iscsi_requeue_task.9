.TH "iscsi_requeue_task" 9 "iscsi_requeue_task" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
iscsi_requeue_task \- requeue task to run from session workqueue
.SH SYNOPSIS
.B "void" iscsi_requeue_task
.BI "(struct iscsi_task *task "  ");"
.SH ARGUMENTS
.IP "task" 12
task to requeue
.SH "DESCRIPTION"
Callers must have taken a ref to the task that is going to be requeued.
