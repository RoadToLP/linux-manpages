.TH "ice_vf_pre_vsi_rebuild" 9 "ice_vf_pre_vsi_rebuild" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_vf_pre_vsi_rebuild \- tasks to be done prior to VSI rebuild
.SH SYNOPSIS
.B "void" ice_vf_pre_vsi_rebuild
.BI "(struct ice_vf *vf "  ");"
.SH ARGUMENTS
.IP "vf" 12
VF to perform pre VSI rebuild tasks
.SH "DESCRIPTION"
These tasks are items that don't need to be amortized since they are most
likely called in a for loop with all VF(s) in the \fBreset_all_vfs\fP case.
