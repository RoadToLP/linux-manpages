.TH "netdev_has_upper_dev_all_rcu" 9 "netdev_has_upper_dev_all_rcu" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
netdev_has_upper_dev_all_rcu \- Check if device is linked to an upper device
.SH SYNOPSIS
.B "bool" netdev_has_upper_dev_all_rcu
.BI "(struct net_device *dev "  ","
.BI "struct net_device *upper_dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
device
.IP "upper_dev" 12
upper device to check
.SH "DESCRIPTION"
Find out if a device is linked to specified upper device and return true
in case it is. Note that this checks the entire upper device chain.
The caller must hold rcu lock.
