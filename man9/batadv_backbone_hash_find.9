.TH "batadv_backbone_hash_find" 9 "batadv_backbone_hash_find" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_backbone_hash_find \- looks for a backbone gateway in the hash
.SH SYNOPSIS
.B "struct batadv_bla_backbone_gw *" batadv_backbone_hash_find
.BI "(struct batadv_priv *bat_priv "  ","
.BI "u8 *addr "  ","
.BI "unsigned short vid "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "addr" 12
the address of the originator
.IP "vid" 12
the VLAN ID
.SH "RETURN"
backbone gateway if found or NULL otherwise
