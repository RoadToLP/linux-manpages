.TH "drm_vma_offset_manager_destroy" 9 "drm_vma_offset_manager_destroy" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
drm_vma_offset_manager_destroy \- Destroy offset manager
.SH SYNOPSIS
.B "void" drm_vma_offset_manager_destroy
.BI "(struct drm_vma_offset_manager *mgr "  ");"
.SH ARGUMENTS
.IP "mgr" 12
Manager object
.SH "DESCRIPTION"
Destroy an object manager which was previously created via
\fBdrm_vma_offset_manager_init\fP. The caller must remove all allocated nodes
before destroying the manager. Otherwise, drm_mm will refuse to free the
requested resources.

The manager must not be accessed after this function is called.
