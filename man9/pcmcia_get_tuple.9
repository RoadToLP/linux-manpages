.TH "pcmcia_get_tuple" 9 "pcmcia_get_tuple" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pcmcia_get_tuple \- get first tuple from CIS
.SH SYNOPSIS
.B "size_t" pcmcia_get_tuple
.BI "(struct pcmcia_device *p_dev "  ","
.BI "cisdata_t code "  ","
.BI "unsigned char **buf "  ");"
.SH ARGUMENTS
.IP "p_dev" 12
the struct pcmcia_device which we need to loop for.
.IP "code" 12
which CIS code shall we look for?
.IP "buf" 12
pointer to store the buffer to.
.SH "DESCRIPTION"
\fBpcmcia_get_tuple\fP gets the content of the first CIS entry of type \fIcode\fP.
It returns the buffer length (or zero). The caller is responsible to free
the buffer passed in \fIbuf\fP.
