.TH "ecryptfs_writepage" 9 "ecryptfs_writepage" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ecryptfs_writepage \- 
.SH SYNOPSIS
.B "int" ecryptfs_writepage
.BI "(struct page *page "  ","
.BI "struct writeback_control *wbc "  ");"
.SH ARGUMENTS
.IP "page" 12
Page that is locked before this call is made
.IP "wbc" 12
-- undescribed --
.SH "DESCRIPTION"
Returns zero on success; non-zero otherwise

This is where we encrypt the data and pass the encrypted data to
the lower filesystem.  In OpenPGP-compatible mode, we operate on
entire underlying packets.
