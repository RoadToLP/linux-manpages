.TH "snd_ctl_enum_info" 9 "snd_ctl_enum_info" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_ctl_enum_info \- fills the info structure for an enumerated control
.SH SYNOPSIS
.B "int" snd_ctl_enum_info
.BI "(struct snd_ctl_elem_info *info "  ","
.BI "unsigned int channels "  ","
.BI "unsigned int items "  ","
.BI "const char *const names[] "  ");"
.SH ARGUMENTS
.IP "info" 12
the structure to be filled
.IP "channels" 12
the number of the control's channels; often one
.IP "items" 12
the number of control values; also the size of \fInames\fP
.IP "names" 12
an array containing the names of all control values
.SH "DESCRIPTION"
Sets all required fields in \fIinfo\fP to their appropriate values.
If the control's accessibility is not the default (readable and writable),
the caller has to fill \fIinfo->access\fP.
.SH "RETURN"
Zero.
