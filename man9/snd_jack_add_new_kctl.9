.TH "snd_jack_add_new_kctl" 9 "snd_jack_add_new_kctl" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_jack_add_new_kctl \- Create a new snd_jack_kctl and add it to jack
.SH SYNOPSIS
.B "int" snd_jack_add_new_kctl
.BI "(struct snd_jack *jack "  ","
.BI "const char * name "  ","
.BI "int mask "  ");"
.SH ARGUMENTS
.IP "jack" 12
the jack instance which the kctl will attaching to
.IP "name" 12
the name for the snd_kcontrol object
.IP "mask" 12
a bitmask of enum snd_jack_type values that can be detected
by this snd_jack_kctl object.
.SH "DESCRIPTION"
Creates a new snd_kcontrol object and adds it to the jack kctl_list.
.SH "RETURN"
Zero if successful, or a negative error code on failure.
