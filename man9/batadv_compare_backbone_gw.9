.TH "batadv_compare_backbone_gw" 9 "batadv_compare_backbone_gw" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_compare_backbone_gw \- compare address and vid of two backbone gws
.SH SYNOPSIS
.B "bool" batadv_compare_backbone_gw
.BI "(const struct hlist_node *node "  ","
.BI "const void *data2 "  ");"
.SH ARGUMENTS
.IP "node" 12
list node of the first entry to compare
.IP "data2" 12
pointer to the second backbone gateway
.SH "RETURN"
true if the backbones have the same data, false otherwise
