.TH "cdns_uart_set_baud_rate" 9 "cdns_uart_set_baud_rate" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
cdns_uart_set_baud_rate \- Calculate and set the baud rate
.SH SYNOPSIS
.B "unsigned int" cdns_uart_set_baud_rate
.BI "(struct uart_port *port "  ","
.BI "unsigned int baud "  ");"
.SH ARGUMENTS
.IP "port" 12
Handle to the uart port structure
.IP "baud" 12
Baud rate to set
.SH "RETURN"
baud rate, requested baud when possible, or actual baud when there
was too much error, zero if no valid divisors are found.
