.TH "xvip_pipeline_start_stop" 9 "xvip_pipeline_start_stop" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
xvip_pipeline_start_stop \- Start ot stop streaming on a pipeline
.SH SYNOPSIS
.B "int" xvip_pipeline_start_stop
.BI "(struct xvip_pipeline *pipe "  ","
.BI "bool start "  ");"
.SH ARGUMENTS
.IP "pipe" 12
The pipeline
.IP "start" 12
Start (when true) or stop (when false) the pipeline
.SH "DESCRIPTION"
Walk the entities chain starting at the pipeline output video node and start
or stop all of them.
.SH "RETURN"
0 if successful, or the return value of the failed video::s_stream
operation otherwise.
