.TH "snd_card_free_when_closed" 9 "snd_card_free_when_closed" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_card_free_when_closed \- Disconnect the card, free it later eventually
.SH SYNOPSIS
.B "int" snd_card_free_when_closed
.BI "(struct snd_card *card "  ");"
.SH ARGUMENTS
.IP "card" 12
soundcard structure
.SH "DESCRIPTION"
Unlike \fBsnd_card_free\fP, this function doesn't try to release the card
resource immediately, but tries to disconnect at first.  When the card
is still in use, the function returns before freeing the resources.
The card resources will be freed when the refcount gets to zero.
