.TH "omap_iommu_domain_activate" 9 "omap_iommu_domain_activate" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_iommu_domain_activate \- activate attached iommu devices
.SH SYNOPSIS
.B "int" omap_iommu_domain_activate
.BI "(struct iommu_domain *domain "  ");"
.SH ARGUMENTS
.IP "domain" 12
iommu domain attached to the target iommu device
.SH "DESCRIPTION"
This API allows the client devices of IOMMU devices to resume the
IOMMUs they control at runtime, before they can resume operations.
System Resume will leverage the PM driver late callbacks.
