.TH "initio_init" 9 "initio_init" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
initio_init \- set up an InitIO host adapter
.SH SYNOPSIS
.B "void" initio_init
.BI "(struct initio_host * host "  ","
.BI "u8 *bios_addr "  ");"
.SH ARGUMENTS
.IP "host" 12
InitIO host adapter
.IP "bios_addr" 12
BIOS address

Set up the host adapter and devices according to the configuration
retrieved from the E2PROM.

Locking: Calls E2PROM layer code which is not re-enterable so must
run single threaded for now.
