.TH "bio_list_copy_data" 9 "bio_list_copy_data" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
bio_list_copy_data \- copy contents of data buffers from one chain of bios to another
.SH SYNOPSIS
.B "void" bio_list_copy_data
.BI "(struct bio *dst "  ","
.BI "struct bio *src "  ");"
.SH ARGUMENTS
.IP "dst" 12
destination bio list
.IP "src" 12
source bio list
.SH "DESCRIPTION"
Stops when it reaches the end of either the \fIsrc\fP list or \fIdst\fP list - that is,
copies min(src->bi_size, dst->bi_size) bytes (or the equivalent for lists of
bios).
