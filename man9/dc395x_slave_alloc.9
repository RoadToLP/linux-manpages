.TH "dc395x_slave_alloc" 9 "dc395x_slave_alloc" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dc395x_slave_alloc \- Called by the scsi mid layer to tell us about a new scsi device that we need to deal with. We allocate a new device and then insert that device into the adapters device list.
.SH SYNOPSIS
.B "int" dc395x_slave_alloc
.BI "(struct scsi_device *scsi_device "  ");"
.SH ARGUMENTS
.IP "scsi_device" 12
The new scsi device that we need to handle.
