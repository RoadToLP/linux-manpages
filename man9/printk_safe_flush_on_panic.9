.TH "printk_safe_flush_on_panic" 9 "printk_safe_flush_on_panic" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
printk_safe_flush_on_panic \- flush all per-cpu nmi buffers when the system goes down.
.SH SYNOPSIS
.B "void" printk_safe_flush_on_panic
.BI "(void "  ");"
.SH ARGUMENTS
.IP "void" 12
no arguments
.SH "DESCRIPTION"

Similar to \fBprintk_safe_flush\fP but it can be called even in NMI context when
the system goes down. It does the best effort to get NMI messages into
the main ring buffer.

Note that it could try harder when there is only one CPU online.
