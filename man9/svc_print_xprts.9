.TH "svc_print_xprts" 9 "svc_print_xprts" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
svc_print_xprts \- Format the transport list for printing
.SH SYNOPSIS
.B "int" svc_print_xprts
.BI "(char *buf "  ","
.BI "int maxlen "  ");"
.SH ARGUMENTS
.IP "buf" 12
target buffer for formatted address
.IP "maxlen" 12
length of target buffer
.SH "DESCRIPTION"
Fills in \fIbuf\fP with a string containing a list of transport names, each name
terminated with '\n'. If the buffer is too small, some entries may be
missing, but it is guaranteed that all lines in the output buffer are
complete.

Returns positive length of the filled-in string.
