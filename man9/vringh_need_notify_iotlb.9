.TH "vringh_need_notify_iotlb" 9 "vringh_need_notify_iotlb" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vringh_need_notify_iotlb \- must we tell the other side about used buffers?
.SH SYNOPSIS
.B "int" vringh_need_notify_iotlb
.BI "(struct vringh *vrh "  ");"
.SH ARGUMENTS
.IP "vrh" 12
the vring we've called \fBvringh_complete_iotlb\fP on.
.SH "DESCRIPTION"
Returns -errno or 0 if we don't need to tell the other side, 1 if we do.
