.TH "snd_ctl_remove_user_ctl" 9 "snd_ctl_remove_user_ctl" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_ctl_remove_user_ctl \- remove and release the unlocked user control
.SH SYNOPSIS
.B "int" snd_ctl_remove_user_ctl
.BI "(struct snd_ctl_file * file "  ","
.BI "struct snd_ctl_elem_id *id "  ");"
.SH ARGUMENTS
.IP "file" 12
active control handle
.IP "id" 12
the control id to remove
.SH "DESCRIPTION"
Finds the control instance with the given id, removes it from the
card list and releases it.
.SH "RETURN"
0 if successful, or a negative error code on failure.
