.TH "l3mdev_link_scope_lookup" 9 "l3mdev_link_scope_lookup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
l3mdev_link_scope_lookup \- IPv6 route lookup based on flow for link local and multicast addresses
.SH SYNOPSIS
.B "struct dst_entry *" l3mdev_link_scope_lookup
.BI "(struct net *net "  ","
.BI "struct flowi6 *fl6 "  ");"
.SH ARGUMENTS
.IP "net" 12
network namespace for device index lookup
.IP "fl6" 12
IPv6 flow struct for lookup
This function does not hold refcnt on the returned dst.
Caller must hold \fBrcu_read_lock\fP.
