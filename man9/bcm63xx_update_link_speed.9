.TH "bcm63xx_update_link_speed" 9 "bcm63xx_update_link_speed" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
bcm63xx_update_link_speed \- Check to see if the link speed has changed.
.SH SYNOPSIS
.B "int" bcm63xx_update_link_speed
.BI "(struct bcm63xx_udc *udc "  ");"
.SH ARGUMENTS
.IP "udc" 12
Reference to the device controller.
.SH "DESCRIPTION"
The link speed update coincides with a SETUP IRQ.  Returns 1 if the
speed has changed, so that the caller can update the endpoint settings.
