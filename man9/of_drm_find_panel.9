.TH "of_drm_find_panel" 9 "of_drm_find_panel" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
of_drm_find_panel \- look up a panel using a device tree node
.SH SYNOPSIS
.B "struct drm_panel *" of_drm_find_panel
.BI "(const struct device_node *np "  ");"
.SH ARGUMENTS
.IP "np" 12
device tree node of the panel
.SH "DESCRIPTION"
Searches the set of registered panels for one that matches the given device
tree node. If a matching panel is found, return a pointer to it.

Possible error codes returned by this function:

- EPROBE_DEFER: the panel device has not been probed yet, and the caller
should retry later
- ENODEV: the device is not available (status != "okay" or "ok")
.SH "RETURN"
A pointer to the panel registered for the specified device tree
node or an \fBERR_PTR\fP if no panel matching the device tree node can be found.
