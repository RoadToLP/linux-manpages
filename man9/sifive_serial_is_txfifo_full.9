.TH "sifive_serial_is_txfifo_full" 9 "sifive_serial_is_txfifo_full" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
sifive_serial_is_txfifo_full \- is the TXFIFO full?
.SH SYNOPSIS
.B "int" sifive_serial_is_txfifo_full
.BI "(struct sifive_serial_port *ssp "  ");"
.SH ARGUMENTS
.IP "ssp" 12
pointer to a struct sifive_serial_port
.SH "DESCRIPTION"
Read the transmit FIFO "full" bit, returning a non-zero value if the
TX FIFO is full, or zero if space remains.  Intended to be used to prevent
writes to the TX FIFO when it's full.
.SH "RETURN"
SIFIVE_SERIAL_TXDATA_FULL_MASK (non-zero) if the transmit FIFO
is full, or 0 if space remains.
