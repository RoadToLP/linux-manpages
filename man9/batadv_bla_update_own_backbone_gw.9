.TH "batadv_bla_update_own_backbone_gw" 9 "batadv_bla_update_own_backbone_gw" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_bla_update_own_backbone_gw \- updates the own backbone gw for a VLAN
.SH SYNOPSIS
.B "void" batadv_bla_update_own_backbone_gw
.BI "(struct batadv_priv *bat_priv "  ","
.BI "struct batadv_hard_iface *primary_if "  ","
.BI "unsigned short vid "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "primary_if" 12
the selected primary interface
.IP "vid" 12
VLAN identifier
.SH "DESCRIPTION"
update or add the own backbone gw to make sure we announce
where we receive other backbone gws
