.TH "drm_gem_dmabuf_vunmap" 9 "drm_gem_dmabuf_vunmap" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
drm_gem_dmabuf_vunmap \- dma_buf vunmap implementation for GEM
.SH SYNOPSIS
.B "void" drm_gem_dmabuf_vunmap
.BI "(struct dma_buf *dma_buf "  ","
.BI "struct dma_buf_map *map "  ");"
.SH ARGUMENTS
.IP "dma_buf" 12
buffer to be unmapped
.IP "map" 12
the virtual address of the buffer
.SH "DESCRIPTION"
Releases a kernel virtual mapping. This can be used as the
\fIdma_buf_ops.vunmap\fP callback. Calls into \fIdrm_gem_object_funcs.vunmap\fP for device specific handling.
