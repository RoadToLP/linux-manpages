.TH "ice_clear_dflt_vsi" 9 "ice_clear_dflt_vsi" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_clear_dflt_vsi \- clear the default forwarding VSI
.SH SYNOPSIS
.B "int" ice_clear_dflt_vsi
.BI "(struct ice_sw *sw "  ");"
.SH ARGUMENTS
.IP "sw" 12
switch used to clear the default VSI
.SH "DESCRIPTION"
If the switch has no default VSI or it's not enabled then return error.

Otherwise try to clear the default VSI and return the result.
