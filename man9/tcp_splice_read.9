.TH "tcp_splice_read" 9 "tcp_splice_read" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tcp_splice_read \- splice data from TCP socket to a pipe
.SH SYNOPSIS
.B "ssize_t" tcp_splice_read
.BI "(struct socket *sock "  ","
.BI "loff_t *ppos "  ","
.BI "struct pipe_inode_info *pipe "  ","
.BI "size_t len "  ","
.BI "unsigned int flags "  ");"
.SH ARGUMENTS
.IP "sock" 12
socket to splice from
.IP "ppos" 12
position (not valid)
.IP "pipe" 12
pipe to splice to
.IP "len" 12
number of bytes to splice
.IP "flags" 12
splice modifier flags
.SH "DESCRIPTION"
Will read pages from given socket and fill them into a pipe.
