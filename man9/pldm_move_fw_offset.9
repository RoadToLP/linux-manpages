.TH "pldm_move_fw_offset" 9 "pldm_move_fw_offset" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pldm_move_fw_offset \- Move the current firmware offset forward
.SH SYNOPSIS
.B "int" pldm_move_fw_offset
.BI "(struct pldmfw_priv *data "  ","
.BI "size_t bytes_to_move "  ");"
.SH ARGUMENTS
.IP "data" 12
pointer to private data
.IP "bytes_to_move" 12
number of bytes to move the offset forward by
.SH "DESCRIPTION"
Check that there is enough space past the current offset, and then move the
offset forward by this ammount.
.SH "RETURN"
zero on success, or -EFAULT if the image is too small to fit the
expected length.
