.TH "batadv_mcast_is_report_ipv4" 9 "batadv_mcast_is_report_ipv4" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_mcast_is_report_ipv4 \-  check for IGMP reports
.SH SYNOPSIS
.B "bool" batadv_mcast_is_report_ipv4
.BI "(struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "skb" 12
the ethernet frame destined for the mesh
.SH "DESCRIPTION"
This call might reallocate skb data.

Checks whether the given frame is a valid IGMP report.
.SH "RETURN"
If so then true, otherwise false.
