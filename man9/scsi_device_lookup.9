.TH "scsi_device_lookup" 9 "scsi_device_lookup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
scsi_device_lookup \- find a device given the host
.SH SYNOPSIS
.B "struct scsi_device *" scsi_device_lookup
.BI "(struct Scsi_Host *shost "  ","
.BI "uint channel "  ","
.BI "uint id "  ","
.BI "u64 lun "  ");"
.SH ARGUMENTS
.IP "shost" 12
SCSI host pointer
.IP "channel" 12
SCSI channel (zero if only one channel)
.IP "id" 12
SCSI target number (physical unit number)
.IP "lun" 12
SCSI Logical Unit Number
.SH "DESCRIPTION"
Looks up the scsi_device with the specified \fIchannel\fP, \fIid\fP, \fIlun\fP
for a given host.  The returned scsi_device has an additional reference that
needs to be released with scsi_device_put once you're done with it.
