.TH "ti_clk_is_in_standby" 9 "ti_clk_is_in_standby" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ti_clk_is_in_standby \- Check if clkctrl clock is in standby or not
.SH SYNOPSIS
.B "bool" ti_clk_is_in_standby
.BI "(struct clk *clk "  ");"
.SH ARGUMENTS
.IP "clk" 12
clock to check standby status for
.SH "DESCRIPTION"
Finds whether the provided clock is in standby mode or not. Returns
true if the provided clock is a clkctrl type clock and it is in standby,
false otherwise.
