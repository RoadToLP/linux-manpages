.TH "ice_ptg_find_ptype" 9 "ice_ptg_find_ptype" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_ptg_find_ptype \- Search for packet type group using packet type (ptype)
.SH SYNOPSIS
.B "enum ice_status" ice_ptg_find_ptype
.BI "(struct ice_hw *hw "  ","
.BI "enum ice_block blk "  ","
.BI "u16 ptype "  ","
.BI "u8 *ptg "  ");"
.SH ARGUMENTS
.IP "hw" 12
pointer to the hardware structure
.IP "blk" 12
HW block
.IP "ptype" 12
the ptype to search for
.IP "ptg" 12
pointer to variable that receives the PTG
.SH "DESCRIPTION"
This function will search the PTGs for a particular ptype, returning the
PTG ID that contains it through the PTG parameter, with the value of
ICE_DEFAULT_PTG (0) meaning it is part the default PTG.
