.TH "batadv_mcast_forw_rtr_count" 9 "batadv_mcast_forw_rtr_count" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_mcast_forw_rtr_count \- count nodes with a multicast router
.SH SYNOPSIS
.B "int" batadv_mcast_forw_rtr_count
.BI "(struct batadv_priv *bat_priv "  ","
.BI "int protocol "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "protocol" 12
the ethernet protocol type to count multicast routers for
.SH "RETURN"
the number of nodes which want all routable IPv4 multicast traffic
if the protocol is ETH_P_IP or the number of nodes which want all routable
IPv6 traffic if the protocol is ETH_P_IPV6. Otherwise returns 0.
