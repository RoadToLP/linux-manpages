.TH "ice_vf_rebuild_vsi_with_release" 9 "ice_vf_rebuild_vsi_with_release" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_vf_rebuild_vsi_with_release \- release and setup the VF's VSI
.SH SYNOPSIS
.B "int" ice_vf_rebuild_vsi_with_release
.BI "(struct ice_vf *vf "  ");"
.SH ARGUMENTS
.IP "vf" 12
VF to release and setup the VSI for
.SH "DESCRIPTION"
This is only called when a single VF is being reset (i.e. VFR, VFLR, host VF
configuration change, etc.).
