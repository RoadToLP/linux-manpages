.TH "nvec_event_mask" 9 "nvec_event_mask" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
nvec_event_mask \- fill the command string with event bitfield ev: points to event command string mask: bit to insert into the event mask
.SH SYNOPSIS
.B "void" nvec_event_mask
.BI "(char *ev "  ","
.BI "u32 mask "  ");"
.SH ARGUMENTS
.IP "ev" 12
-- undescribed --
.IP "mask" 12
-- undescribed --
.SH "DESCRIPTION"

Configure event command expects a 32 bit bitfield which describes
which events to enable. The bitfield has the following structure
(from highest byte to lowest):
system state bits 7-0
system state bits 15-8
oem system state bits 7-0
oem system state bits 15-8
