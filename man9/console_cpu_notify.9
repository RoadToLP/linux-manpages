.TH "console_cpu_notify" 9 "console_cpu_notify" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
console_cpu_notify \- print deferred console messages after CPU hotplug
.SH SYNOPSIS
.B "int" console_cpu_notify
.BI "(unsigned int cpu "  ");"
.SH ARGUMENTS
.IP "cpu" 12
unused
.SH "DESCRIPTION"
If \fBprintk\fP is called from a CPU that is not online yet, the messages
will be printed on the console only if there are CON_ANYTIME consoles.
This function is called when a new CPU comes online (or fails to come
up) or goes offline.
