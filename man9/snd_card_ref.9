.TH "snd_card_ref" 9 "snd_card_ref" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_card_ref \- Get the card object from the index
.SH SYNOPSIS
.B "struct snd_card *" snd_card_ref
.BI "(int idx "  ");"
.SH ARGUMENTS
.IP "idx" 12
the card index
.SH "DESCRIPTION"
Returns a card object corresponding to the given index or NULL if not found.
Release the object via \fBsnd_card_unref\fP.
