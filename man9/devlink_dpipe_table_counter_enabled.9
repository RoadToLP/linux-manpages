.TH "devlink_dpipe_table_counter_enabled" 9 "devlink_dpipe_table_counter_enabled" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
devlink_dpipe_table_counter_enabled \- check if counter allocation required
.SH SYNOPSIS
.B "bool" devlink_dpipe_table_counter_enabled
.BI "(struct devlink *devlink "  ","
.BI "const char *table_name "  ");"
.SH ARGUMENTS
.IP "devlink" 12
devlink
.IP "table_name" 12
tables name

Used by driver to check if counter allocation is required.
After counter allocation is turned on the table entries
are updated to include counter statistics.

After that point on the driver must respect the counter
state so that each entry added to the table is added
with a counter.
