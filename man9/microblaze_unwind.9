.TH "microblaze_unwind" 9 "microblaze_unwind" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
microblaze_unwind \- Stack unwinder for Microblaze (external entry point)
.SH SYNOPSIS
.B "void" microblaze_unwind
.BI "(struct task_struct *task "  ","
.BI "struct stack_trace *trace "  ","
.BI "const char *loglvl "  ");"
.SH ARGUMENTS
.IP "task" 12
Task whose stack we are to unwind (NULL == current)
.IP "trace" 12
Where to store stack backtrace (PC values).
NULL == print backtrace to kernel log
.IP "loglvl" 12
Used for printk log level if (trace == NULL).
