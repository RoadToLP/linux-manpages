.TH "drm_add_override_edid_modes" 9 "drm_add_override_edid_modes" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
drm_add_override_edid_modes \- add modes from override/firmware EDID
.SH SYNOPSIS
.B "int" drm_add_override_edid_modes
.BI "(struct drm_connector *connector "  ");"
.SH ARGUMENTS
.IP "connector" 12
connector we're probing
.SH "DESCRIPTION"
Add modes from the override/firmware EDID, if available. Only to be used from
\fBdrm_helper_probe_single_connector_modes\fP as a fallback for when DDC probe
failed during \fBdrm_get_edid\fP and caused the override/firmware EDID to be
skipped.
.SH "RETURN"
The number of modes added or 0 if we couldn't find any.
