.TH "nfs_umount" 9 "nfs_umount" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
nfs_umount \- Notify a server that we have unmounted this export
.SH SYNOPSIS
.B "void" nfs_umount
.BI "(const struct nfs_mount_request *info "  ");"
.SH ARGUMENTS
.IP "info" 12
pointer to umount request arguments
.SH "DESCRIPTION"
MOUNTPROC_UMNT is advisory, so we set a short timeout, and always
use UDP.
