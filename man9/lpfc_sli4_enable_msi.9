.TH "lpfc_sli4_enable_msi" 9 "lpfc_sli4_enable_msi" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_sli4_enable_msi \- Enable MSI interrupt mode to SLI-4 device
.SH SYNOPSIS
.B "int" lpfc_sli4_enable_msi
.BI "(struct lpfc_hba *phba "  ");"
.SH ARGUMENTS
.IP "phba" 12
pointer to lpfc hba data structure.
.SH "DESCRIPTION"
This routine is invoked to enable the MSI interrupt mode to device with
SLI-4 interface spec. The kernel function \fBpci_alloc_irq_vectors\fP is
called to enable the MSI vector. The device driver is responsible for
calling the \fBrequest_irq\fP to register MSI vector with a interrupt the
handler, which is done in this function.

Return codes
0 - successful
other values - error
