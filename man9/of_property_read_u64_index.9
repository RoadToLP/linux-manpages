.TH "of_property_read_u64_index" 9 "of_property_read_u64_index" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
of_property_read_u64_index \- Find and read a u64 from a multi-value property.
.SH SYNOPSIS
.B "int" of_property_read_u64_index
.BI "(const struct device_node *np "  ","
.BI "const char *propname "  ","
.BI "u32 index "  ","
.BI "u64 *out_value "  ");"
.SH ARGUMENTS
.IP "np" 12
device node from which the property value is to be read.
.IP "propname" 12
name of the property to be searched.
.IP "index" 12
index of the u64 in the list of values
.IP "out_value" 12
pointer to return value, modified only if no error.
.SH "DESCRIPTION"
Search for a property in a device node and read nth 64-bit value from
it. Returns 0 on success, -EINVAL if the property does not exist,
-ENODATA if property does not have a value, and -EOVERFLOW if the
property data isn't large enough.

The out_value is modified only if a valid u64 value can be decoded.
