.TH "dp_link_process_link_status_update" 9 "dp_link_process_link_status_update" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dp_link_process_link_status_update \- processes link status updates
.SH SYNOPSIS
.B "int" dp_link_process_link_status_update
.BI "(struct dp_link_private *link "  ");"
.SH ARGUMENTS
.IP "link" 12
Display Port link module data
.SH "DESCRIPTION"
This function will check for changes in the link status, e.g. clock
recovery done on all lanes, and trigger link training if there is a
failure/error on the link.

The function will return 0 if the a link status update has been processed,
otherwise it will return -EINVAL.
