.TH "devm_hwspin_lock_unregister" 9 "devm_hwspin_lock_unregister" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
devm_hwspin_lock_unregister \- unregister an hw spinlock device for a managed device
.SH SYNOPSIS
.B "int" devm_hwspin_lock_unregister
.BI "(struct device *dev "  ","
.BI "struct hwspinlock_device *bank "  ");"
.SH ARGUMENTS
.IP "dev" 12
the backing device
.IP "bank" 12
the hwspinlock device, which usually provides numerous hw locks
.SH "DESCRIPTION"
This function should be called from the underlying platform-specific
implementation, to unregister an existing (and unused) hwspinlock.

Should be called from a process context (might sleep)

Returns 0 on success, or an appropriate error code on failure
