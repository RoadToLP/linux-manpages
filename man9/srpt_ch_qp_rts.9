.TH "srpt_ch_qp_rts" 9 "srpt_ch_qp_rts" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
srpt_ch_qp_rts \- change the state of a channel to 'ready to send' (RTS)
.SH SYNOPSIS
.B "int" srpt_ch_qp_rts
.BI "(struct srpt_rdma_ch *ch "  ","
.BI "struct ib_qp *qp "  ");"
.SH ARGUMENTS
.IP "ch" 12
channel of the queue pair.
.IP "qp" 12
queue pair to change the state of.
.SH "DESCRIPTION"
Returns zero upon success and a negative value upon failure.
.SH "NOTE"
currently a struct ib_qp_attr takes 136 bytes on a 64-bit system.
If this structure ever becomes larger, it might be necessary to allocate
it dynamically instead of on the stack.
