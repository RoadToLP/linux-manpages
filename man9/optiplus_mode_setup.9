.TH "optiplus_mode_setup" 9 "optiplus_mode_setup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
optiplus_mode_setup \- DMA setup for Firestar Plus
.SH SYNOPSIS
.B "void" optiplus_mode_setup
.BI "(struct ata_port *ap "  ","
.BI "struct ata_device *adev "  ","
.BI "u8 mode "  ");"
.SH ARGUMENTS
.IP "ap" 12
ATA port
.IP "adev" 12
device
.IP "mode" 12
desired mode

The Firestar plus has additional UDMA functionality for UDMA0-2 and
requires we do some additional work. Because the base work we must do
is mostly shared we wrap the Firestar setup functionality in this
one
