.TH "vio_cmo_bus_probe" 9 "vio_cmo_bus_probe" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vio_cmo_bus_probe \- Handle CMO specific bus probe activities
.SH SYNOPSIS
.B "int" vio_cmo_bus_probe
.BI "(struct vio_dev *viodev "  ");"
.SH ARGUMENTS
.IP "viodev" 12
-- undescribed --
.SH "DESCRIPTION"

\fIviodev\fP - Pointer to struct vio_dev for device

Determine the devices IO memory entitlement needs, attempting
to satisfy the system minimum entitlement at first and scheduling
a balance operation to take care of the rest at a later time.
.SH "RETURN"
0 on success, -EINVAL when device doesn't support CMO, and
-ENOMEM when entitlement is not available for device or
device entry.
