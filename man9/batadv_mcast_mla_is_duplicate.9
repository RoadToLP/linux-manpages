.TH "batadv_mcast_mla_is_duplicate" 9 "batadv_mcast_mla_is_duplicate" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_mcast_mla_is_duplicate \- check whether an address is in a list
.SH SYNOPSIS
.B "bool" batadv_mcast_mla_is_duplicate
.BI "(u8 *mcast_addr "  ","
.BI "struct hlist_head *mcast_list "  ");"
.SH ARGUMENTS
.IP "mcast_addr" 12
the multicast address to check
.IP "mcast_list" 12
the list with multicast addresses to search in
.SH "RETURN"
true if the given address is already in the given list.
Otherwise returns false.
