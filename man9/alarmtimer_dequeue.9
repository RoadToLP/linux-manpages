.TH "alarmtimer_dequeue" 9 "alarmtimer_dequeue" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
alarmtimer_dequeue \- Removes an alarm timer from an alarm_base timerqueue
.SH SYNOPSIS
.B "void" alarmtimer_dequeue
.BI "(struct alarm_base *base "  ","
.BI "struct alarm *alarm "  ");"
.SH ARGUMENTS
.IP "base" 12
pointer to the base where the timer is running
.IP "alarm" 12
pointer to alarm being removed
.SH "DESCRIPTION"
Removes alarm to a alarm_base timerqueue

Must hold base->lock when calling.
