.TH "ixgbe_x550em_a_has_mii" 9 "ixgbe_x550em_a_has_mii" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ixgbe_x550em_a_has_mii \- is this the first ixgbe x550em_a PCI function?
.SH SYNOPSIS
.B "bool" ixgbe_x550em_a_has_mii
.BI "(struct ixgbe_hw *hw "  ");"
.SH ARGUMENTS
.IP "hw" 12
pointer to hardware structure
.SH "DESCRIPTION"
Returns true if hw points to lowest numbered PCI B:D.F x550_em_a device in
the SoC.  There are up to 4 MACs sharing a single MDIO bus on the x550em_a,
but we only want to register one MDIO bus.
