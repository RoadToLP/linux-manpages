.TH "via_reinit_one" 9 "via_reinit_one" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
via_reinit_one \- reinit after resume @pdev; PCI device
.SH SYNOPSIS
.B "int" via_reinit_one
.BI "(struct pci_dev *pdev "  ");"
.SH ARGUMENTS
.IP "pdev" 12
-- undescribed --
.SH "DESCRIPTION"

Called when the VIA PATA device is resumed. We must then
reconfigure the fifo and other setup we may have altered. In
addition the kernel needs to have the resume methods on PCI
quirk supported.
