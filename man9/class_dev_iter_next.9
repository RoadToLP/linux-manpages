.TH "class_dev_iter_next" 9 "class_dev_iter_next" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
class_dev_iter_next \- iterate to the next device
.SH SYNOPSIS
.B "struct device *" class_dev_iter_next
.BI "(struct class_dev_iter *iter "  ");"
.SH ARGUMENTS
.IP "iter" 12
class iterator to proceed
.SH "DESCRIPTION"
Proceed \fIiter\fP to the next device and return it.  Returns NULL if
iteration is complete.

The returned device is referenced and won't be released till
iterator is proceed to the next device or exited.  The caller is
free to do whatever it wants to do with the device including
calling back into class code.
