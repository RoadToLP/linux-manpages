.TH "lpfc_sli_handle_slow_ring_event_s3" 9 "lpfc_sli_handle_slow_ring_event_s3" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
lpfc_sli_handle_slow_ring_event_s3 \- Handle SLI3 ring event for non-FCP rings
.SH SYNOPSIS
.B "void" lpfc_sli_handle_slow_ring_event_s3
.BI "(struct lpfc_hba *phba "  ","
.BI "struct lpfc_sli_ring *pring "  ","
.BI "uint32_t mask "  ");"
.SH ARGUMENTS
.IP "phba" 12
Pointer to HBA context object.
.IP "pring" 12
Pointer to driver SLI ring object.
.IP "mask" 12
Host attention register mask for this ring.
.SH "DESCRIPTION"
This function is called from the worker thread when there is a ring event
for non-fcp rings. The caller does not hold any lock. The function will
remove each response iocb in the response ring and calls the handle
response iocb routine (lpfc_sli_sp_handle_rspiocb) to process it.
