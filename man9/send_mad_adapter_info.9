.TH "send_mad_adapter_info" 9 "send_mad_adapter_info" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
send_mad_adapter_info \- - Sends the mad adapter info request and stores the result so it can be retrieved with sysfs. We COULD consider causing a failure if the returned SRP version doesn't match ours.
.SH SYNOPSIS
.B "void" send_mad_adapter_info
.BI "(struct ibmvscsi_host_data *hostdata "  ");"
.SH ARGUMENTS
.IP "hostdata" 12
ibmvscsi_host_data of host
.SH "DESCRIPTION"
Returns zero if successful.
