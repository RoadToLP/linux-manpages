.TH "usb_ep_autoconfig_reset" 9 "usb_ep_autoconfig_reset" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
usb_ep_autoconfig_reset \- reset endpoint autoconfig state
.SH SYNOPSIS
.B "void" usb_ep_autoconfig_reset
.BI "(struct usb_gadget *gadget "  ");"
.SH ARGUMENTS
.IP "gadget" 12
device for which autoconfig state will be reset
.SH "DESCRIPTION"
Use this for devices where one configuration may need to assign
endpoint resources very differently from the next one.  It clears
state such as ep->claimed and the record of assigned endpoints
used by \fBusb_ep_autoconfig\fP.
