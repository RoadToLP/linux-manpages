.TH "pwrdm_read_pwrst" 9 "pwrdm_read_pwrst" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pwrdm_read_pwrst \- get current powerdomain power state
.SH SYNOPSIS
.B "int" pwrdm_read_pwrst
.BI "(struct powerdomain *pwrdm "  ");"
.SH ARGUMENTS
.IP "pwrdm" 12
struct powerdomain * to get power state
.SH "DESCRIPTION"
Return the powerdomain \fIpwrdm\fP's current power state. Returns -EINVAL
if the powerdomain pointer is null or returns the current power state
upon success. Note that if the power domain only supports the ON state
then just return ON as the current state.
