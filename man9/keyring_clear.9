.TH "keyring_clear" 9 "keyring_clear" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
keyring_clear \- Clear a keyring
.SH SYNOPSIS
.B "int" keyring_clear
.BI "(struct key *keyring "  ");"
.SH ARGUMENTS
.IP "keyring" 12
The keyring to clear.
.SH "DESCRIPTION"
Clear the contents of the specified keyring.

Returns 0 if successful or -ENOTDIR if the keyring isn't a keyring.
