.TH "write_eeprom" 9 "write_eeprom" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
write_eeprom \- actually writes the relevant part of the data value.
.SH SYNOPSIS
.B "int" write_eeprom
.BI "(struct s2io_nic *sp "  ","
.BI "int off "  ","
.BI "u64 data "  ","
.BI "int cnt "  ");"
.SH ARGUMENTS
.IP "sp" 12
private member of the device structure, which is a pointer to the
s2io_nic structure.
.IP "off" 12
offset at which the data must be written
.IP "data" 12
The data that is to be written
.IP "cnt" 12
Number of bytes of the data that are actually to be written into
the Eeprom. (max of 3)
.SH "DESCRIPTION"
Actually writes the relevant part of the data value into the Eeprom
through the I2C bus.
Return value:
0 on success, -1 on failure.
