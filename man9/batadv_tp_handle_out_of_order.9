.TH "batadv_tp_handle_out_of_order" 9 "batadv_tp_handle_out_of_order" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_tp_handle_out_of_order \- store an out of order packet
.SH SYNOPSIS
.B "bool" batadv_tp_handle_out_of_order
.BI "(struct batadv_tp_vars *tp_vars "  ","
.BI "const struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "tp_vars" 12
the private data of the current TP meter session
.IP "skb" 12
the buffer containing the received packet
.SH "DESCRIPTION"
Store the out of order packet in the unacked list for late processing. This
packets are kept in this list so that they can be ACKed at once as soon as
all the previous packets have been received
.SH "RETURN"
true if the packed has been successfully processed, false otherwise
