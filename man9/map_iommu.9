.TH "map_iommu" 9 "map_iommu" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
map_iommu \- map the iommu's registers
.SH SYNOPSIS
.B "int" map_iommu
.BI "(struct intel_iommu *iommu "  ","
.BI "u64 phys_addr "  ");"
.SH ARGUMENTS
.IP "iommu" 12
the iommu to map
.IP "phys_addr" 12
the physical address of the base resgister
.SH "DESCRIPTION"
Memory map the iommu's registers.  Start w/ a single page, and
possibly expand if that turns out to be insufficent.
