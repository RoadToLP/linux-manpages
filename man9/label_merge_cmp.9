.TH "label_merge_cmp" 9 "label_merge_cmp" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
label_merge_cmp \- cmp of @a merging with @b against @z for set ordering
.SH SYNOPSIS
.B "int" label_merge_cmp
.BI "(struct aa_label *a "  ","
.BI "struct aa_label *b "  ","
.BI "struct aa_label *z "  ");"
.SH ARGUMENTS
.IP "a" 12
label to merge then compare (NOT NULL)
.IP "b" 12
label to merge then compare (NOT NULL)
.IP "z" 12
label to compare merge against (NOT NULL)
.SH "DESCRIPTION"
Assumes: using the most recent versions of \fIa\fP, \fIb\fP, and \fIz\fP
.SH "RETURN"
<0  if a < b
==0 if a == b
>0  if a > b
