.TH "ice_cfg_txq_interrupt" 9 "ice_cfg_txq_interrupt" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_cfg_txq_interrupt \- configure interrupt on Tx queue
.SH SYNOPSIS
.B "void" ice_cfg_txq_interrupt
.BI "(struct ice_vsi *vsi "  ","
.BI "u16 txq "  ","
.BI "u16 msix_idx "  ","
.BI "u16 itr_idx "  ");"
.SH ARGUMENTS
.IP "vsi" 12
the VSI being configured
.IP "txq" 12
Tx queue being mapped to MSI-X vector
.IP "msix_idx" 12
MSI-X vector index within the function
.IP "itr_idx" 12
ITR index of the interrupt cause
.SH "DESCRIPTION"
Configure interrupt on Tx queue by associating Tx queue to MSI-X vector
within the function space.
