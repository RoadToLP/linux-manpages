.TH "unwinder_register" 9 "unwinder_register" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
unwinder_register \- Used to install new stack unwinder
.SH SYNOPSIS
.B "int" unwinder_register
.BI "(struct unwinder *u "  ");"
.SH ARGUMENTS
.IP "u" 12
unwinder to be registered
.SH "DESCRIPTION"
Install the new stack unwinder on the unwinder list, which is sorted
by rating.

Returns -EBUSY if registration fails, zero otherwise.
