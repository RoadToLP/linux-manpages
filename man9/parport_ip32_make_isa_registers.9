.TH "parport_ip32_make_isa_registers" 9 "parport_ip32_make_isa_registers" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
parport_ip32_make_isa_registers \- compute (ISA) register addresses
.SH SYNOPSIS
.B "void" parport_ip32_make_isa_registers
.BI "(struct parport_ip32_regs *regs "  ","
.BI "void __iomem *base "  ","
.BI "void __iomem *base_hi "  ","
.BI "unsigned int regshift "  ");"
.SH ARGUMENTS
.IP "regs" 12
pointer to \fIstruct parport_ip32_regs\fP to fill
.IP "base" 12
base address of standard and EPP registers
.IP "base_hi" 12
base address of ECP registers
.IP "regshift" 12
how much to shift register offset by
.SH "DESCRIPTION"
Compute register addresses, according to the ISA standard.  The addresses
of the standard and EPP registers are computed from address \fIbase\fP.  The
addresses of the ECP registers are computed from address \fIbase_hi\fP.
