.TH "vringh_need_notify_kern" 9 "vringh_need_notify_kern" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vringh_need_notify_kern \- must we tell the other side about used buffers?
.SH SYNOPSIS
.B "int" vringh_need_notify_kern
.BI "(struct vringh *vrh "  ");"
.SH ARGUMENTS
.IP "vrh" 12
the vring we've called \fBvringh_complete_kern\fP on.
.SH "DESCRIPTION"
Returns -errno or 0 if we don't need to tell the other side, 1 if we do.
