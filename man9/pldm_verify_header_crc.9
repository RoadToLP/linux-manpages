.TH "pldm_verify_header_crc" 9 "pldm_verify_header_crc" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
pldm_verify_header_crc \- Verify that the CRC in the header matches
.SH SYNOPSIS
.B "int" pldm_verify_header_crc
.BI "(struct pldmfw_priv *data "  ");"
.SH ARGUMENTS
.IP "data" 12
pointer to private data
.SH "DESCRIPTION"
Calculates the 32-bit CRC using the standard IEEE 802.3 CRC polynomial and
compares it to the value stored in the header.
.SH "RETURN"
zero on success if the CRC matches, or -EBADMSG on an invalid CRC.
