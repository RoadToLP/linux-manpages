.TH "ftrace_text_reserved" 9 "ftrace_text_reserved" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ftrace_text_reserved \- return true if range contains an ftrace location
.SH SYNOPSIS
.B "int" ftrace_text_reserved
.BI "(const void *start "  ","
.BI "const void *end "  ");"
.SH ARGUMENTS
.IP "start" 12
start of range to search
.IP "end" 12
end of range to search (inclusive). \fIend\fP points to the last byte to check.
.SH "DESCRIPTION"
Returns 1 if \fIstart\fP and \fIend\fP contains a ftrace location.
That is, the instruction that is either a NOP or call to
the function tracer. It checks the ftrace internal tables to
determine if the address belongs or not.
