.TH "dev_or_parent_of_node" 9 "dev_or_parent_of_node" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dev_or_parent_of_node \- Get device node of @dev or @dev's parent
.SH SYNOPSIS
.B "struct device_node *" dev_or_parent_of_node
.BI "(struct device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
Device to get device node of
.SH "RETURN"
device node pointer of \fIdev\fP, or the device node pointer of
\fIdev->parent\fP if dev doesn't have a device node, or NULL if neither
\fIdev\fP or \fIdev->parent\fP have a device node.
