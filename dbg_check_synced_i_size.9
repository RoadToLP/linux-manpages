.TH "dbg_check_synced_i_size" 9 "dbg_check_synced_i_size" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dbg_check_synced_i_size \- check synchronized inode size.
.SH SYNOPSIS
.B "int" dbg_check_synced_i_size
.BI "(const struct ubifs_info *c "  ","
.BI "struct inode *inode "  ");"
.SH ARGUMENTS
.IP "c" 12
UBIFS file-system description object
.IP "inode" 12
inode to check
.SH "DESCRIPTION"
If inode is clean, synchronized inode size has to be equivalent to current
inode size. This function has to be called only for locked inodes (\fIi_mutex\fP
has to be locked). Returns 0 if synchronized inode size if correct, and
-EINVAL if not.
