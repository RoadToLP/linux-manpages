.TH "unassign_adapter_store" 9 "unassign_adapter_store" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
unassign_adapter_store \- 
.SH SYNOPSIS
.B "ssize_t" unassign_adapter_store
.BI "(struct device *dev "  ","
.BI "struct device_attribute *attr "  ","
.BI "const char *buf "  ","
.BI "size_t count "  ");"
.SH ARGUMENTS
.IP "dev" 12
the matrix device
.IP "attr" 12
the mediated matrix device's unassign_adapter attribute
.IP "buf" 12
a buffer containing the adapter number (APID) to be unassigned
.IP "count" 12
the number of bytes in \fIbuf\fP
.SH "DESCRIPTION"
Parses the APID from \fIbuf\fP and clears the corresponding bit in the mediated
matrix device's APM.

Returns the number of bytes processed if the APID is valid; otherwise,
returns one of the following errors:
-EINVAL if the APID is not a number
-ENODEV if the APID it exceeds the maximum value configured for the
system
