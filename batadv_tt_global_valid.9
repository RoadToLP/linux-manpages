.TH "batadv_tt_global_valid" 9 "batadv_tt_global_valid" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_tt_global_valid \- verify global tt entry and get flags
.SH SYNOPSIS
.B "bool" batadv_tt_global_valid
.BI "(const void *entry_ptr "  ","
.BI "const void *data_ptr "  ","
.BI "u8 *flags "  ");"
.SH ARGUMENTS
.IP "entry_ptr" 12
to be checked global tt entry
.IP "data_ptr" 12
an orig_node object (may be NULL)
.IP "flags" 12
a pointer to store TT flags for this client to
.SH "DESCRIPTION"
Checks the validity of the given global TT entry. If it is, then the provided
flags pointer is updated either with the common (summed) TT flags if data_ptr
is NULL or the specific, per originator TT flags otherwise.
.SH "RETURN"
true if the entry is a valid, false otherwise.
