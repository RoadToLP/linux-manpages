.TH "parport_put_port" 9 "parport_put_port" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
parport_put_port \- decrement a port's reference count
.SH SYNOPSIS
.B "void" parport_put_port
.BI "(struct parport *port "  ");"
.SH ARGUMENTS
.IP "port" 12
the port

This should be called once for each call to \fBparport_get_port\fP,
once the port is no longer needed. When the reference count reaches
zero (port is no longer used), free_port is called.
