.TH "acpi_match_device" 9 "acpi_match_device" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_match_device \- Match a struct device against a given list of ACPI IDs
.SH SYNOPSIS
.B "const struct acpi_device_id *" acpi_match_device
.BI "(const struct acpi_device_id *ids "  ","
.BI "const struct device *dev "  ");"
.SH ARGUMENTS
.IP "ids" 12
Array of struct acpi_device_id object to match against.
.IP "dev" 12
The device structure to match.
.SH "DESCRIPTION"
Check if \fIdev\fP has a valid ACPI handle and if there is a struct acpi_device
object for that handle and use that object to match against a given list of
device IDs.

Return a pointer to the first matching ID on success or NULL on failure.
