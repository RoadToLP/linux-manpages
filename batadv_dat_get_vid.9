.TH "batadv_dat_get_vid" 9 "batadv_dat_get_vid" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_dat_get_vid \- extract the VLAN identifier from skb if any
.SH SYNOPSIS
.B "unsigned short" batadv_dat_get_vid
.BI "(struct sk_buff *skb "  ","
.BI "int *hdr_size "  ");"
.SH ARGUMENTS
.IP "skb" 12
the buffer containing the packet to extract the VID from
.IP "hdr_size" 12
the size of the batman-adv header encapsulating the packet
.SH "RETURN"
If the packet embedded in the skb is vlan tagged this function
returns the VID with the BATADV_VLAN_HAS_TAG flag. Otherwise BATADV_NO_FLAGS
is returned.
