.TH "vmw_cmdbuf_ctx_submit" 9 "vmw_cmdbuf_ctx_submit" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vmw_cmdbuf_ctx_submit \- Submit command buffers from a command buffer context.
.SH SYNOPSIS
.B "void" vmw_cmdbuf_ctx_submit
.BI "(struct vmw_cmdbuf_man *man "  ","
.BI "struct vmw_cmdbuf_context *ctx "  ");"
.SH ARGUMENTS
.IP "man" 12
The command buffer manager.
.IP "ctx" 12
The command buffer context.
.SH "DESCRIPTION"
Submits command buffers to hardware until there are no more command
buffers to submit or the hardware can't handle more command buffers.
