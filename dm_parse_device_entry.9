.TH "dm_parse_device_entry" 9 "dm_parse_device_entry" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dm_parse_device_entry \- parse a device entry
.SH SYNOPSIS
.B "char *" dm_parse_device_entry
.BI "(struct dm_device *dev "  ","
.BI "char *str "  ");"
.SH ARGUMENTS
.IP "dev" 12
device to store the parsed information.
.IP "str" 12
the pointer to a string with the format:
name,uuid,minor,flags,table[; ...]
.SH "DESCRIPTION"
Return the remainder string after the table entry, i.e, after the semi-colon
which delimits the entry or NULL if reached the end of the string.
