.TH "i40e_ptp_tx_hwtstamp" 9 "i40e_ptp_tx_hwtstamp" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
i40e_ptp_tx_hwtstamp \- Utility function which returns the Tx timestamp
.SH SYNOPSIS
.B "void" i40e_ptp_tx_hwtstamp
.BI "(struct i40e_pf *pf "  ");"
.SH ARGUMENTS
.IP "pf" 12
Board private structure
.SH "DESCRIPTION"
Read the value of the Tx timestamp from the registers, convert it into a
value consumable by the stack, and store that result into the shhwtstamps
struct before returning it up the stack.
