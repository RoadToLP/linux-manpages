.TH "snd_info_create_card_entry" 9 "snd_info_create_card_entry" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_info_create_card_entry \- create an info entry for the given card
.SH SYNOPSIS
.B "struct snd_info_entry *" snd_info_create_card_entry
.BI "(struct snd_card *card "  ","
.BI "const char *name "  ","
.BI "struct snd_info_entry * parent "  ");"
.SH ARGUMENTS
.IP "card" 12
the card instance
.IP "name" 12
the file name
.IP "parent" 12
the parent directory
.SH "DESCRIPTION"
Creates a new info entry and assigns it to the given card.
.SH "RETURN"
The pointer of the new instance, or NULL on failure.
