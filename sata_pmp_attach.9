.TH "sata_pmp_attach" 9 "sata_pmp_attach" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
sata_pmp_attach \- attach a SATA PMP device
.SH SYNOPSIS
.B "int" sata_pmp_attach
.BI "(struct ata_device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
SATA PMP device to attach

Configure and attach SATA PMP device \fIdev\fP.  This function is
also responsible for allocating and initializing PMP links.

LOCKING:
Kernel thread context (may sleep).
.SH "RETURN"
0 on success, -errno on failure.
