.TH "ixgbe_acquire_msix_vectors" 9 "ixgbe_acquire_msix_vectors" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ixgbe_acquire_msix_vectors \- acquire MSI-X vectors
.SH SYNOPSIS
.B "int" ixgbe_acquire_msix_vectors
.BI "(struct ixgbe_adapter *adapter "  ");"
.SH ARGUMENTS
.IP "adapter" 12
board private structure
.SH "DESCRIPTION"
Attempts to acquire a suitable range of MSI-X vector interrupts. Will
return a negative error code if unable to acquire MSI-X vectors for any
reason.
