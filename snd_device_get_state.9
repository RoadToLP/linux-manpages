.TH "snd_device_get_state" 9 "snd_device_get_state" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_device_get_state \- Get the current state of the given device
.SH SYNOPSIS
.B "int" snd_device_get_state
.BI "(struct snd_card *card "  ","
.BI "void *device_data "  ");"
.SH ARGUMENTS
.IP "card" 12
the card instance
.IP "device_data" 12
the data pointer to release
.SH "DESCRIPTION"
Returns the current state of the given device object.  For the valid
device, either \fISNDRV_DEV_BUILD\fP, \fISNDRV_DEV_REGISTERED\fP or
\fISNDRV_DEV_DISCONNECTED\fP is returned.
Or for a non-existing device, -1 is returned as an error.
