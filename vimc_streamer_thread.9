.TH "vimc_streamer_thread" 9 "vimc_streamer_thread" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vimc_streamer_thread \- Process frames through the pipeline
.SH SYNOPSIS
.B "int" vimc_streamer_thread
.BI "(void *data "  ");"
.SH ARGUMENTS
.IP "data" 12
vimc_stream struct of the current stream
.SH "DESCRIPTION"
From the source to the sink, gets a frame from each subdevice and send to
the next one of the pipeline at a fixed framerate.
.SH "RETURN"
Always zero (created as ``int instead of void`` to comply with
kthread API).
