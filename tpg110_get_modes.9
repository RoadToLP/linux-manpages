.TH "tpg110_get_modes" 9 "tpg110_get_modes" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tpg110_get_modes \- return the appropriate mode
.SH SYNOPSIS
.B "int" tpg110_get_modes
.BI "(struct drm_panel *panel "  ","
.BI "struct drm_connector *connector "  ");"
.SH ARGUMENTS
.IP "panel" 12
the panel to get the mode for
.IP "connector" 12
reference to the central DRM connector control structure
.SH "DESCRIPTION"
This currently does not present a forest of modes, instead it
presents the mode that is configured for the system under use,
and which is detected by reading the registers of the display.
