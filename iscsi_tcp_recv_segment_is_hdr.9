.TH "iscsi_tcp_recv_segment_is_hdr" 9 "iscsi_tcp_recv_segment_is_hdr" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
iscsi_tcp_recv_segment_is_hdr \- tests if we are reading in a header
.SH SYNOPSIS
.B "int" iscsi_tcp_recv_segment_is_hdr
.BI "(struct iscsi_tcp_conn *tcp_conn "  ");"
.SH ARGUMENTS
.IP "tcp_conn" 12
iscsi tcp conn
.SH "DESCRIPTION"
returns non zero if we are currently processing or setup to process
a header.
