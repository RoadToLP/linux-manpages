.TH "ixgbe_ipsec_vf_add_sa" 9 "ixgbe_ipsec_vf_add_sa" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ixgbe_ipsec_vf_add_sa \- translate VF request to SA add
.SH SYNOPSIS
.B "int" ixgbe_ipsec_vf_add_sa
.BI "(struct ixgbe_adapter *adapter "  ","
.BI "u32 *msgbuf "  ","
.BI "u32 vf "  ");"
.SH ARGUMENTS
.IP "adapter" 12
board private structure
.IP "msgbuf" 12
The message buffer
.IP "vf" 12
the VF index
.SH "DESCRIPTION"
Make up a new xs and algorithm info from the data sent by the VF.
We only need to sketch in just enough to set up the HW offload.
Put the resulting offload_handle into the return message to the VF.

Returns 0 or error value
