.TH "shrink_tnc_trees" 9 "shrink_tnc_trees" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
shrink_tnc_trees \- shrink UBIFS TNC trees.
.SH SYNOPSIS
.B "int" shrink_tnc_trees
.BI "(int nr "  ","
.BI "int age "  ","
.BI "int *contention "  ");"
.SH ARGUMENTS
.IP "nr" 12
number of znodes to free
.IP "age" 12
the age of znodes to free
.IP "contention" 12
if any contention, this is set to 1
.SH "DESCRIPTION"
This function walks the list of mounted UBIFS file-systems and frees clean
znodes which are older than \fIage\fP, until at least \fInr\fP znodes are freed.
Returns the number of freed znodes.
