.TH "mei_txe_is_input_ready" 9 "mei_txe_is_input_ready" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
mei_txe_is_input_ready \- check if TXE is ready for receiving data
.SH SYNOPSIS
.B "bool" mei_txe_is_input_ready
.BI "(struct mei_device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
the device structure
.SH "RETURN"
true if INPUT STATUS READY bit is set
