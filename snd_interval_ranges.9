.TH "snd_interval_ranges" 9 "snd_interval_ranges" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_interval_ranges \- refine the interval value from the list of ranges
.SH SYNOPSIS
.B "int" snd_interval_ranges
.BI "(struct snd_interval *i "  ","
.BI "unsigned int count "  ","
.BI "const struct snd_interval *ranges "  ","
.BI "unsigned int mask "  ");"
.SH ARGUMENTS
.IP "i" 12
the interval value to refine
.IP "count" 12
the number of elements in the list of ranges
.IP "ranges" 12
the ranges list
.IP "mask" 12
the bit-mask to evaluate
.SH "DESCRIPTION"
Refines the interval value from the list of ranges.
When mask is non-zero, only the elements corresponding to bit 1 are
evaluated.
.SH "RETURN"
Positive if the value is changed, zero if it's not changed, or a
negative error code.
