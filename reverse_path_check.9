.TH "reverse_path_check" 9 "reverse_path_check" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
reverse_path_check \- The tfile_check_list is list of epitem_head, which have links that are proposed to be newly added. We need to make sure that those added links don't add too many paths such that we will spend all our time waking up eventpoll objects.
.SH SYNOPSIS
.B "int" reverse_path_check
.BI "(void "  ");"
.SH ARGUMENTS
.IP "void" 12
no arguments
.SH "RETURN"
Returns zero if the proposed links don't create too many paths,
-1 otherwise.
