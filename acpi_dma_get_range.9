.TH "acpi_dma_get_range" 9 "acpi_dma_get_range" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_dma_get_range \- Get device DMA parameters.
.SH SYNOPSIS
.B "int" acpi_dma_get_range
.BI "(struct device *dev "  ","
.BI "u64 *dma_addr "  ","
.BI "u64 *offset "  ","
.BI "u64 *size "  ");"
.SH ARGUMENTS
.IP "dev" 12
device to configure
.IP "dma_addr" 12
pointer device DMA address result
.IP "offset" 12
pointer to the DMA offset result
.IP "size" 12
pointer to DMA range size result
.SH "DESCRIPTION"
Evaluate DMA regions and return respectively DMA region start, offset
and size in dma_addr, offset and size on parsing success; it does not
update the passed in values on failure.

Return 0 on success, < 0 on failure.
