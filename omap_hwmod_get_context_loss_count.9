.TH "omap_hwmod_get_context_loss_count" 9 "omap_hwmod_get_context_loss_count" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_hwmod_get_context_loss_count \- get lost context count
.SH SYNOPSIS
.B "int" omap_hwmod_get_context_loss_count
.BI "(struct omap_hwmod *oh "  ");"
.SH ARGUMENTS
.IP "oh" 12
struct omap_hwmod *
.SH "DESCRIPTION"
Returns the context loss count of associated \fIoh\fP
upon success, or zero if no context loss data is available.

On OMAP4, this queries the per-hwmod context loss register,
assuming one exists.  If not, or on OMAP2/3, this queries the
enclosing powerdomain context loss count.
