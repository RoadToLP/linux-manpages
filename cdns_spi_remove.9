.TH "cdns_spi_remove" 9 "cdns_spi_remove" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
cdns_spi_remove \- Remove method for the SPI driver
.SH SYNOPSIS
.B "int" cdns_spi_remove
.BI "(struct platform_device *pdev "  ");"
.SH ARGUMENTS
.IP "pdev" 12
Pointer to the platform_device structure
.SH "DESCRIPTION"
This function is called if a device is physically removed from the system or
if the driver module is being unloaded. It frees all resources allocated to
the device.
.SH "RETURN"
0 on success and error value on error
