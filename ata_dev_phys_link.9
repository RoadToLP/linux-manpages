.TH "ata_dev_phys_link" 9 "ata_dev_phys_link" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ata_dev_phys_link \- find physical link for a device
.SH SYNOPSIS
.B "struct ata_link *" ata_dev_phys_link
.BI "(struct ata_device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
ATA device to look up physical link for

Look up physical link which \fIdev\fP is attached to.  Note that
this is different from \fIdev->link\fP only when \fIdev\fP is on slave
link.  For all other cases, it's the same as \fIdev->link\fP.

LOCKING:
Don't care.
.SH "RETURN"
Pointer to the found physical link.
