.TH "acpi_dev_resource_memory" 9 "acpi_dev_resource_memory" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
acpi_dev_resource_memory \- Extract ACPI memory resource information.
.SH SYNOPSIS
.B "bool" acpi_dev_resource_memory
.BI "(struct acpi_resource *ares "  ","
.BI "struct resource *res "  ");"
.SH ARGUMENTS
.IP "ares" 12
Input ACPI resource object.
.IP "res" 12
Output generic resource object.
.SH "DESCRIPTION"
Check if the given ACPI resource object represents a memory resource and
if that's the case, use the information in it to populate the generic
resource object pointed to by \fIres\fP.
.SH "RETURN"
1) false with res->flags setting to zero: not the expected resource type
2) false with IORESOURCE_DISABLED in res->flags: valid unassigned resource
3) true: valid assigned resource
