.TH "ssam_controller_resume" 9 "ssam_controller_resume" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ssam_controller_resume \- Resume the controller from suspend.
.SH SYNOPSIS
.B "int" ssam_controller_resume
.BI "(struct ssam_controller *ctrl "  ");"
.SH ARGUMENTS
.IP "ctrl" 12
The controller to resume.
.SH "DESCRIPTION"
Resume the controller from the suspended state it was put into via
\fBssam_controller_suspend\fP. This function does not issue display-on and
D0-entry notifications. If required, those have to be sent manually after
this call.
.SH "RETURN"
Returns -EINVAL if the controller is currently not suspended.
