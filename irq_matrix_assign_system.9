.TH "irq_matrix_assign_system" 9 "irq_matrix_assign_system" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
irq_matrix_assign_system \- Assign system wide entry in the matrix
.SH SYNOPSIS
.B "void" irq_matrix_assign_system
.BI "(struct irq_matrix *m "  ","
.BI "unsigned int bit "  ","
.BI "bool replace "  ");"
.SH ARGUMENTS
.IP "m" 12
Matrix pointer
.IP "bit" 12
Which bit to reserve
.IP "replace" 12
Replace an already allocated vector with a system
vector at the same bit position.
.SH "DESCRIPTION"
The \fBBUG_ON\fPs below are on purpose. If this goes wrong in the
early boot process, then the chance to survive is about zero.
If this happens when the system is life, it's not much better.
