.TH "binder_free_buf" 9 "binder_free_buf" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
binder_free_buf \- free the specified buffer
.SH SYNOPSIS
.B "void" binder_free_buf
.BI "(struct binder_proc *proc "  ","
.BI "struct binder_buffer *buffer "  ");"
.SH ARGUMENTS
.IP "proc" 12
binder proc that owns buffer
.IP "buffer" 12
buffer to be freed
.SH "DESCRIPTION"
If buffer for an async transaction, enqueue the next async
transaction from the node.

Cleanup buffer and free it.
