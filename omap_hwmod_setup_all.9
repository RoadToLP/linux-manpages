.TH "omap_hwmod_setup_all" 9 "omap_hwmod_setup_all" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
omap_hwmod_setup_all \- set up all registered IP blocks
.SH SYNOPSIS
.B "int" omap_hwmod_setup_all
.BI "(void "  ");"
.SH ARGUMENTS
.IP "void" 12
no arguments
.SH "DESCRIPTION"

Initialize and set up all IP blocks registered with the hwmod code.
Must be called after \fBomap2_clk_init\fP.  Resolves the struct clk
names to struct clk pointers for each registered omap_hwmod.  Also
calls \fB_setup\fP on each hwmod.  Returns 0 upon success.
