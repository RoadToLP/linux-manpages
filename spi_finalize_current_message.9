.TH "spi_finalize_current_message" 9 "spi_finalize_current_message" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
spi_finalize_current_message \- the current message is complete
.SH SYNOPSIS
.B "void" spi_finalize_current_message
.BI "(struct spi_controller *ctlr "  ");"
.SH ARGUMENTS
.IP "ctlr" 12
the controller to return the message to
.SH "DESCRIPTION"
Called by the driver to notify the core that the message in the front of the
queue is complete and can be removed from the queue.
