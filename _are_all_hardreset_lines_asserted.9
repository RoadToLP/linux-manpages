.TH "_are_all_hardreset_lines_asserted" 9 "_are_all_hardreset_lines_asserted" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
_are_all_hardreset_lines_asserted \- return true if the @oh is hard-reset
.SH SYNOPSIS
.B "bool" _are_all_hardreset_lines_asserted
.BI "(struct omap_hwmod *oh "  ");"
.SH ARGUMENTS
.IP "oh" 12
struct omap_hwmod *
.SH "DESCRIPTION"
If all hardreset lines associated with \fIoh\fP are asserted, then return true.
Otherwise, if part of \fIoh\fP is out hardreset or if no hardreset lines
associated with \fIoh\fP are asserted, then return false.
This function is used to avoid executing some parts of the IP block
enable/disable sequence if its hardreset line is set.
