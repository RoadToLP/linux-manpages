.TH "ice_check_vf_ready_for_cfg" 9 "ice_check_vf_ready_for_cfg" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ice_check_vf_ready_for_cfg \- check if VF is ready to be configured/queried
.SH SYNOPSIS
.B "int" ice_check_vf_ready_for_cfg
.BI "(struct ice_vf *vf "  ");"
.SH ARGUMENTS
.IP "vf" 12
VF to check if it's ready to be configured/queried
.SH "DESCRIPTION"
The purpose of this function is to make sure the VF is not in reset, not
disabled, and initialized so it can be configured and/or queried by a host
administrator.
