.TH "tcw_set_data" 9 "tcw_set_data" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
tcw_set_data \- set data address and tida flag of a tcw
.SH SYNOPSIS
.B "void" tcw_set_data
.BI "(struct tcw *tcw "  ","
.BI "void *data "  ","
.BI "int use_tidal "  ");"
.SH ARGUMENTS
.IP "tcw" 12
the tcw address
.IP "data" 12
the data address
.IP "use_tidal" 12
zero of the data address specifies a contiguous block of data,
non-zero if it specifies a list if tidaws.
.SH "DESCRIPTION"
Set the input/output data address of a tcw (depending on the value of the
r-flag and w-flag). If \fIuse_tidal\fP is non-zero, the corresponding tida flag
is set as well.
