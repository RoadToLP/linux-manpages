.TH "cma_acquire_dev_by_src_ip" 9 "cma_acquire_dev_by_src_ip" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
cma_acquire_dev_by_src_ip \- Acquire cma device, port, gid attribute based on source ip address.
.SH SYNOPSIS
.B "int" cma_acquire_dev_by_src_ip
.BI "(struct rdma_id_private *id_priv "  ");"
.SH ARGUMENTS
.IP "id_priv" 12
cm_id which should be bound to cma device
.SH "DESCRIPTION"
\fBcma_acquire_dev_by_src_ip\fP binds cm id to cma device, port and GID attribute
based on source IP address. It returns 0 on success or error code otherwise.
It is applicable to active and passive side cm_id.
