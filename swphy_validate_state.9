.TH "swphy_validate_state" 9 "swphy_validate_state" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
swphy_validate_state \- validate the software phy status
.SH SYNOPSIS
.B "int" swphy_validate_state
.BI "(const struct fixed_phy_status *state "  ");"
.SH ARGUMENTS
.IP "state" 12
software phy status
.SH "DESCRIPTION"
This checks that we can represent the state stored in \fIstate\fP can be
represented in the emulated MII registers.  Returns 0 if it can,
otherwise returns -EINVAL.
