.TH "mei_nfc" 9 "mei_nfc" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
mei_nfc \- The nfc fixup function. The function retrieves nfc radio name and set is as device attribute so we can load the proper device driver for it
.SH SYNOPSIS
.B "void" mei_nfc
.BI "(struct mei_cl_device *cldev "  ");"
.SH ARGUMENTS
.IP "cldev" 12
me client device (nfc)
