.TH "drm_add_edid_modes" 9 "drm_add_edid_modes" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
drm_add_edid_modes \- add modes from EDID data, if available
.SH SYNOPSIS
.B "int" drm_add_edid_modes
.BI "(struct drm_connector *connector "  ","
.BI "struct edid *edid "  ");"
.SH ARGUMENTS
.IP "connector" 12
connector we're probing
.IP "edid" 12
EDID data
.SH "DESCRIPTION"
Add the specified modes to the connector's mode list. Also fills out the
\fIdrm_display_info\fP structure and ELD in \fIconnector\fP with any information which
can be derived from the edid.
.SH "RETURN"
The number of modes added or 0 if we couldn't find any.
