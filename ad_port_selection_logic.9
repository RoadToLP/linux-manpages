.TH "ad_port_selection_logic" 9 "ad_port_selection_logic" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ad_port_selection_logic \- select aggregation groups
.SH SYNOPSIS
.B "void" ad_port_selection_logic
.BI "(struct port *port "  ","
.BI "bool *update_slave_arr "  ");"
.SH ARGUMENTS
.IP "port" 12
the port we're looking at
.IP "update_slave_arr" 12
Does slave array need update?
.SH "DESCRIPTION"
Select aggregation groups, and assign each port for it's aggregetor. The
selection logic is called in the inititalization (after all the handshkes),
and after every lacpdu receive (if selected is off).
