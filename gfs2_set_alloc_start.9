.TH "gfs2_set_alloc_start" 9 "gfs2_set_alloc_start" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
gfs2_set_alloc_start \- Set starting point for block allocation
.SH SYNOPSIS
.B "void" gfs2_set_alloc_start
.BI "(struct gfs2_rbm *rbm "  ","
.BI "const struct gfs2_inode *ip "  ","
.BI "bool dinode "  ");"
.SH ARGUMENTS
.IP "rbm" 12
The rbm which will be set to the required location
.IP "ip" 12
The gfs2 inode
.IP "dinode" 12
Flag to say if allocation includes a new inode
.SH "DESCRIPTION"
This sets the starting point from the reservation if one is active
otherwise it falls back to guessing a start point based on the
inode's goal block or the last allocation point in the rgrp.
