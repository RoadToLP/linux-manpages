.TH "intel_runtime_pm_get" 9 "intel_runtime_pm_get" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
intel_runtime_pm_get \- grab a runtime pm reference
.SH SYNOPSIS
.B "intel_wakeref_t" intel_runtime_pm_get
.BI "(struct intel_runtime_pm *rpm "  ");"
.SH ARGUMENTS
.IP "rpm" 12
the intel_runtime_pm structure
.SH "DESCRIPTION"
This function grabs a device-level runtime pm reference (mostly used for GEM
code to ensure the GTT or GT is on) and ensures that it is powered up.

Any runtime pm reference obtained by this function must have a symmetric
call to \fBintel_runtime_pm_put\fP to release the reference again.
.SH "RETURN"
the wakeref cookie to pass to \fBintel_runtime_pm_put\fP
