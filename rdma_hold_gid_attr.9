.TH "rdma_hold_gid_attr" 9 "rdma_hold_gid_attr" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
rdma_hold_gid_attr \- Get reference to existing GID attribute
.SH SYNOPSIS
.B "void" rdma_hold_gid_attr
.BI "(const struct ib_gid_attr *attr "  ");"
.SH ARGUMENTS
.IP "attr" 12
Pointer to the GID attribute whose reference
needs to be taken.
.SH "DESCRIPTION"
Increase the reference count to a GID attribute to keep it from being
freed. Callers are required to already be holding a reference to attribute.
