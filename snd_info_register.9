.TH "snd_info_register" 9 "snd_info_register" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_info_register \- register the info entry
.SH SYNOPSIS
.B "int" snd_info_register
.BI "(struct snd_info_entry *entry "  ");"
.SH ARGUMENTS
.IP "entry" 12
the info entry
.SH "DESCRIPTION"
Registers the proc info entry.
The all children entries are registered recursively.
.SH "RETURN"
Zero if successful, or a negative error code on failure.
