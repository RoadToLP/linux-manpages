.TH "start_userspace" 9 "start_userspace" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
start_userspace \- prepare a new userspace process
.SH SYNOPSIS
.B "int" start_userspace
.BI "(unsigned long stub_stack "  ");"
.SH ARGUMENTS
.IP "stub_stack" 12
pointer to the stub stack. Can be NULL, if? FIXME:
.SH "DESCRIPTION"
Setups a new temporary stack page that is used while \fBuserspace_tramp\fP runs
Clones the kernel process into a new userspace process, with FDs only.
.SH "RETURN"
When positive: the process id of the new userspace process,
when negative: an error number.
FIXME: can PIDs become negative?!
