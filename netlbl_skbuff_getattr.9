.TH "netlbl_skbuff_getattr" 9 "netlbl_skbuff_getattr" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
netlbl_skbuff_getattr \- Determine the security attributes of a packet
.SH SYNOPSIS
.B "int" netlbl_skbuff_getattr
.BI "(const struct sk_buff *skb "  ","
.BI "u16 family "  ","
.BI "struct netlbl_lsm_secattr *secattr "  ");"
.SH ARGUMENTS
.IP "skb" 12
the packet
.IP "family" 12
protocol family
.IP "secattr" 12
the security attributes
.SH "DESCRIPTION"
Examines the given packet to see if a recognized form of packet labeling
is present, if so it parses the packet label and returns the security
attributes in \fIsecattr\fP.  Returns zero on success, negative values on
failure.
