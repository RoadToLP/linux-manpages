.TH "ntb_msi_peer_trigger" 9 "ntb_msi_peer_trigger" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ntb_msi_peer_trigger \- Trigger an interrupt handler on a peer
.SH SYNOPSIS
.B "int" ntb_msi_peer_trigger
.BI "(struct ntb_dev *ntb "  ","
.BI "int peer "  ","
.BI "struct ntb_msi_desc *desc "  ");"
.SH ARGUMENTS
.IP "ntb" 12
NTB device context
.IP "peer" 12
Peer index
.IP "desc" 12
MSI descriptor data which triggers the interrupt
.SH "DESCRIPTION"
This function triggers an interrupt on a peer. It requires
the descriptor structure to have been passed from that peer
by some other means.
.SH "RETURN"
Zero on success, otherwise a negative error number.
