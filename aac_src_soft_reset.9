.TH "aac_src_soft_reset" 9 "aac_src_soft_reset" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
aac_src_soft_reset \- perform soft reset to speed up access
.SH SYNOPSIS
.B "int" aac_src_soft_reset
.BI "(struct aac_dev *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
device to configure
.SH "DESCRIPTION"

Assumptions: That the controller is in a state where we can
bring it back to life with an init struct. We can only use
fast sync commands, as the timeout is 5 seconds.
