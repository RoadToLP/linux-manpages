.TH "bringup_hibernate_cpu" 9 "bringup_hibernate_cpu" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
bringup_hibernate_cpu \- Bring up the CPU that we hibernated on
.SH SYNOPSIS
.B "int" bringup_hibernate_cpu
.BI "(unsigned int sleep_cpu "  ");"
.SH ARGUMENTS
.IP "sleep_cpu" 12
The cpu we hibernated on and should be brought up.
.SH "DESCRIPTION"
On some architectures like arm64, we can hibernate on any CPU, but on
wake up the CPU we hibernated on might be offline as a side effect of
using maxcpus= for example.
