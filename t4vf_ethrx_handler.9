.TH "t4vf_ethrx_handler" 9 "t4vf_ethrx_handler" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
t4vf_ethrx_handler \- process an ingress ethernet packet
.SH SYNOPSIS
.B "int" t4vf_ethrx_handler
.BI "(struct sge_rspq *rspq "  ","
.BI "const __be64 *rsp "  ","
.BI "const struct pkt_gl *gl "  ");"
.SH ARGUMENTS
.IP "rspq" 12
the response queue that received the packet
.IP "rsp" 12
the response queue descriptor holding the RX_PKT message
.IP "gl" 12
the gather list of packet fragments

Process an ingress ethernet packet and deliver it to the stack.
