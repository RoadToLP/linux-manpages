.TH "ext2_alloc_blocks" 9 "ext2_alloc_blocks" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ext2_alloc_blocks \- multiple allocate blocks needed for a branch
.SH SYNOPSIS
.B "int" ext2_alloc_blocks
.BI "(struct inode *inode "  ","
.BI "ext2_fsblk_t goal "  ","
.BI "int indirect_blks "  ","
.BI "int blks "  ","
.BI "ext2_fsblk_t new_blocks[4] "  ","
.BI "int *err "  ");"
.SH ARGUMENTS
.IP "inode" 12
-- undescribed --
.IP "goal" 12
-- undescribed --
.IP "indirect_blks" 12
the number of blocks need to allocate for indirect
blocks
.IP "blks" 12
the number of blocks need to allocate for direct blocks
.IP "new_blocks" 12
on return it will store the new block numbers for
the indirect blocks(if needed) and the first direct block,
.IP "err" 12
-- undescribed --
