.TH "amdgpu_dma_buf_move_notify" 9 "amdgpu_dma_buf_move_notify" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
amdgpu_dma_buf_move_notify \- &attach.move_notify implementation
.SH SYNOPSIS
.B "void" amdgpu_dma_buf_move_notify
.BI "(struct dma_buf_attachment *attach "  ");"
.SH ARGUMENTS
.IP "attach" 12
the DMA-buf attachment
.SH "DESCRIPTION"
Invalidate the DMA-buf attachment, making sure that the we re-create the
mapping before the next use.
