.TH "ips_detect_cpu" 9 "ips_detect_cpu" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ips_detect_cpu \- detect whether CPU supports IPS
.SH SYNOPSIS
.B "struct ips_mcp_limits *" ips_detect_cpu
.BI "(struct ips_driver *ips "  ");"
.SH ARGUMENTS
.IP "ips" 12
-- undescribed --
.SH "DESCRIPTION"

Walk our list and see if we're on a supported CPU.  If we find one,
return the limits for it.
