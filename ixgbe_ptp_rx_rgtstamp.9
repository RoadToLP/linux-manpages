.TH "ixgbe_ptp_rx_rgtstamp" 9 "ixgbe_ptp_rx_rgtstamp" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ixgbe_ptp_rx_rgtstamp \- utility function which checks for RX time stamp
.SH SYNOPSIS
.B "void" ixgbe_ptp_rx_rgtstamp
.BI "(struct ixgbe_q_vector *q_vector "  ","
.BI "struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "q_vector" 12
structure containing interrupt and ring information
.IP "skb" 12
particular skb to send timestamp with
.SH "DESCRIPTION"
if the timestamp is valid, we convert it into the timecounter ns
value, then store that result into the shhwtstamps structure which
is passed up the network stack
