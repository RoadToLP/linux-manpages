.TH "adapter_init_params" 9 "adapter_init_params" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
adapter_init_params \- Initialize the various parameters in the adapter structure. Note that the pointer to the scsi_host is set early (when this instance is created) and the io_port and irq values are set later after they have been reserved. This just gets everything set to a good starting position.
.SH SYNOPSIS
.B "void" adapter_init_params
.BI "(struct AdapterCtlBlk *acb "  ");"
.SH ARGUMENTS
.IP "acb" 12
The adapter to initialize.
.SH "DESCRIPTION"

The eeprom structure in the adapter needs to have been set before
calling this function.
