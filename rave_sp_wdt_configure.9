.TH "rave_sp_wdt_configure" 9 "rave_sp_wdt_configure" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
rave_sp_wdt_configure \- Configure watchdog device
.SH SYNOPSIS
.B "int" rave_sp_wdt_configure
.BI "(struct watchdog_device *wdd "  ","
.BI "bool on "  ");"
.SH ARGUMENTS
.IP "wdd" 12
Device to configure
.IP "on" 12
Desired state of the watchdog timer (ON/OFF)
.SH "DESCRIPTION"
This function configures two aspects of the watchdog timer:

- Wheither it is ON or OFF
- Its timeout duration

with first aspect specified via function argument and second via
the value of 'wdd->timeout'.
