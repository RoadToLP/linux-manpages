.TH "batadv_mcast_forw_mode_check_ipv6" 9 "batadv_mcast_forw_mode_check_ipv6" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_mcast_forw_mode_check_ipv6 \- check for optimized forwarding potential
.SH SYNOPSIS
.B "int" batadv_mcast_forw_mode_check_ipv6
.BI "(struct batadv_priv *bat_priv "  ","
.BI "struct sk_buff *skb "  ","
.BI "bool *is_unsnoopable "  ","
.BI "int *is_routable "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "skb" 12
the IPv6 packet to check
.IP "is_unsnoopable" 12
stores whether the destination is snoopable
.IP "is_routable" 12
stores whether the destination is routable
.SH "DESCRIPTION"
Checks whether the given IPv6 packet has the potential to be forwarded with a
mode more optimal than classic flooding.
.SH "RETURN"
If so then 0. Otherwise -EINVAL is or -ENOMEM if we are out of memory
