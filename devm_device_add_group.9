.TH "devm_device_add_group" 9 "devm_device_add_group" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
devm_device_add_group \- given a device, create a managed attribute group
.SH SYNOPSIS
.B "int" devm_device_add_group
.BI "(struct device *dev "  ","
.BI "const struct attribute_group *grp "  ");"
.SH ARGUMENTS
.IP "dev" 12
The device to create the group for
.IP "grp" 12
The attribute group to create
.SH "DESCRIPTION"
This function creates a group for the first time.  It will explicitly
warn and error if any of the attribute files being created already exist.

Returns 0 on success or error code on failure.
