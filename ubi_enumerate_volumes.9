.TH "ubi_enumerate_volumes" 9 "ubi_enumerate_volumes" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ubi_enumerate_volumes \- send "add" notification for all existing volumes.
.SH SYNOPSIS
.B "int" ubi_enumerate_volumes
.BI "(struct notifier_block *nb "  ");"
.SH ARGUMENTS
.IP "nb" 12
the notifier to call
.SH "DESCRIPTION"
This function walks all UBI devices and volumes and sends the
UBI_VOLUME_ADDED notification for each volume. If \fInb\fP is NULL, then all
registered notifiers are called, otherwise only the \fInb\fP notifier is called.
Returns the number of sent notifications.
