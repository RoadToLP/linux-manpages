.TH "ata_rwcmd_protocol" 9 "ata_rwcmd_protocol" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ata_rwcmd_protocol \- set taskfile r/w commands and protocol
.SH SYNOPSIS
.B "int" ata_rwcmd_protocol
.BI "(struct ata_taskfile *tf "  ","
.BI "struct ata_device *dev "  ");"
.SH ARGUMENTS
.IP "tf" 12
command to examine and configure
.IP "dev" 12
device tf belongs to

Examine the device configuration and tf->flags to calculate
the proper read/write commands and protocol to use.

LOCKING:
caller.
