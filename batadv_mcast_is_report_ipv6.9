.TH "batadv_mcast_is_report_ipv6" 9 "batadv_mcast_is_report_ipv6" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_mcast_is_report_ipv6 \- check for MLD reports
.SH SYNOPSIS
.B "bool" batadv_mcast_is_report_ipv6
.BI "(struct sk_buff *skb "  ");"
.SH ARGUMENTS
.IP "skb" 12
the ethernet frame destined for the mesh
.SH "DESCRIPTION"
This call might reallocate skb data.

Checks whether the given frame is a valid MLD report.
.SH "RETURN"
If so then true, otherwise false.
