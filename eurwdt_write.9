.TH "eurwdt_write" 9 "eurwdt_write" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
eurwdt_write \- 
.SH SYNOPSIS
.B "ssize_t" eurwdt_write
.BI "(struct file *file "  ","
.BI "const char __user *buf "  ","
.BI "size_t count "  ","
.BI "loff_t *ppos "  ");"
.SH ARGUMENTS
.IP "file" 12
file handle to the watchdog
.IP "buf" 12
buffer to write (unused as data does not matter here
.IP "count" 12
count of bytes
.IP "ppos" 12
pointer to the position to write. No seeks allowed
.SH "DESCRIPTION"
A write to a watchdog device is defined as a keepalive signal. Any
write of data will do, as we we don't define content meaning.
