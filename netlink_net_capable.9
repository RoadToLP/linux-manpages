.TH "netlink_net_capable" 9 "netlink_net_capable" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
netlink_net_capable \- Netlink network namespace message capability test
.SH SYNOPSIS
.B "bool" netlink_net_capable
.BI "(const struct sk_buff *skb "  ","
.BI "int cap "  ");"
.SH ARGUMENTS
.IP "skb" 12
socket buffer holding a netlink command from userspace
.IP "cap" 12
The capability to use
.SH "DESCRIPTION"
Test to see if the opener of the socket we received the message
from had when the netlink socket was created and the sender of the
message has the capability \fIcap\fP over the network namespace of
the socket we received the message from.
