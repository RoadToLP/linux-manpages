.TH "dio_bus_match" 9 "dio_bus_match" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
dio_bus_match \- Tell if a DIO device structure has a matching DIO device id structure
.SH SYNOPSIS
.B "int" dio_bus_match
.BI "(struct device *dev "  ","
.BI "struct device_driver *drv "  ");"
.SH ARGUMENTS
.IP "dev" 12
the DIO device structure to match against
.IP "drv" 12
the \fIdevice_driver\fP that points to the array of DIO device id structures to search

Used by the driver core to check whether a DIO device present in the
system is in a driver's list of supported devices. Returns 1 if supported,
and 0 if there is no match.
