.TH "ata_scsi_remove_dev" 9 "ata_scsi_remove_dev" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
ata_scsi_remove_dev \- remove attached SCSI device
.SH SYNOPSIS
.B "void" ata_scsi_remove_dev
.BI "(struct ata_device *dev "  ");"
.SH ARGUMENTS
.IP "dev" 12
ATA device to remove attached SCSI device for

This function is called from \fBata_eh_scsi_hotplug\fP and
responsible for removing the SCSI device attached to \fIdev\fP.

LOCKING:
Kernel thread context (may sleep).
