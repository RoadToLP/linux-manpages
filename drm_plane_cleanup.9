.TH "drm_plane_cleanup" 9 "drm_plane_cleanup" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
drm_plane_cleanup \- Clean up the core plane usage
.SH SYNOPSIS
.B "void" drm_plane_cleanup
.BI "(struct drm_plane *plane "  ");"
.SH ARGUMENTS
.IP "plane" 12
plane to cleanup
.SH "DESCRIPTION"
This function cleans up \fIplane\fP and removes it from the DRM mode setting
core. Note that the function does *not* free the plane structure itself,
this is the responsibility of the caller.
