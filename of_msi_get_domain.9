.TH "of_msi_get_domain" 9 "of_msi_get_domain" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
of_msi_get_domain \- Use msi-parent to find the relevant MSI domain
.SH SYNOPSIS
.B "struct irq_domain *" of_msi_get_domain
.BI "(struct device *dev "  ","
.BI "struct device_node *np "  ","
.BI "enum irq_domain_bus_token token "  ");"
.SH ARGUMENTS
.IP "dev" 12
device for which the domain is requested
.IP "np" 12
device node for \fIdev\fP
.IP "token" 12
bus type for this domain
.SH "DESCRIPTION"
Parse the msi-parent property (both the simple and the complex
versions), and returns the corresponding MSI domain.
.SH "RETURN"
the MSI domain for this device (or NULL on failure).
