.TH "clone_private_mount" 9 "clone_private_mount" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
clone_private_mount \- create a private clone of a path
.SH SYNOPSIS
.B "struct vfsmount *" clone_private_mount
.BI "(const struct path *path "  ");"
.SH ARGUMENTS
.IP "path" 12
-- undescribed --
.SH "DESCRIPTION"

This creates a new vfsmount, which will be the clone of \fIpath\fP.  The new will
not be attached anywhere in the namespace and will be private (i.e. changes
to the originating mount won't be propagated into this).

Release with \fBmntput\fP.
