.TH "may_change_ptraced_domain" 9 "may_change_ptraced_domain" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
may_change_ptraced_domain \- check if can change profile on ptraced task
.SH SYNOPSIS
.B "int" may_change_ptraced_domain
.BI "(struct aa_label *to_label "  ","
.BI "const char **info "  ");"
.SH ARGUMENTS
.IP "to_label" 12
profile to change to  (NOT NULL)
.IP "info" 12
message if there is an error
.SH "DESCRIPTION"
Check if current is ptraced and if so if the tracing task is allowed
to trace the new domain
.SH "RETURN"
0 or error if change not allowed
