.TH "psc_dma_bcom_enqueue_next_buffer" 9 "psc_dma_bcom_enqueue_next_buffer" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
psc_dma_bcom_enqueue_next_buffer \- Enqueue another audio buffer
.SH SYNOPSIS
.B "void" psc_dma_bcom_enqueue_next_buffer
.BI "(struct psc_dma_stream *s "  ");"
.SH ARGUMENTS
.IP "s" 12
pointer to stream private data structure
.SH "DESCRIPTION"
Enqueues another audio period buffer into the bestcomm queue.
.SH "NOTE"
The routine must only be called when there is space available in
the queue.  Otherwise the enqueue will fail and the audio ring buffer
will get out of sync
