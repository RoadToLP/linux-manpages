.TH "i3c_master_entdaa_locked" 9 "i3c_master_entdaa_locked" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
i3c_master_entdaa_locked \- start a DAA (Dynamic Address Assignment) procedure
.SH SYNOPSIS
.B "int" i3c_master_entdaa_locked
.BI "(struct i3c_master_controller *master "  ");"
.SH ARGUMENTS
.IP "master" 12
master used to send frames on the bus
.SH "DESCRIPTION"
Send a ENTDAA CCC command to start a DAA procedure.

Note that this function only sends the ENTDAA CCC command, all the logic
behind dynamic address assignment has to be handled in the I3C master
driver.

This function must be called with the bus lock held in write mode.
.SH "RETURN"
0 in case of success, a positive I3C error code if the error is
one of the official Mx error codes, and a negative error code otherwise.
