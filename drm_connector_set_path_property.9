.TH "drm_connector_set_path_property" 9 "drm_connector_set_path_property" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
drm_connector_set_path_property \- set tile property on connector
.SH SYNOPSIS
.B "int" drm_connector_set_path_property
.BI "(struct drm_connector *connector "  ","
.BI "const char *path "  ");"
.SH ARGUMENTS
.IP "connector" 12
connector to set property on.
.IP "path" 12
path to use for property; must not be NULL.
.SH "DESCRIPTION"
This creates a property to expose to userspace to specify a
connector path. This is mainly used for DisplayPort MST where
connectors have a topology and we want to allow userspace to give
them more meaningful names.
.SH "RETURN"
Zero on success, negative errno on failure.
