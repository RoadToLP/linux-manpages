.TH "gpmc_read_settings_dt" 9 "gpmc_read_settings_dt" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
gpmc_read_settings_dt \- read gpmc settings from device-tree
.SH SYNOPSIS
.B "void" gpmc_read_settings_dt
.BI "(struct device_node *np "  ","
.BI "struct gpmc_settings *p "  ");"
.SH ARGUMENTS
.IP "np" 12
pointer to device-tree node for a gpmc child device
.IP "p" 12
pointer to gpmc settings structure
.SH "DESCRIPTION"
Reads the GPMC settings for a GPMC child device from device-tree and
stores them in the GPMC settings structure passed. The GPMC settings
structure is initialised to zero by this function and so any
previously stored settings will be cleared.
