.TH "iommu_group_add_device" 9 "iommu_group_add_device" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
iommu_group_add_device \- add a device to an iommu group
.SH SYNOPSIS
.B "int" iommu_group_add_device
.BI "(struct iommu_group *group "  ","
.BI "struct device *dev "  ");"
.SH ARGUMENTS
.IP "group" 12
the group into which to add the device (reference should be held)
.IP "dev" 12
the device
.SH "DESCRIPTION"
This function is called by an iommu driver to add a device into a
group.  Adding a device increments the group reference count.
