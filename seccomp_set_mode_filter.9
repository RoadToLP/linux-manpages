.TH "seccomp_set_mode_filter" 9 "seccomp_set_mode_filter" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
seccomp_set_mode_filter \- internal function for setting seccomp filter
.SH SYNOPSIS
.B "long" seccomp_set_mode_filter
.BI "(unsigned int flags "  ","
.BI "const char __user *filter "  ");"
.SH ARGUMENTS
.IP "flags" 12
flags to change filter behavior
.IP "filter" 12
struct sock_fprog containing filter
.SH "DESCRIPTION"
This function may be called repeatedly to install additional filters.
Every filter successfully installed will be evaluated (in reverse order)
for each system call the task makes.

Once current->seccomp.mode is non-zero, it may not be changed.

Returns 0 on success or -EINVAL on failure.
