.TH "inode_dax" 9 "inode_dax" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
inode_dax \- convert a public inode into its dax_dev
.SH SYNOPSIS
.B "struct dax_device *" inode_dax
.BI "(struct inode *inode "  ");"
.SH ARGUMENTS
.IP "inode" 12
An inode with i_cdev pointing to a dax_dev
.SH "DESCRIPTION"
Note this is not equivalent to \fBto_dax_dev\fP which is for private
internal use where we know the inode filesystem type == dax_fs_type.
