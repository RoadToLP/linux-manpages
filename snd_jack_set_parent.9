.TH "snd_jack_set_parent" 9 "snd_jack_set_parent" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
snd_jack_set_parent \- Set the parent device for a jack
.SH SYNOPSIS
.B "void" snd_jack_set_parent
.BI "(struct snd_jack *jack "  ","
.BI "struct device *parent "  ");"
.SH ARGUMENTS
.IP "jack" 12
The jack to configure
.IP "parent" 12
The device to set as parent for the jack.
.SH "DESCRIPTION"
Set the parent for the jack devices in the device tree.  This
function is only valid prior to registration of the jack.  If no
parent is configured then the parent device will be the sound card.
