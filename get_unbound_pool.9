.TH "get_unbound_pool" 9 "get_unbound_pool" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
get_unbound_pool \- get a worker_pool with the specified attributes
.SH SYNOPSIS
.B "struct worker_pool *" get_unbound_pool
.BI "(const struct workqueue_attrs *attrs "  ");"
.SH ARGUMENTS
.IP "attrs" 12
the attributes of the worker_pool to get
.SH "DESCRIPTION"
Obtain a worker_pool which has the same attributes as \fIattrs\fP, bump the
reference count and return it.  If there already is a matching
worker_pool, it will be used; otherwise, this function attempts to
create a new one.

Should be called with wq_pool_mutex held.
.SH "RETURN"
On success, a worker_pool with the same attributes as \fIattrs\fP.
On failure, NULL.
