.TH "batadv_tt_local_client_is_roaming" 9 "batadv_tt_local_client_is_roaming" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_tt_local_client_is_roaming \- tells whether the client is roaming
.SH SYNOPSIS
.B "bool" batadv_tt_local_client_is_roaming
.BI "(struct batadv_priv *bat_priv "  ","
.BI "u8 *addr "  ","
.BI "unsigned short vid "  ");"
.SH ARGUMENTS
.IP "bat_priv" 12
the bat priv with all the soft interface information
.IP "addr" 12
the mac address of the local client to query
.IP "vid" 12
VLAN identifier
.SH "RETURN"
true if the local client is known to be roaming (it is not served by
this node anymore) or not. If yes, the client is still present in the table
to keep the latter consistent with the node TTVN
