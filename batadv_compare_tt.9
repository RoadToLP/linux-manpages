.TH "batadv_compare_tt" 9 "batadv_compare_tt" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
batadv_compare_tt \- check if two TT entries are the same
.SH SYNOPSIS
.B "bool" batadv_compare_tt
.BI "(const struct hlist_node *node "  ","
.BI "const void *data2 "  ");"
.SH ARGUMENTS
.IP "node" 12
the list element pointer of the first TT entry
.IP "data2" 12
pointer to the tt_common_entry of the second TT entry
.SH "DESCRIPTION"
Compare the MAC address and the VLAN ID of the two TT entries and check if
they are the same TT client.
.SH "RETURN"
true if the two TT clients are the same, false otherwise
