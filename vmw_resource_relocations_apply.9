.TH "vmw_resource_relocations_apply" 9 "vmw_resource_relocations_apply" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
vmw_resource_relocations_apply \- Apply all relocations on a list
.SH SYNOPSIS
.B "void" vmw_resource_relocations_apply
.BI "(uint32_t *cb "  ","
.BI "struct list_head *list "  ");"
.SH ARGUMENTS
.IP "cb" 12
Pointer to the start of the command buffer bein patch. This need not be
the same buffer as the one being parsed when the relocation list was built,
but the contents must be the same modulo the resource ids.
.IP "list" 12
Pointer to the head of the relocation list.
