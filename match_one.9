.TH "match_one" 9 "match_one" "May 2021" "Kernel Hacker's Manual" LINUX
.SH NAME
match_one \- Determines if a string matches a simple pattern
.SH SYNOPSIS
.B "int" match_one
.BI "(char *s "  ","
.BI "const char *p "  ","
.BI "substring_t args[] "  ");"
.SH ARGUMENTS
.IP "s" 12
the string to examine for presence of the pattern
.IP "p" 12
the string containing the pattern
.IP "args" 12
array of MAX_OPT_ARGS \fIsubstring_t\fP elements. Used to return match
locations.
.SH "DESCRIPTION"
Determines if the pattern \fIp\fP is present in string \fIs\fP. Can only
match extremely simple token=arg style patterns. If the pattern is found,
the location(s) of the arguments will be returned in the \fIargs\fP array.
